---
feature_id: "039"
task_id: "T021c"
task_summary: "Add lease-aware idempotent replay and next-boundary reconciliation decisions in scripts/speckit_workflow_reconcile.py so committed events, attempts, artifact state, and the cursor converge without duplicate effects."
story_id: "US3"
primary_edit_seam: "scripts/speckit_workflow_reconcile.py:reconcile_replay_boundary"
related_paths: ["scripts/feature_lock.py", "scripts/pipeline_ledger.py", "scripts/task_ledger.py"]
---

# HUD: T021c - Lease-aware idempotent replay

## Objective

Deliver `T021c` so the task scope satisfies: Add lease-aware idempotent replay and next-boundary reconciliation decisions in scripts/speckit_workflow_reconcile.py so committed events, attempts, artifact state, and the cursor converge without duplicate effects..

## Relevant Domains

- resilience: replay after nested resume must not duplicate effects.
- ops governance: lease and idempotency identity constrain every effect.
- observability: bridge records link attempts to cursor/ledger/artifact evidence.

## Candidate Design Slices

- **PL-06 — Add Durable Resume, Reconciliation, Lease, and Handoff Envelope.** Apply compatible reconciliation to lease-aware replay.

## Proposed Solution

- Add `scripts/speckit_workflow_reconcile.py:reconcile_replay_boundary` that consumes T021b compatible decision plus feature lease state.
- Derive persisted idempotency key from run, feature/task, phase, attempt, and effect identity; query prior bridge/result evidence before generation, validation, event recording, task transition, or checkpoint.
- Permit effect only with valid acquired/renewed lease and absent equivalent completed key; otherwise return duplicate-safe result, lease block, or drift outcome without repeating mutation.

## Current Repo Behavior

Feature locking and pipeline transitions exist separately, but no single reconciliation boundary links lease, attempt, cursor, and effect idempotency before replay.

## Target Behavior

- Preserve FR-024 and FR-025: replay is safe across nested-parent rerun, and one feature has single-flight ownership.
- Specialize PL-06 by preventing duplicate artifacts/events/checkpoints while allowing idempotent return of persisted result.
- Human pause lease behavior remains explicit, not inferred.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_workflow_reconcile.py:reconcile_replay_boundary` (new).

## Reuse Candidates

- T021a evidence and T021b compatibility decision.
- `scripts/feature_lock.py` lifecycle primitives for later lease operations.

## Required Edits

- Add effect identity/key model, previous-result lookup, lease validation hook, and duplicate-safe decision result.
- Define phase/event/artifact/task-transition replay boundaries without directly writing ledgers.

## Touched Symbols

### Modify

- `scripts/speckit_workflow_reconcile.py:evaluate_reconciliation`

### Create

- `scripts/speckit_workflow_reconcile.py:reconcile_replay_boundary`
- `scripts/speckit_workflow_reconcile.py:EffectIdentity`

### Preserve

- Lease lifecycle implementation for T023.
- Ledger append authority.

## Tests To Add Or Update

- T022 covers duplicate event idempotency and incompatible version block.
- T026c later proves fresh-process crash/replay live.

## Acceptance Criteria

- Equivalent replay returns persisted outcome without duplicate effect.
- Invalid/missing/expired/conflicting lease or incompatible evidence blocks before effect.

## Done Criteria

- T022 tests pass.
- No replay code writes raw ledger JSONL or bypasses feature lease.

## Constraints And Invariants

- Preserve FR-023 through FR-025.
- Parent/nested replay remains safe.
- No implicit implementation or external effects.

## Implementation Checklist

- [ ] Construct persisted effect identity.
- [ ] Check compatible reconciliation and lease.
- [ ] Detect prior completed effect.
- [ ] Return permit/block/duplicate-safe decision.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
