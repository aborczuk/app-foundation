---
feature_id: "039"
task_id: "T023"
task_summary: "Add or extend feature lease handling in scripts/feature_lock.py and the workflow adapter for acquire, renew, release, pause, expiry, and recovery behavior."
story_id: "US3"
primary_edit_seam: "scripts/feature_lock.py"
related_paths: ["scripts/speckit_workflow_reconcile.py", "scripts/speckit_workflow_adapter.py"]
---

# HUD: T023 - Feature lease lifecycle

## Objective

Deliver `T023` so the task scope satisfies: Add or extend feature lease handling in scripts/feature_lock.py and the workflow adapter for acquire, renew, release, pause, expiry, and recovery behavior..

## Relevant Domains

- resilience: single-flight ownership survives pause, expiry, recovery, and fresh agent resume.
- ops governance: lease holder/expiry is auditable and explicit.
- security: lease validation blocks competing effectful work.

## Candidate Design Slices

- **PL-06 — Add Durable Resume, Reconciliation, Lease, and Handoff Envelope.** Extend feature lock into workflow lease lifecycle.

## Proposed Solution

- Extend `scripts/feature_lock.py` with typed lease acquire, renew, release, inspect, expiry, and recovery operations keyed by feature/run/correlation/actor.
- Integrate lease checks with the workflow adapter/reconciliation boundary: acquire before effectful continuation, renew during active automated work, persist pause behavior, release on terminal completion, and recover expired ownership only after reconciliation.
- Return structured holder/expiry/conflict results; do not silently steal active lease.

## Current Repo Behavior

`scripts/feature_lock.py` supplies existing lock primitives, but it is not yet a full workflow lease contract across pause/resume/recovery.

## Target Behavior

- Preserve FR-025 and FR-023: exactly one active feature owner and explicit behavior during human pause/expiry.
- Specialize PL-06 by making leases visible in reconciliation/handoff state.
- Conflicting or expired lease blocks effect until safe recovery conditions pass.

## Primary Edit Seam

**File:Symbol**: `scripts/feature_lock.py` lease lifecycle functions.

## Reuse Candidates

- Existing `scripts/feature_lock.py` acquire/release mechanics.
- T021 reconciliation decision before recovery/steal.

## Required Edits

- Add lease record model and explicit acquire/renew/release/recover APIs.
- Add adapter bridge calls at start/resume/effect/pause/terminal boundaries and stable conflict reasons.

## Touched Symbols

### Modify

- `scripts/feature_lock.py`

### Create

- `scripts/feature_lock.py:FeatureLease`
- `scripts/feature_lock.py:renew_feature_lease`
- `scripts/feature_lock.py:recover_feature_lease`

### Preserve

- Existing lock compatibility and no direct ledger mutation.

## Tests To Add Or Update

- Extend T022 or add focused feature-lock tests for acquire/renew/pause/expiry/recovery.
- T026c provides live recovery proof.

## Acceptance Criteria

- Concurrent acquisition is rejected with holder/expiry evidence.
- Renew/release/pause/expiry/recover paths are explicit and recovery requires reconciliation compatibility.

## Done Criteria

- Lease unit tests pass.
- No operation silently takes an unexpired lease or triggers implementation work.

## Constraints And Invariants

- Preserve FR-023, FR-025, and correlation identity rules.
- Pause behavior has no dependent mutation.
- Bound diagnostics and expose no secrets.

## Implementation Checklist

- [ ] Define lease record/state transitions.
- [ ] Add acquire/renew/release/expiry APIs.
- [ ] Integrate with reconcile/adapter boundaries.
- [ ] Test conflicts and recovery.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
