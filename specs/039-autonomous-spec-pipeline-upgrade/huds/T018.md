---
feature_id: "039"
task_id: "T018"
task_summary: "Add loop tests in tests/unit/test_speckit_workflow_loops.py covering stale output, cap exhaustion, malformed condition output, recoverable retry, and no false success."
story_id: "US1"
primary_edit_seam: "tests/unit/test_speckit_workflow_loops.py"
related_paths: ["scripts/speckit_workflow_loops.py", "scripts/speckit_workflow_outcomes.py"]
---

# HUD: T018 - Workflow loop tests

## Objective

Deliver `T018` so the task scope satisfies: Add loop tests in tests/unit/test_speckit_workflow_loops.py covering stale output, cap exhaustion, malformed condition output, recoverable retry, and no false success..

## Relevant Domains

- testing: loop success/failure semantics need deterministic state fixtures.
- resilience: regressions must prevent cap fall-through and stale-output decisions.

## Candidate Design Slices

- **PL-05 — Add Loop, Outcome, and Human Pause Policy.** Unit-test the T017 convergence contract.

## Proposed Solution

- Create `tests/unit/test_speckit_workflow_loops.py` with fake persisted outcomes, condition evaluators, idempotent body counters, and post-loop assertions.
- Test stale outcome rejection, finite cap exhaustion, malformed condition data, one recoverable retry then validated pass, and a valid final artifact whose stale condition still blocks success.
- Assert no test branch creates an event request without post-loop assertion pass.

## Current Repo Behavior

T017 is new; no existing unit suite covers upstream-style while/do-while semantics with repository typed outcomes.

## Target Behavior

- Prove FR-012/FR-013 and listed edge cases.
- Distinguish exhausted, malformed, and validated completion outcomes deterministically.
- Keep tests fake-backed unit coverage.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_speckit_workflow_loops.py` (new).

## Reuse Candidates

- T015 outcome constructors and T017 loop policy interfaces.

## Required Edits

- Add fixture constructors and named regression tests for every listed condition.
- Track body/validator calls to prove finite count and no false success.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_speckit_workflow_loops.py:test_rejects_stale_or_malformed_condition_output`
- `tests/unit/test_speckit_workflow_loops.py:test_cap_exhaustion_blocks_success`
- `tests/unit/test_speckit_workflow_loops.py:test_recoverable_retry_requires_post_loop_assertion`

### Preserve

- Live workflow control-flow proof for T026.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_speckit_workflow_loops.py`.

## Acceptance Criteria

- Each required edge case has a deterministic outcome/reason assertion.
- The body runs no more than the declared budget and event success remains absent until assertion pass.

## Done Criteria

- Focused pytest passes.
- Tests distinguish unit fakes from live engine behavior.

## Constraints And Invariants

- No real workflow engine, ledger, or external effect.
- Do not rely on timing.
- Preserve typed outcome contract.

## Implementation Checklist

- [ ] Create persisted outcome fixtures.
- [ ] Cover stale/malformed/cap cases.
- [ ] Cover retry and post-loop assertion.
- [ ] Run pytest guard.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
