---
feature_id: "039"
task_id: "T007"
task_summary: "Add manifest consistency tests in tests/unit/test_validate_command_manifest.py for workflow fields, mirror version drift, route ownership conflicts, and prohibited implementation-start routes."
story_id: "US1"
primary_edit_seam: "tests/unit/test_validate_command_manifest.py"
related_paths: ["scripts/pipeline_ledger.py", "command-manifest.yaml"]
---

# HUD: T007 - Manifest consistency tests

## Objective

Deliver `T007` so the task scope satisfies: Add manifest consistency tests in tests/unit/test_validate_command_manifest.py for workflow fields, mirror version drift, route ownership conflicts, and prohibited implementation-start routes..

## Relevant Domains

- testing: validates manifest contract failures without runtime orchestration.
- ops governance: prevents ownership, version, and terminal-route drift.

## Candidate Design Slices

- **PL-02 — Define Workflow Ownership and Manifest Routing.** Contract-test the schema enforced by T006.

## Proposed Solution

- Create `tests/unit/test_validate_command_manifest.py` with minimal manifest fixtures for canonical route success and invalid ownership/mirror/terminal cases.
- Call the real `scripts/pipeline_ledger.py:cmd_validate_manifest` boundary, asserting stable reason codes rather than YAML implementation details.
- Cover a legacy alias mapped correctly and one that bypasses the canonical route.

## Current Repo Behavior

The dedicated test module does not exist. `scripts/pipeline_ledger.py:cmd_validate_manifest` is the validated production seam after T006.

## Target Behavior

- Prove canonical phases retain one owner and one result contract.
- Prove mismatched mirror version, ownership conflict, legacy bypass, and implementation-start terminal route fail deterministically.
- Keep this as unit/contract coverage; workflow execution remains integration work.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_validate_command_manifest.py` (new).

## Reuse Candidates

- Existing unit script-loader pattern in `tests/unit/test_speckit_tasks_gate.py`.
- T006 reason-code contract.

## Required Edits

- Add fixture builders and one focused test per manifest invariant.
- Assert no validation path changes manifest content, ledgers, or runtime results.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_validate_command_manifest.py:test_canonical_route_validates`
- `tests/unit/test_validate_command_manifest.py:test_rejects_route_ownership_conflict`
- `tests/unit/test_validate_command_manifest.py:test_rejects_mirror_version_drift`
- `tests/unit/test_validate_command_manifest.py:test_rejects_legacy_bypass_and_implementation_start`

### Preserve

- Live workflow verification belongs to T026a–T026c.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_validate_command_manifest.py`.

## Acceptance Criteria

- Each invalid fixture yields a stable deterministic reason naming the contract violation.
- A valid canonical fixture passes without relying on a live engine.

## Done Criteria

- Focused pytest passes and all four requested invariants are covered.
- Tests do not mutate repository manifest or ledgers.

## Constraints And Invariants

- Test the real validator seam, not a duplicate validator.
- Keep fixtures explicit and no broader than lifecycle ownership.
- Do not represent fake-only tests as live verification.

## Implementation Checklist

- [ ] Build valid canonical fixture.
- [ ] Add owner/version/legacy/terminal failure fixtures.
- [ ] Assert stable reason codes.
- [ ] Run focused pytest guard.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
