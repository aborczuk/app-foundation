---
feature_id: "039"
task_id: "T026c"
task_summary: "Add live fault-and-resume tests in tests/integration/test_speckit_workflow_live.py for crash recovery, implementation-ready handoff convergence, and terminal reconciliation without starting implementation work."
story_id: "US5"
primary_edit_seam: "tests/integration/test_speckit_workflow_live.py:test_live_crash_recovery_and_handoff_convergence"
related_paths: ["scripts/speckit_workflow_reconcile.py", "scripts/speckit_handoff_envelope.py"]
---

# HUD: T026c - Live crash recovery and handoff

## Objective

Deliver `T026c` so the task scope satisfies: Add live fault-and-resume tests in tests/integration/test_speckit_workflow_live.py for crash recovery, implementation-ready handoff convergence, and terminal reconciliation without starting implementation work..

## Relevant Domains

- resilience: crash boundaries and nested replay must converge without duplicates.
- testing: real engine/fresh process proves handoff and terminal reconciliation.
- observability: collect cursor, ledger, artifact, lease evidence after fault.

## Candidate Design Slices

- **PL-07 — Prove Live Prompt-to-Handoff Safety.** Complete live fault/restart/handoff proof.

## Proposed Solution

- Extend `tests/integration/test_speckit_workflow_live.py` with fault injection after validation-before-append and append-before-cursor-persist, then restart from a fresh process.
- Assert reconciliation/lease/idempotency avoid duplicate event/task/artifact effects, converge to one compact T024 handoff envelope, and terminal reconciliation blocks unresolved drift.
- Verify handoff identifies `/speckit.implement` only after all pre-implementation proofs pass.

## Current Repo Behavior

T026a/b establish live harness and command/loop/pause coverage; no live crash/restart/reconciliation scenario proves replay safety.

## Target Behavior

- Preserve FR-023 through FR-026 and FR-032 live.
- Fresh process recovery handles faults at named boundaries without duplicate transition.
- Drift remains terminal/blocked with diagnostic pointer, not false completion.

## Primary Edit Seam

**File:Symbol**: `tests/integration/test_speckit_workflow_live.py` crash/recovery/handoff scenarios.

## Reuse Candidates

- T026a harness, T021 reconciliation, T023 lease, and T024 envelope.

## Required Edits

- Add deterministic fault hooks, fresh-process restart helper, side-effect counters, and envelope assertions.
- Test one compatible recovery and one unresolved-drift terminal case.

## Touched Symbols

### Modify

- `tests/integration/test_speckit_workflow_live.py`

### Create

- `tests/integration/test_speckit_workflow_live.py:test_live_crash_recovery_and_handoff_convergence`

### Preserve

- No implementation execution.

## Tests To Add Or Update

- Run live selector with pinned engine/runner.
- Require actual fresh process; no mocked recovery backend.

## Acceptance Criteria

- Fault/restart produces no duplicate artifact/event/task transition and reaches one valid handoff when evidence agrees.
- Conflicting evidence blocks with stable reason/action/pointer.

## Done Criteria

- Live fault selector passes.
- Evidence records real engine, cursor, and repository authority checks.

## Constraints And Invariants

- Isolated workspace and cleanup only.
- Preserve ledger authority and redaction.
- Stop at handoff.

## Implementation Checklist

- [ ] Insert named fault points.
- [ ] Restart fresh process.
- [ ] Assert idempotency/reconciliation.
- [ ] Assert handoff or block.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
