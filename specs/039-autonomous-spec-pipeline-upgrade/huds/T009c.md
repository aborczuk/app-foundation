---
feature_id: "039"
task_id: "T009c"
task_summary: "Add result-channel redaction and envelope finalization in scripts/speckit_command_adapter.py so approved command results preserve output caps, redact protected values, and report actionable policy or runner failures without shell-text execution."
story_id: "US2"
primary_edit_seam: "scripts/speckit_command_adapter.py:finalize_command_result"
related_paths: ["scripts/hook_pretool_dispatch.py", "tests/unit/test_speckit_command_adapter.py"]
---

# HUD: T009c - Safe adapter result envelope

## Objective

Deliver `T009c` so the task scope satisfies: Add result-channel redaction and envelope finalization in scripts/speckit_command_adapter.py so approved command results preserve output caps, redact protected values, and report actionable policy or runner failures without shell-text execution..

## Relevant Domains

- security: redact protected values before structured results leave the adapter.
- observability: retain bounded diagnostics and artifact references without dumping sensitive output.
- resilience: normalize all policy/runner outcomes into one machine contract.

## Candidate Design Slices

- **PL-03 — Build Safe Command and Shell Adapter Contract.** Finalize safe result handling after T009a policy and T009b execution.

## Proposed Solution

- Add `scripts/speckit_command_adapter.py:finalize_command_result` that accepts typed policy or runner results only.
- Redact declared secret patterns and sensitive argument values, enforce final stdout/stderr caps, normalize pass/fail/recoverable fields, and emit one compact JSON envelope with an artifact pointer for retained bounded evidence.
- Keep redaction deterministic and do not reinterpret failures as success.

## Current Repo Behavior

T009a/T009b define the reviewed request and bounded execution seams; no adapter-owned redaction/result envelope currently protects shell-step diagnostics.

## Target Behavior

- Preserve FR-027: one stdout object, stderr progress/diagnostics, output caps, secret redaction, wrapped external errors, and artifact pointers.
- Specialize PL-03 by converting every policy or runner outcome into a safe structured adapter result.
- A redaction failure, nonconforming JSON, or policy rejection remains failure.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_command_adapter.py:finalize_command_result` (new).

## Reuse Candidates

- T009a typed policy decision and T009b `CommandRunResult`.
- Repository bounded diagnostic conventions from hook guards; do not copy hook payload dumps.

## Required Edits

- Add redaction rules/config resolution, safe truncation, result normalization, and artifact-pointer fields.
- Ensure stdout serialization is exactly one JSON object and no raw secret/output crosses the envelope.

## Touched Symbols

### Modify

- `scripts/speckit_command_adapter.py:finalize_command_result` after T009a/T009b create the module.

### Create

- `scripts/speckit_command_adapter.py:AdapterResultEnvelope`
- `scripts/speckit_command_adapter.py:redact_result_fields`

### Preserve

- T009a validation and T009b runner classifications.
- Full retained evidence only at declared artifact paths.

## Tests To Add Or Update

- T010 covers secret redaction, oversized output, malformed JSON, and failure normalization.
- Assert stdout contains one parseable safe envelope.

## Acceptance Criteria

- Secret-bearing policy/runner output cannot appear in returned stdout or bounded stderr.
- Success and each failure type produce one typed envelope with reason and diagnostic pointer.

## Done Criteria

- T010 redaction/envelope tests pass.
- The adapter emits no raw command/output dump by default.

## Constraints And Invariants

- Never transform failure into success after redaction.
- Preserve FR-009, FR-011, and FR-027.
- Do not write ledger events or implementation artifacts.

## Implementation Checklist

- [ ] Define safe envelope fields.
- [ ] Redact and cap policy/runner data.
- [ ] Serialize one stdout object.
- [ ] Validate with T010 adversarial cases.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
