---
feature_id: "039"
task_id: "T019"
task_summary: "Add human intervention policy in scripts/speckit_human_policy.py with reason codes, authority, required information, allowed choices, prohibited effects, and typed resume validation."
story_id: "US2"
primary_edit_seam: "scripts/speckit_human_policy.py:validate_resume_input"
related_paths: ["scripts/pipeline_driver.py", "scripts/speckit_workflow_outcomes.py"]
---

# HUD: T019 - Human intervention policy

## Objective

Deliver `T019` so the task scope satisfies: Add human intervention policy in scripts/speckit_human_policy.py with reason codes, authority, required information, allowed choices, prohibited effects, and typed resume validation..

## Relevant Domains

- ops governance: human intervention needs reason, authority, choice, and prohibited-effect policy.
- security: sensitive/destructive/external decisions cannot continue unattended.
- resilience: pause state and typed resume input must be durable and scope-checked.

## Candidate Design Slices

- **PL-05 — Add Loop, Outcome, and Human Pause Policy.** Add the versioned classified human-decision boundary.

## Proposed Solution

- Create `scripts/speckit_human_policy.py` with versioned `HumanInterventionPolicy`, reason codes, authority, required information, allowed choices, prohibited effects, and typed resume schema.
- Cover material ambiguity, mandated approval, sensitive access, destructive work, external effect, drift, exhausted recovery, and no machine owner.
- Provide `classify_human_pause` and `validate_resume_input`; persist a pause request before effect and reject wrong authority, type, scope, freshness, or correlation.

## Current Repo Behavior

`scripts/pipeline_driver.py:emit_human_status` reports a human status but no versioned reason/authority/resume policy owns whether/when a protected effect may proceed.

## Target Behavior

- Preserve FR-017 through FR-019: pause only for stable classified reasons, before dependent mutation/external effect, with typed authorized resume.
- Specialize PL-05 by forbidding generic phase-boundary approval prompts.
- Unclassified/unauthorized input remains paused or terminal, never auto-continued.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_human_policy.py:validate_resume_input` (new).

## Reuse Candidates

- `scripts/pipeline_driver.py:emit_human_status` for status presentation only.
- T015 typed outcomes for the human-pause result kind.

## Required Edits

- Add policy/reason/authority/choice/resume models and classifiers.
- Add durable pause request construction with correlation and prohibited-effect fields; validate every resume attribute before returning permitted action.

## Touched Symbols

### Modify

- None.

### Create

- `scripts/speckit_human_policy.py:HumanInterventionPolicy`
- `scripts/speckit_human_policy.py:classify_human_pause`
- `scripts/speckit_human_policy.py:validate_resume_input`

### Preserve

- Existing human-owned approvals and ledger authority.
- Effect execution remains downstream of validated resume.

## Tests To Add Or Update

- T020 covers each reason class and unauthorized/stale resume input.
- T026 exercises pause/resume live only after policy unit coverage exists.

## Acceptance Criteria

- Every protected reason maps to explicit authority, information, choices, prohibited effects, and resume type.
- Invalid/unauthorized resume cannot release pause or trigger an effect.

## Done Criteria

- T020 tests pass.
- Policy serialization has version/correlation and never contains secrets.

## Constraints And Invariants

- Preserve FR-017, FR-018, FR-019, FR-027.
- Pause before effect; no generic approval at ordinary phase transitions.
- Do not auto-authorize destructive/external/sensitive actions.

## Implementation Checklist

- [ ] Define stable reason policy table.
- [ ] Build durable pause request.
- [ ] Validate resume authority/scope/freshness/correlation.
- [ ] Cover all reason classes in T020.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
