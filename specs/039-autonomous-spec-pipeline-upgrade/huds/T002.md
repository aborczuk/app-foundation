---
feature_id: "039"
task_id: "T002"
task_summary: "Add unit coverage for preflight pass/fail cases in tests/unit/test_speckit_workflow_preflight.py, including missing workflow support, wrong release, malformed JSON, and ambiguous project root."
story_id: "US4"
primary_edit_seam: "tests/unit/test_speckit_workflow_preflight.py"
related_paths: ["scripts/speckit_workflow_preflight.py", "tests/unit/test_speckit_tasks_gate.py"]
---

# HUD: T002 - Capability preflight unit coverage

## Objective

Deliver `T002` so the task scope satisfies: Add unit coverage for preflight pass/fail cases in tests/unit/test_speckit_workflow_preflight.py, including missing workflow support, wrong release, malformed JSON, and ambiguous project root..

## Relevant Domains

- testing: deterministic unit tests must prove every pass/fail preflight branch without a live CLI.
- environment: fixtures model explicit roots and feature directories instead of ambient cwd.
- code patterns: tests preserve the repo's dynamic script-module loading and typed result contract.

## Candidate Design Slices

- **PL-01 — Pin and Rehearse Spec Kit Upgrade.** This is the deterministic test half of its capability preflight seam.

## Proposed Solution

- Add `tests/unit/test_speckit_workflow_preflight.py` using fake subprocess results and temporary directories to exercise `run_preflight` directly.
- Cover one valid pinned capability response plus missing workflow support, wrong release, malformed JSON, floating version, ambiguous root, and feature escape branches.
- Assert each failure has its stable reason/remediation envelope and that no test case requests a ledger or artifact mutation.

## Current Repo Behavior

`tests/unit/test_speckit_tasks_gate.py` loads Python scripts through `importlib.util` and uses temporary files for deterministic gate cases. The T001 preflight seam is new, so no existing test validates its CLI JSON contract.

## Target Behavior

- Prove US4/AS-4.2 independently: invalid version/capability/root inputs block before mutation with detected evidence.
- Specialize PL-01 by making the pinned capability contract reproducible without a real installed Spec Kit binary.
- `test_speckit_workflow_preflight.py` must distinguish malformed machine output from a valid response that fails a semantic pin or capability assertion.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_speckit_workflow_preflight.py` (new test module) targeting `scripts/speckit_workflow_preflight.py:run_preflight`.

## Reuse Candidates

- `tests/unit/test_speckit_tasks_gate.py:_load_script_module` for loading repository scripts without packaging changes.
- `tmp_path` and monkeypatched subprocess seams for hermetic filesystem/process inputs.

## Required Edits

- Create focused fixtures for a valid version/features JSON response and explicit root/feature directory topology.
- Add named tests for each rejection reason and assert stdout-envelope fields, mutation-free behavior, and bounded diagnostics rather than implementation details.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_speckit_workflow_preflight.py:test_run_preflight_accepts_pinned_capabilities`
- `tests/unit/test_speckit_workflow_preflight.py:test_run_preflight_rejects_missing_capability`
- `tests/unit/test_speckit_workflow_preflight.py:test_run_preflight_rejects_wrong_release`
- `tests/unit/test_speckit_workflow_preflight.py:test_run_preflight_rejects_malformed_json`
- `tests/unit/test_speckit_workflow_preflight.py:test_run_preflight_rejects_ambiguous_root`

### Preserve

- The live engine and upgrade rehearsal remain T027; these tests stay fake-backed unit tests.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_speckit_workflow_preflight.py`.
- Keep the test module labeled unit/contract coverage; do not present fake subprocess tests as live verification.

## Acceptance Criteria

- Each valid/invalid input class produces the expected typed outcome and reason code.
- Tests prove no invalid response can be treated as success or perform a mutation.

## Done Criteria

- The focused pytest selector passes.
- The test set contains the five named required cases and a valid control case with no network or installed CLI dependency.

## Constraints And Invariants

- Do not contact upstream or require a user installation in unit tests.
- Preserve the exact `v0.12.9` pin and explicit identity rules.
- Fake-only coverage remains distinct from the live verification required in T027.

## Implementation Checklist

- [ ] Reuse the repository script-loader pattern.
- [ ] Build valid JSON and malformed/nonconforming fixtures.
- [ ] Assert reason, remediation, and mutation-free result for every reject path.
- [ ] Run the focused pytest guard.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
