---
feature_id: "039"
task_id: "T021b"
task_summary: "Implement deterministic cross-authority drift and compatibility evaluation in scripts/speckit_workflow_reconcile.py that returns explicit conflicts, bounded diagnostic pointers, and one non-mutating next action instead of selecting state silently."
story_id: "US3"
primary_edit_seam: "scripts/speckit_workflow_reconcile.py:evaluate_reconciliation"
related_paths: ["scripts/speckit_workflow_outcomes.py", "scripts/feature_lock.py"]
---

# HUD: T021b - Reconciliation drift evaluation

## Objective

Deliver `T021b` so the task scope satisfies: Implement deterministic cross-authority drift and compatibility evaluation in scripts/speckit_workflow_reconcile.py that returns explicit conflicts, bounded diagnostic pointers, and one non-mutating next action instead of selecting state silently..

## Relevant Domains

- resilience: conflicts must block with a non-mutating recovery action.
- ops governance: reconcile all authoritative sources before decision.
- observability: every drift reason must be explicit and bounded.

## Candidate Design Slices

- **PL-06 — Add Durable Resume, Reconciliation, Lease, and Handoff Envelope.** Evaluate T021a evidence without guessing.

## Proposed Solution

- Add `scripts/speckit_workflow_reconcile.py:evaluate_reconciliation` that compares cursor, ledger state, planning/runtime/artifact evidence, manifest/version/provenance, and lease.
- Detect version incompatibility, missing/changed artifact pointers, inconsistent phase/event/attempt, expired/conflicting lease, and unrecognized runtime state.
- Return either a compatible decision or typed drift block containing every reason, diagnostic pointers, and exactly one non-mutating next action; do not replay or mutate.

## Current Repo Behavior

T021a creates normalized evidence collection but no single evaluator determines compatibility or blocks conflicting authorities.

## Target Behavior

- Preserve FR-023: start/resume/effect boundaries reconcile every authority and block rather than guess.
- Specialize PL-06 by separating decision from collection and replay.
- Drift never claims completion or recommends a mutating fix.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_workflow_reconcile.py:evaluate_reconciliation` (new).

## Reuse Candidates

- T021a `ReconciliationEvidence`.
- T015 outcome/reason model for typed blocked result.

## Required Edits

- Add compatibility matrix and typed drift report/next-action model.
- Compare immutable versions/identities and artifact fingerprint/pointer state; aggregate all failures.

## Touched Symbols

### Modify

- `scripts/speckit_workflow_reconcile.py:ReconciliationEvidence`

### Create

- `scripts/speckit_workflow_reconcile.py:evaluate_reconciliation`
- `scripts/speckit_workflow_reconcile.py:DriftReport`

### Preserve

- Replay effects for T021c.
- Human policy authority for a classified pause.

## Tests To Add Or Update

- T022 covers drift, version incompatibility, missing pointer, and non-mutating next action.

## Acceptance Criteria

- Compatible evidence produces explicit permitted next action.
- Any disagreement produces all drift reasons and one safe non-mutating recovery action.

## Done Criteria

- T022 evaluator tests pass.
- Evaluation has no side effect, ledger append, lease renewal, or replay.

## Constraints And Invariants

- Preserve FR-020 through FR-024.
- Do not privilege workflow log/cursor over ledger completion authority.
- Keep diagnostics redacted/bounded.

## Implementation Checklist

- [ ] Define compatibility rules.
- [ ] Compare all evidence fields.
- [ ] Aggregate drift reasons.
- [ ] Return safe action only.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
