---
feature_id: "039"
task_id: "T017"
task_summary: "Add loop policy support in scripts/speckit_workflow_loops.py for `while` and `do-while` repair/convergence using latest persisted validator or task output, finite budgets, deterministic conditions, and post-loop assertions."
story_id: "US1"
primary_edit_seam: "scripts/speckit_workflow_loops.py:run_bounded_loop"
related_paths: ["scripts/speckit_workflow_outcomes.py", "scripts/speckit_workflow_adapter.py"]
---

# HUD: T017 - Bounded workflow loop policy

## Objective

Deliver `T017` so the task scope satisfies: Add loop policy support in scripts/speckit_workflow_loops.py for `while` and `do-while` repair/convergence using latest persisted validator or task output, finite budgets, deterministic conditions, and post-loop assertions..

## Relevant Domains

- resilience: bounded loops must not fall through as success.
- code patterns: conditions consume latest persisted typed outcome.
- ops governance: exhaustion and post-loop failure are classified, not hidden.

## Candidate Design Slices

- **PL-05 — Add Loop, Outcome, and Human Pause Policy.** Implement deterministic while/do-while repair and convergence policy.

## Proposed Solution

- Create `scripts/speckit_workflow_loops.py` with typed `LoopPolicy`, `LoopState`, and `run_bounded_loop` supporting while and do-while semantics.
- Read only the latest persisted validator/task outcome, evaluate declared deterministic condition, enforce finite attempt budget, execute idempotent body, and persist each attempt/result identity.
- Require a post-loop assertion; cap exhaustion, stale/malformed condition output, or assertion failure returns a non-success T015 outcome.

## Current Repo Behavior

The planned upstream workflow supports control flow, but repository adapters do not yet own a typed bounded convergence policy or post-loop assertion.

## Target Behavior

- Preserve FR-012, FR-013, and edge cases for stale output/cap fall-through.
- Machine-decidable recoverable conditions continue automatically within budget; success requires explicit post-loop validation.
- Loops do not append events or start implementation.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_workflow_loops.py:run_bounded_loop` (new).

## Reuse Candidates

- T015 `ValidationOutcome` for current state and terminal classification.
- T013c phase execution boundary for idempotent body/validator calls.

## Required Edits

- Add policy/condition/result models, while/do-while dispatcher, attempt persistence identity, budget checks, stale-output detection, and post-loop assertion invocation.
- Return typed outcome and diagnostic pointer only.

## Touched Symbols

### Modify

- None.

### Create

- `scripts/speckit_workflow_loops.py:run_bounded_loop`
- `scripts/speckit_workflow_loops.py:LoopPolicy`
- `scripts/speckit_workflow_loops.py:assert_post_loop`

### Preserve

- Human decision policy for T019.
- Upstream cursor ownership and ledger authority.

## Tests To Add Or Update

- T018 covers stale state, cap exhaustion, malformed condition, retry, and false-success prevention.

## Acceptance Criteria

- A recoverable typed outcome retries only within declared finite budget using latest persisted state.
- Completion requires post-loop assertion; exhaustion or invalid condition cannot advance success.

## Done Criteria

- T018 tests pass.
- The helper has no unbounded loop, implicit success, or direct event append.

## Constraints And Invariants

- Preserve FR-013, FR-014, FR-024, and no implementation start.
- Bodies must be idempotent/replay-safe.
- No generic approval pause at normal phase boundaries.

## Implementation Checklist

- [ ] Define policy and condition contract.
- [ ] Implement while/do-while budget semantics.
- [ ] Persist attempt identity and read latest output.
- [ ] Require post-loop assertion.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
