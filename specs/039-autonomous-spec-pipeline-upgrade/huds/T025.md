---
feature_id: "039"
task_id: "T025"
task_summary: "Add envelope tests in tests/unit/test_speckit_handoff_envelope.py for compact JSON stdout, stderr diagnostics, artifact pointers, redaction, and downstream implementation workflow handoff."
story_id: "US3"
primary_edit_seam: "tests/unit/test_speckit_handoff_envelope.py"
related_paths: ["scripts/speckit_handoff_envelope.py", "scripts/speckit_workflow_reconcile.py"]
---

# HUD: T025 - Handoff envelope tests

## Objective

Deliver `T025` so the task scope satisfies: Add envelope tests in tests/unit/test_speckit_handoff_envelope.py for compact JSON stdout, stderr diagnostics, artifact pointers, redaction, and downstream implementation workflow handoff..

## Relevant Domains

- testing: envelope output channels/pointers/redaction need deterministic contracts.
- security: stdout must not expose secret or full diagnostics.
- observability: downstream implementation receives exact compact state.

## Candidate Design Slices

- **PL-06 — Add Durable Resume, Reconciliation, Lease, and Handoff Envelope.** Test T024 serialization and terminal boundary.

## Proposed Solution

- Create `tests/unit/test_speckit_handoff_envelope.py` with compatible, pause, drift, and failed state fixtures.
- Assert one parseable compact stdout JSON object; bounded stderr diagnostics; required artifact pointers; redaction; lease/version/attempt fields; and `/speckit.implement` next workflow only for fully ready fixture.
- Assert all non-ready cases expose permitted non-mutating next action rather than downstream implementation.

## Current Repo Behavior

T024 is a new envelope seam with no unit tests.

## Target Behavior

- Prove FR-026/FR-027 and handoff boundary deterministically.
- No sensitive field or full evidence dump crosses stdout.
- Downstream route is accurately gated.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_speckit_handoff_envelope.py` (new).

## Reuse Candidates

- T024 envelope model and T021/T023 fixture data.

## Required Edits

- Add state fixture builders and stdout/stderr capture assertions.
- Add redaction/pointer/next-workflow tests.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_speckit_handoff_envelope.py:test_ready_envelope_routes_to_implement`
- `tests/unit/test_speckit_handoff_envelope.py:test_nonready_envelope_blocks_handoff`
- `tests/unit/test_speckit_handoff_envelope.py:test_envelope_redacts_and_bounds_channels`

### Preserve

- Live handoff convergence proof in T026c.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_speckit_handoff_envelope.py`.

## Acceptance Criteria

- Ready fixture yields one complete compact envelope with `/speckit.implement` next workflow.
- Nonready/redaction cases cannot claim implementation handoff or expose protected values.

## Done Criteria

- Focused pytest passes.
- Tests distinguish artifact pointer from full diagnostic content.

## Constraints And Invariants

- No direct ledgers, network, or workflow engine.
- Preserve envelope version/correlation field integrity.
- No implementation execution in tests.

## Implementation Checklist

- [ ] Build compatible/nonready fixtures.
- [ ] Assert output channel contract.
- [ ] Assert pointers/redaction.
- [ ] Assert strict handoff gate.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
