---
feature_id: "039"
task_id: "T020"
task_summary: "Add human policy tests in tests/unit/test_speckit_human_policy.py for material ambiguity, mandated approval, sensitive access, destructive work, external effect, drift, exhausted recovery, and unauthorized resume."
story_id: "US2"
primary_edit_seam: "tests/unit/test_speckit_human_policy.py"
related_paths: ["scripts/speckit_human_policy.py", "scripts/speckit_workflow_outcomes.py"]
---

# HUD: T020 - Human policy tests

## Objective

Deliver `T020` so the task scope satisfies: Add human policy tests in tests/unit/test_speckit_human_policy.py for material ambiguity, mandated approval, sensitive access, destructive work, external effect, drift, exhausted recovery, and unauthorized resume..

## Relevant Domains

- testing: every human reason and resume rejection needs a deterministic fixture.
- security: unauthorized/sensitive/destructive paths must remain paused.
- ops governance: authority and prohibited-effect policy is regression-tested.

## Candidate Design Slices

- **PL-05 — Add Loop, Outcome, and Human Pause Policy.** Test T019 reason routing and resume validation.

## Proposed Solution

- Create `tests/unit/test_speckit_human_policy.py` with one policy fixture per material ambiguity, mandated approval, sensitive access, destructive work, external effect, drift, exhausted recovery, and no-machine-owner reason.
- Assert each pause names authority, required information, allowed choices, prohibited effects, correlation, and typed resume schema.
- Test wrong authority, stale/cross-run correlation, unsupported choice, and scope/type mismatch all remain blocked.

## Current Repo Behavior

T019 creates the first versioned policy seam; no unit matrix currently proves each human decision class or resume authorization boundary.

## Target Behavior

- Prove FR-017 through FR-019.
- Every classified reason pauses before effect; no invalid resume releases it.
- Keep tests deterministic and fake-only.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_speckit_human_policy.py` (new).

## Reuse Candidates

- T019 policy and `validate_resume_input` API.
- T015 human-pause outcome type.

## Required Edits

- Add policy fixtures and reason/authority assertions.
- Add parameterized invalid resume cases with stable reason assertions.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_speckit_human_policy.py:test_reason_policy_requires_authority_and_effect_boundary`
- `tests/unit/test_speckit_human_policy.py:test_invalid_resume_remains_paused`

### Preserve

- Live pause/resume proof for T026b.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_speckit_human_policy.py`.

## Acceptance Criteria

- All eight reason classes have explicit policy coverage.
- Unauthorized/stale/out-of-scope resume cannot permit dependent effect.

## Done Criteria

- Focused pytest passes.
- Tests never perform an external/destructive action.

## Constraints And Invariants

- No generic phase-boundary approval fixture.
- Do not include secrets in fixtures.
- Preserve typed outcome/diagnostic conventions.

## Implementation Checklist

- [ ] Build reason policy fixtures.
- [ ] Assert pause fields/prohibited effects.
- [ ] Exercise resume rejection matrix.
- [ ] Run pytest guard.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
