---
feature_id: "039"
task_id: "T016"
task_summary: "Add unit tests for workflow adapter success/failure gates in tests/unit/test_speckit_workflow_adapter.py."
story_id: "US1"
primary_edit_seam: "tests/unit/test_speckit_workflow_adapter.py"
related_paths: ["scripts/speckit_workflow_adapter.py", "scripts/speckit_workflow_outcomes.py", "scripts/speckit_specify_step.py"]
---

# HUD: T016 - Workflow adapter gate tests

## Objective

Deliver `T016` so the task scope satisfies: Add unit tests for workflow adapter success/failure gates in tests/unit/test_speckit_workflow_adapter.py..

## Relevant Domains

- testing: adapter gates need deterministic pass/failure oracles.
- resilience: prove failed generation never advances success.
- ops governance: verify event requests remain post-validation only.

## Candidate Design Slices

- **PL-04 — Implement Pre-Implementation Phase Workflow Adapters.** Contract-test T013a–T013c, T014, and T015 at the phase boundary.

## Proposed Solution

- Create `tests/unit/test_speckit_workflow_adapter.py` with fake runner/validator contracts and typed T015 outcomes.
- Cover valid execution, runner failure, missing contract, missing artifact, scaffold-only output, validator failure, recoverable retry, human pause, and terminal failure.
- Assert only validated pass returns a success-event/handoff request and no case appends a ledger event.

## Current Repo Behavior

The adapter and outcome modules are new. Existing `scripts/pipeline_driver.py:append_pipeline_success_event` provides the event seam that tests must ensure is not called prematurely.

## Target Behavior

- Prove FR-007 through FR-009 independently before live verification.
- Specialize PL-04 by covering registry, execution, outcome mapping, and specify success gate failure paths.
- Fake-only tests remain contract/unit tests.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_speckit_workflow_adapter.py` (new).

## Reuse Candidates

- Repository script-loading and monkeypatch fixtures.
- T013 contracts and T015 `ValidationOutcome` constructors.

## Required Edits

- Add deterministic fake runner/validator fixtures and call-tracking ledger/event stub.
- Add named tests for every non-pass classification and success-event prohibition.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_speckit_workflow_adapter.py:test_validated_pass_requests_success`
- `tests/unit/test_speckit_workflow_adapter.py:test_scaffold_or_validation_failure_blocks_success`
- `tests/unit/test_speckit_workflow_adapter.py:test_outcomes_preserve_retry_pause_and_terminal_classification`

### Preserve

- Live engine/recovery proof in T026a–T026c.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_speckit_workflow_adapter.py`.

## Acceptance Criteria

- Every non-pass input returns typed outcome and no success request/event.
- A validated pass is the only path that constructs a success/handoff request.

## Done Criteria

- Focused pytest passes.
- Test names and assertions distinguish unit fakes from live verification.

## Constraints And Invariants

- No real LLM, workflow engine, ledger, or external call.
- No generic file-exists shortcut.
- Preserve one stdout/result contract semantics.

## Implementation Checklist

- [ ] Build fake contracts/runners/validators.
- [ ] Cover pass and all failed classifications.
- [ ] Assert event prohibition.
- [ ] Run focused pytest guard.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
