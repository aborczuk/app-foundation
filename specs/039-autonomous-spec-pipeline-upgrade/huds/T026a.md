---
feature_id: "039"
task_id: "T026a"
task_summary: "Add a pinned-engine live verification harness in tests/integration/test_speckit_workflow_live.py that boots a feature from a prompt and records bounded, machine-readable evidence for the governed workflow run."
story_id: "US5"
primary_edit_seam: "tests/integration/test_speckit_workflow_live.py:live_workflow_harness"
related_paths: ["scripts/speckit_workflow_preflight.py", "scripts/speckit_command_adapter.py"]
---

# HUD: T026a - Pinned-engine live harness

## Objective

Deliver `T026a` so the task scope satisfies: Add a pinned-engine live verification harness in tests/integration/test_speckit_workflow_live.py that boots a feature from a prompt and records bounded, machine-readable evidence for the governed workflow run..

## Relevant Domains

- testing: critical prompt-to-handoff path needs a real pinned engine/runner.
- environment: live harness pins versions, explicit root, and isolated artifacts.
- observability: captures bounded machine-readable evidence.

## Candidate Design Slices

- **PL-07 — Prove Live Prompt-to-Handoff Safety.** Establish the live harness before scenario/fault tests.

## Proposed Solution

- Create `tests/integration/test_speckit_workflow_live.py` live fixture/harness that invokes the pinned Spec Kit engine and repository-supported runner in an isolated checkout/workspace.
- Verify runtime preflight/version/capabilities, explicit feature identity, command adapter availability, artifact/result capture, and cleanup/diagnostic pointers.
- Mark skipped only when declared live prerequisites are unavailable; never substitute mocks for a passing critical runtime path.

## Current Repo Behavior

Existing unit contracts do not prove the pinned workflow engine or runner; the specification requires separate live verification.

## Target Behavior

- Preserve FR-032 and AGENTS live-backend rule.
- Harness proves exact pinned engine/runner preconditions and produces reusable bounded evidence for T026b/c.
- No harness case starts implementation.

## Primary Edit Seam

**File:Symbol**: `tests/integration/test_speckit_workflow_live.py` live harness fixtures (new).

## Reuse Candidates

- T001 preflight and T003 clean-checkout inputs.
- T024 diagnostic/artifact pointer conventions.

## Required Edits

- Add explicit opt-in/live prerequisite fixtures, isolated workspace setup, pinned version assertion, and structured evidence capture.
- Label any fake helper as contract/simulation and keep it outside the live passing path.

## Touched Symbols

### Modify

- None.

### Create

- `tests/integration/test_speckit_workflow_live.py:live_workflow_harness`
- `tests/integration/test_speckit_workflow_live.py:test_live_pinned_engine_bootstrap`

### Preserve

- Implementation execution remains prohibited.

## Tests To Add Or Update

- Run the documented live pytest selector only with real pinned engine/runner prerequisites.
- T026b/c extend this module.

## Acceptance Criteria

- Harness rejects wrong/missing pin and names prerequisite evidence.
- Valid live environment proves prompt bootstrap plumbing and emits bounded artifacts.

## Done Criteria

- Real live selector passes in supported environment.
- No mocked backend is labeled integration/live.

## Constraints And Invariants

- Explicit isolated root/feature only; no production mutation.
- Preserve pinned `v0.12.9` contract and output redaction.
- Stop before implementation.

## Implementation Checklist

- [ ] Define live prerequisites.
- [ ] Build isolated harness.
- [ ] Assert pin/capability and root identity.
- [ ] Persist bounded evidence.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
