---
feature_id: "039"
task_id: "T008"
task_summary: "Update Codex/Claude command source generation or compatibility mapping in .codex/skills/speckit-run/SKILL.md and .claude/commands/speckit.run.md so one command source starts or resumes the workflow through handoff."
story_id: "US1"
primary_edit_seam: ".codex/skills/speckit-run/SKILL.md"
related_paths: [".claude/commands/speckit.run.md", "command-manifest.yaml", "scripts/pipeline_driver.py"]
---

# HUD: T008 - Canonical run command compatibility mapping

## Objective

Deliver `T008` so the task scope satisfies: Update Codex/Claude command source generation or compatibility mapping in .codex/skills/speckit-run/SKILL.md and .claude/commands/speckit.run.md so one command source starts or resumes the workflow through handoff..

## Relevant Domains

- ops governance: one canonical entrypoint must prevent competing orchestration ownership.
- code patterns: Codex skill and retained Claude command should be thin manifest-driven interfaces.
- resilience: start/status/continue/resume need explicit identity and handoff boundaries.

## Candidate Design Slices

- **PL-02 — Define Workflow Ownership and Manifest Routing.** Align retained command surfaces with the one canonical start-or-resume route.

## Proposed Solution

- Update `.codex/skills/speckit-run/SKILL.md` and `.claude/commands/speckit.run.md` so both delegate route selection to the manifest contract rather than independently choosing phase behavior.
- Require explicit prompt, feature ID, or run ID; resolve only start, continue, status, or resume through implementation-ready handoff.
- Describe result envelope, diagnostics, permission class, and the terminal rule that the command never begins implementation.

## Current Repo Behavior

The task identifies two retained command surfaces, `.codex/skills/speckit-run/SKILL.md` and `.claude/commands/speckit.run.md`; without a manifest-backed compatibility mapping they can drift into separate orchestration owners.

## Target Behavior

- Preserve FR-004 and FR-028: one canonical command accepts explicit identity and chooses lifecycle action without branch/cwd inference.
- Specialize PL-02 by mapping both surfaces to the T005 manifest contract.
- The command stops with implementation-ready handoff and reports the next downstream workflow instead of executing it.

## Primary Edit Seam

**File:Symbol**: `.codex/skills/speckit-run/SKILL.md` and `.claude/commands/speckit.run.md` command-dispatch guidance.

## Reuse Candidates

- `command-manifest.yaml:commands` lifecycle metadata from T005.
- `scripts/pipeline_driver.py` result/next-phase conventions, without making it a second route authority.

## Required Edits

- Replace duplicated route decisions with a manifest lookup and a named compatibility mapping.
- Document required explicit inputs, output envelope, safe resume rules, error diagnostics, and handoff-only terminal behavior.

## Touched Symbols

### Modify

- `.codex/skills/speckit-run/SKILL.md`
- `.claude/commands/speckit.run.md`

### Create

- A manifest-owned compatibility mapping if T005 needs one for retained aliases.

### Preserve

- Human-controlled approval gates.
- The separate `/speckit.implement` workflow.

## Tests To Add Or Update

- T007 manifest consistency coverage verifies mirrors/version drift.
- Add a documentation/contract assertion only if existing command-doc tests provide the seam.

## Acceptance Criteria

- Both surfaces name the same canonical route owner and accepted identity inputs.
- Neither surface claims authority to start implementation, close tasks, or bypass manifest validation.

## Done Criteria

- T007 mirror validation passes.
- A reviewer can follow either surface to the same start/continue/status/resume behavior and terminal handoff.

## Constraints And Invariants

- No ambient cwd/branch identity inference.
- No direct ledger JSONL access or ad hoc shell execution.
- Preserve FR-004, FR-018, FR-028, and FR-029.

## Implementation Checklist

- [ ] Read T005 route names and compatibility aliases.
- [ ] Align both retained surfaces with manifest lookup.
- [ ] Declare explicit inputs and result envelope.
- [ ] Verify implementation remains downstream-only.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
