---
feature_id: "039"
task_id: "T024"
task_summary: "Add implementation-ready handoff envelope generation in scripts/speckit_handoff_envelope.py with identity, phase/task attempt, last event, pending reason, permitted action, next step, versions, lease, and diagnostic pointers."
story_id: "US3"
primary_edit_seam: "scripts/speckit_handoff_envelope.py:build_handoff_envelope"
related_paths: ["scripts/speckit_workflow_reconcile.py", "scripts/feature_lock.py"]
---

# HUD: T024 - Implementation-ready handoff envelope

## Objective

Deliver `T024` so the task scope satisfies: Add implementation-ready handoff envelope generation in scripts/speckit_handoff_envelope.py with identity, phase/task attempt, last event, pending reason, permitted action, next step, versions, lease, and diagnostic pointers..

## Relevant Domains

- observability: a fresh agent needs one compact, safe next-action envelope.
- resilience: handoff captures cursor, attempt, reason, versions, lease, and diagnostics.
- ops governance: envelope stops at implementation-ready handoff.

## Candidate Design Slices

- **PL-06 — Add Durable Resume, Reconciliation, Lease, and Handoff Envelope.** Build the terminal rehydration/implementation handoff artifact.

## Proposed Solution

- Create `scripts/speckit_handoff_envelope.py:build_handoff_envelope` consuming compatible reconciliation, T015 outcome, T023 lease, manifest/version, and artifact pointers.
- Include feature/run/correlation, status, phase/task attempt, last event, pending reason, permitted action, next step, versions/provenance, lease, and bounded diagnostic pointers.
- Emit exactly one compact JSON stdout object and explicitly set `next_workflow` to `/speckit.implement` only when all pre-implementation completion gates pass.

## Current Repo Behavior

No compact single rehydration envelope currently joins workflow cursor, ledgers, results, artifacts, manifest, and lease for a new agent.

## Target Behavior

- Preserve FR-026 and FR-027: one status result gives complete next-action context without direct JSONL inspection.
- Specialize PL-06 by making terminal artifact a handoff, not implementation execution.
- Incomplete/drift/pause state reports permitted safe action instead.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_handoff_envelope.py:build_handoff_envelope` (new).

## Reuse Candidates

- T021 reconciliation evidence/decision.
- T023 lease record and T015 outcome model.

## Required Edits

- Add typed envelope/serialization model and completeness gate.
- Assemble artifact/diagnostic pointers and redact/cap output; choose next workflow only for verified handoff.

## Touched Symbols

### Modify

- None.

### Create

- `scripts/speckit_handoff_envelope.py:build_handoff_envelope`
- `scripts/speckit_handoff_envelope.py:ImplementationHandoffEnvelope`

### Preserve

- Separate `/speckit.implement` execution ownership.
- Ledger authority and full evidence retention.

## Tests To Add Or Update

- T025 covers JSON channels, pointers, redaction, and downstream handoff.
- T026c verifies live convergence to the envelope.

## Acceptance Criteria

- Envelope contains all required identity/state/reason/action/version/lease/pointer fields.
- Only fully reconciled implementation-ready state names `/speckit.implement` as next workflow.

## Done Criteria

- T025 tests pass.
- Output is one compact safe JSON object with no direct ledger dump.

## Constraints And Invariants

- Preserve FR-026, FR-027, and no implementation-start rule.
- No secret/full diagnostics in stdout.
- Do not claim handoff on drift, pause, or failed validation.

## Implementation Checklist

- [ ] Define envelope schema.
- [ ] Collect compatible state and pointers.
- [ ] Gate implementation-ready next workflow.
- [ ] Serialize compact redacted JSON.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
