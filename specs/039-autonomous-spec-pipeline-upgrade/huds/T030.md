---
feature_id: "039"
task_id: "T030"
task_summary: "Run final solution gates in scripts/speckit_solution_step.py and scripts/speckit_tasking_chain.py for task graph, HUD validation, acceptance scaffolding, ledger registration, and prompt-to-handoff verification evidence."
story_id: "US5"
primary_edit_seam: "scripts/speckit_solution_step.py:finalize"
related_paths: ["scripts/speckit_tasking_chain.py", "scripts/speckit_remake_huds.py", "scripts/task_ledger.py"]
---

# HUD: T030 - Solution closeout gates

## Objective

Deliver `T030` so the task scope satisfies: Run final solution gates in scripts/speckit_solution_step.py and scripts/speckit_tasking_chain.py for task graph, HUD validation, acceptance scaffolding, ledger registration, and prompt-to-handoff verification evidence..

## Relevant Domains

- testing: final gates prove task graph, HUDs, acceptance scaffolding, and readiness evidence.
- ops governance: task registration/event request occur only after deterministic checks.
- resilience: final result must fail closed and point to remediation.

## Candidate Design Slices

- **PL-07 — Prove Live Prompt-to-Handoff Safety.** Execute deterministic solution closeout without starting implementation.

## Proposed Solution

- Use `scripts/speckit_solution_step.py:finalize` as the only solution closeout owner after tasking stabilization.
- Run tasking chain, tasks format gate, every HUD validation, acceptance-test scaffold, task registration through `scripts/task_ledger.py`, and collected prompt-to-handoff verification evidence.
- Require complete validated artifacts, no unresolved placeholders, settled estimates, and terminal handoff boundary before emitting one `solution_approved` event request envelope; do not append it or start `/speckit.implement` here.

## Current Repo Behavior

`scripts/speckit_solution_step.py` supplies solution scaffold/finalize helpers and `scripts/speckit_tasking_chain.py` supplies settled estimate/breakdown validation; the final ticket centralizes their required sequencing.

## Target Behavior

- Preserve FR-015, FR-021, FR-031, FR-032 and solution command finalize contract.
- Every deterministic gate passes before task registration/acceptance scaffold/event request.
- Result explicitly points to `/speckit.implement` while performing no implementation task.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_solution_step.py:finalize`.

## Reuse Candidates

- `scripts/speckit_tasking_chain.py:run_chain`.
- `scripts/speckit_remake_huds.py:_validate`.
- `scripts/task_ledger.py` registration interface.

## Required Edits

- If finalize lacks any named gate, add it in deterministic order with compact failure reasons/artifact paths.
- Generate acceptance scaffolding and register only after graph/HUD validation pass; emit only event-request envelope.

## Touched Symbols

### Modify

- `scripts/speckit_solution_step.py:finalize` if gate coverage is incomplete.

### Create

- Acceptance-test scaffold artifacts owned by finalize.

### Preserve

- Pipeline ledger append authority outside finalize.
- `/speckit.implement` as downstream workflow only.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/speckit_tasking_chain.py --feature-dir specs/039-autonomous-spec-pipeline-upgrade --json`.
- Run `uv run python scripts/speckit_remake_huds.py validate --feature-dir specs/039-autonomous-spec-pipeline-upgrade --json`.
- Run solution finalize command and inspect exact JSON envelope.

## Acceptance Criteria

- Finalize blocks on any missing/placeholder HUD, unstable estimate, invalid task graph, missing acceptance scaffold, or registration failure.
- Passing finalize returns exactly one solution-approved event request for this feature and no implementation effect.

## Done Criteria

- All deterministic solution gates pass and produce required artifacts.
- Final response is finalize JSON; next workflow is `/speckit.implement`.

## Constraints And Invariants

- Do not append pipeline event directly, mutate implementation files, close implementation tasks, commit, push, or contact external systems.
- Ledger access uses script interfaces only.
- Preserve strict output/redaction bounds.

## Implementation Checklist

- [ ] Run settled graph/format/HUD gates.
- [ ] Generate acceptance scaffold.
- [ ] Register tasks via script interface.
- [ ] Finalize and return exact event-request JSON.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
