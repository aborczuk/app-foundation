---
feature_id: "039"
task_id: "T026b"
task_summary: "Add live tests in tests/integration/test_speckit_workflow_live.py for reviewed command validation, bounded repair, and classified pause/resume using the pinned-engine harness."
story_id: "US5"
primary_edit_seam: "tests/integration/test_speckit_workflow_live.py:test_live_command_repair_and_pause_resume"
related_paths: ["scripts/speckit_command_adapter.py", "scripts/speckit_workflow_loops.py", "scripts/speckit_human_policy.py"]
---

# HUD: T026b - Live command repair and pause/resume

## Objective

Deliver `T026b` so the task scope satisfies: Add live tests in tests/integration/test_speckit_workflow_live.py for reviewed command validation, bounded repair, and classified pause/resume using the pinned-engine harness..

## Relevant Domains

- testing: live runner must prove command validation, bounded repair, and pause/resume.
- resilience: recoverable loops and human policy interact with real cursor behavior.
- security: unsafe shell request is rejected before effect in live path.

## Candidate Design Slices

- **PL-07 — Prove Live Prompt-to-Handoff Safety.** Exercise command, repair, and classified pause behavior on T026a harness.

## Proposed Solution

- Extend `tests/integration/test_speckit_workflow_live.py` with real pinned-engine scenarios for approved command execution, rejection of invalid shell request, a bounded recoverable repair loop, and a persisted human pause followed by authorized resume.
- Assert latest persisted validator outcome drives loop condition; post-loop assertion is required; pause occurs before dependent mutation and typed resume retains correlation.
- Capture engine/runtime/adapter evidence paths and distinguish transient prerequisite failures from functional failures.

## Current Repo Behavior

T026a supplies live bootstrap only; command, loop, and pause contracts are currently unit-tested but not proven against the real engine.

## Target Behavior

- Preserve FR-010 through FR-019 and FR-032 live.
- Unsafe command cannot create process; validated recovery converges within budget; authorized resume continues from durable pause.
- No scenario reaches implementation execution.

## Primary Edit Seam

**File:Symbol**: `tests/integration/test_speckit_workflow_live.py` live command/repair/pause test cases.

## Reuse Candidates

- T026a harness.
- T009 adapter, T017 loop, and T019 policy contracts.

## Required Edits

- Add live scenarios and assertions for review rejection, repair condition/budget/assertion, pause persistence, and resume validation.
- Store bounded evidence pointers and explicit cleanup.

## Touched Symbols

### Modify

- `tests/integration/test_speckit_workflow_live.py`

### Create

- `tests/integration/test_speckit_workflow_live.py:test_live_command_repair_and_pause_resume`

### Preserve

- Crash/recovery/handoff proof for T026c.

## Tests To Add Or Update

- Run live selector under T026a prerequisite guard.
- Ensure no mock backend satisfies this test.

## Acceptance Criteria

- Live invalid command is rejected before process creation.
- Live recoverable loop requires post-loop pass; live pause persists and authorized correlated resume succeeds.

## Done Criteria

- Real engine selector passes with evidence artifacts.
- Scenarios do not start implementation or external unauthorized effects.

## Constraints And Invariants

- Explicit finite budget and isolated workspace.
- No secret diagnostic output.
- Preserve human approval restrictions.

## Implementation Checklist

- [ ] Add command rejection case.
- [ ] Add recovery loop case.
- [ ] Add persisted pause/resume case.
- [ ] Capture live evidence.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
