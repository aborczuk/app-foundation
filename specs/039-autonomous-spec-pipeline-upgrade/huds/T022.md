---
feature_id: "039"
task_id: "T022"
task_summary: "Add reconciliation tests in tests/unit/test_speckit_workflow_reconcile.py for drift blocking, non-mutating next action, duplicate event idempotency, version incompatibility, and missing artifact pointers."
story_id: "US3"
primary_edit_seam: "tests/unit/test_speckit_workflow_reconcile.py"
related_paths: ["scripts/speckit_workflow_reconcile.py", "scripts/feature_lock.py"]
---

# HUD: T022 - Reconciliation tests

## Objective

Deliver `T022` so the task scope satisfies: Add reconciliation tests in tests/unit/test_speckit_workflow_reconcile.py for drift blocking, non-mutating next action, duplicate event idempotency, version incompatibility, and missing artifact pointers..

## Relevant Domains

- testing: prove multi-authority drift blocks and replay stays idempotent.
- resilience: missing pointers/versions/leases get safe next actions.
- ops governance: no direct ledger mutation in reconciliation tests.

## Candidate Design Slices

- **PL-06 — Add Durable Resume, Reconciliation, Lease, and Handoff Envelope.** Test T021 collection, drift, and replay boundaries.

## Proposed Solution

- Create `tests/unit/test_speckit_workflow_reconcile.py` with controlled snapshots for cursor, ledger query, runtime artifact, manifest version, and lease.
- Test drift blocking, one non-mutating next action, duplicate effect/event idempotency, version incompatibility, missing artifact pointer, and expired/conflicting lease.
- Assert compatible replay returns stored result without new event/task transition calls.

## Current Repo Behavior

T021a–c introduce new reconciliation seams; no focused test module currently verifies cross-authority conflict behavior.

## Target Behavior

- Prove FR-021 through FR-025 at unit level.
- Every conflict blocks rather than guesses; every duplicate is effect-safe.
- Live fresh-process proof remains T026c.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_speckit_workflow_reconcile.py` (new).

## Reuse Candidates

- T021 evidence/report/effect identity models.
- Existing ledger script test fixtures without direct JSONL access.

## Required Edits

- Build deterministic snapshots and action/event spies.
- Add named test cases for conflict, pointer, version, lease, and duplicate paths.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_speckit_workflow_reconcile.py:test_drift_blocks_with_safe_next_action`
- `tests/unit/test_speckit_workflow_reconcile.py:test_duplicate_effect_is_idempotent`
- `tests/unit/test_speckit_workflow_reconcile.py:test_version_or_pointer_conflict_blocks_replay`

### Preserve

- T026c owns live crash/restart evidence.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_speckit_workflow_reconcile.py`.

## Acceptance Criteria

- All listed conflict types return deterministic block/reason/action.
- Duplicate replay performs no second event or effect.

## Done Criteria

- Focused pytest passes.
- Tests use script/API fixtures only, never ledger JSONL files.

## Constraints And Invariants

- No external process/network.
- Preserve one action/aggregate reasons rule.
- Do not impersonate live verification.

## Implementation Checklist

- [ ] Construct authority snapshots.
- [ ] Assert drift aggregation/safe action.
- [ ] Assert duplicate-safe replay.
- [ ] Run pytest guard.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
