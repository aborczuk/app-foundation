---
feature_id: "039"
task_id: "T021a"
task_summary: "Add reconciliation evidence collection in scripts/speckit_workflow_reconcile.py for workflow cursor, pipeline ledger, task/planning evidence, runtime results, artifacts, manifest, versions, and the current feature lease."
story_id: "US3"
primary_edit_seam: "scripts/speckit_workflow_reconcile.py:collect_reconciliation_evidence"
related_paths: ["scripts/pipeline_ledger.py", "scripts/task_ledger.py", "scripts/pipeline_driver.py"]
---

# HUD: T021a - Reconciliation evidence collection

## Objective

Deliver `T021a` so the task scope satisfies: Add reconciliation evidence collection in scripts/speckit_workflow_reconcile.py for workflow cursor, pipeline ledger, task/planning evidence, runtime results, artifacts, manifest, versions, and the current feature lease..

## Relevant Domains

- resilience: collect all authorities before deciding resume/replay.
- observability: evidence needs correlation, versions, attempts, and diagnostic pointers.
- ops governance: no direct ledger JSONL reads.

## Candidate Design Slices

- **PL-06 — Add Durable Resume, Reconciliation, Lease, and Handoff Envelope.** First collect normalized read-only reconciliation evidence.

## Proposed Solution

- Create `scripts/speckit_workflow_reconcile.py:collect_reconciliation_evidence` with typed snapshots for workflow cursor, pipeline/task ledger script queries, planning evidence, runtime results, artifacts, manifest/version/provenance, and feature lease.
- Require explicit feature/run/correlation identity and record missing/corrupt/unavailable sources as evidence states, not guesses.
- Use script-owned ledger interfaces only and perform no mutation or replay in this split.

## Current Repo Behavior

Pipeline/task ledgers are authoritative but must be accessed via script APIs; runtime/artifact/manifest evidence is currently dispersed with no single reconciliation snapshot.

## Target Behavior

- Preserve FR-020, FR-021, and FR-023: all decision authorities are correlated before start/resume/effect boundaries.
- Specialize PL-06 by separating evidence collection from drift decisions and replay.
- Missing evidence yields a typed blocked input, not inferred state.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_workflow_reconcile.py:collect_reconciliation_evidence` (new).

## Reuse Candidates

- `scripts/pipeline_ledger.py` and `scripts/task_ledger.py` script-owned query APIs.
- Existing runtime result/artifact path conventions in `scripts/pipeline_driver.py`.

## Required Edits

- Add correlation identity and immutable evidence models.
- Add collectors for each authority with bounded diagnostics and no JSONL file access.

## Touched Symbols

### Modify

- None.

### Create

- `scripts/speckit_workflow_reconcile.py:collect_reconciliation_evidence`
- `scripts/speckit_workflow_reconcile.py:ReconciliationEvidence`

### Preserve

- Drift evaluation for T021b and replay/lease action for T021c.

## Tests To Add Or Update

- T022 covers missing pointers, conflicts, and non-mutating recovery action.
- Test collection with controlled script/API fixtures.

## Acceptance Criteria

- A collector result names every authority and its correlated version/attempt state.
- Missing or corrupt authority data is explicit and does not mutate/replay.

## Done Criteria

- T022 evidence tests pass.
- No collector reads or writes ledger JSONL directly.

## Constraints And Invariants

- Preserve FR-020 through FR-023 and FR-027.
- Explicit identity only; no cwd/branch inference.
- Bounded/redacted diagnostics.

## Implementation Checklist

- [ ] Define correlated evidence model.
- [ ] Query every authority via owned interfaces.
- [ ] Represent missing/drift-ready evidence explicitly.
- [ ] Hand immutable snapshot to T021b.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
