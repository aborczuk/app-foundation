---
feature_id: "039"
task_id: "T028"
task_summary: "Add security fixture tests in tests/unit/test_speckit_workflow_security.py for traversal, interpolation, cwd, executable, secret, output, timeout, and protected-effect boundaries."
story_id: "US5"
primary_edit_seam: "tests/unit/test_speckit_workflow_security.py"
related_paths: ["scripts/speckit_command_adapter.py", "scripts/speckit_human_policy.py", "command-manifest.yaml"]
---

# HUD: T028 - Workflow security fixtures

## Objective

Deliver `T028` so the task scope satisfies: Add security fixture tests in tests/unit/test_speckit_workflow_security.py for traversal, interpolation, cwd, executable, secret, output, timeout, and protected-effect boundaries..

## Relevant Domains

- security: adversarial boundary fixtures prove no unsafe command/effect escapes.
- testing: policy failures must be deterministic and non-mutating.
- observability: redaction/output caps are verifiable.

## Candidate Design Slices

- **PL-03 — Build Safe Command and Shell Adapter Contract** and **PL-07 — Prove Live Prompt-to-Handoff Safety.** Add the cross-cutting security fixture matrix.

## Proposed Solution

- Create `tests/unit/test_speckit_workflow_security.py` with fixtures for path traversal, cwd escape, dynamic executable, shell interpolation, secret expansion/redaction, oversized/malformed output, timeout, and protected-effect attempts.
- Exercise adapter policy/runner/envelope plus manifest scope and human/lease boundaries as applicable.
- Assert every hostile input blocks before effect, returns stable safe reason, and does not leak secret-bearing text.

## Current Repo Behavior

T010 covers adapter-specific security tests; no cross-workflow fixture module asserts protected-effect boundaries across routing, reconciliation, and output channels.

## Target Behavior

- Preserve FR-010, FR-011, FR-027, FR-029, and FR-032.
- Every named adversarial input is rejected deterministically without process/effect escape.
- Unit fixtures complement, not replace, T026 live proof.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_speckit_workflow_security.py` (new).

## Reuse Candidates

- T010 adapter fixtures and T019/T023 policy models.
- T005 permission/mutation declarations.

## Required Edits

- Add parameterized hostile inputs and spies for subprocess, ledger/event, filesystem, and external-effect hooks.
- Assert redaction/cap channel invariants.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_speckit_workflow_security.py:test_rejects_traversal_cwd_executable_and_interpolation`
- `tests/unit/test_speckit_workflow_security.py:test_redacts_secrets_and_bounds_output`
- `tests/unit/test_speckit_workflow_security.py:test_protected_effect_boundary_blocks_side_effects`

### Preserve

- T026 live runner is separate proof.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_speckit_workflow_security.py`.

## Acceptance Criteria

- Every adversarial class has a deterministic rejection/no-effect assertion.
- No fixture allows secret output or unreviewed external/implementation effect.

## Done Criteria

- Focused pytest passes.
- Tests are unit/security fixtures, explicitly not live-engine tests.

## Constraints And Invariants

- No real shell/network/destructive operation.
- Use explicit bounded fixtures.
- Preserve command manifest authority.

## Implementation Checklist

- [ ] Build hostile input matrix.
- [ ] Spy on potential effects.
- [ ] Assert reasons/redaction/caps.
- [ ] Run pytest guard.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
