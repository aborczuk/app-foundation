---
feature_id: "039"
task_id: "T010"
task_summary: "Add adapter security tests in tests/unit/test_speckit_command_adapter.py for rejected dynamic executable, cwd escape, untrusted interpolation, oversized output, malformed JSON, timeout, and redaction."
story_id: "US2"
primary_edit_seam: "tests/unit/test_speckit_command_adapter.py"
related_paths: ["scripts/speckit_command_adapter.py", "scripts/pipeline_driver.py"]
---

# HUD: T010 - Command adapter security tests

## Objective

Deliver `T010` so the task scope satisfies: Add adapter security tests in tests/unit/test_speckit_command_adapter.py for rejected dynamic executable, cwd escape, untrusted interpolation, oversized output, malformed JSON, timeout, and redaction..

## Relevant Domains

- security: adversarial fixtures prove rejection before effect.
- testing: covers policy, runner, and redaction contracts independently.
- resilience: timeout/output/malformed JSON failures stay classified.

## Candidate Design Slices

- **PL-03 — Build Safe Command and Shell Adapter Contract.** Verify all three adapter splits without live workflow execution.

## Proposed Solution

- Create `tests/unit/test_speckit_command_adapter.py` covering T009a request validation, T009b bounded execution, and T009c envelope redaction.
- Use deterministic local fake subprocess results to test dynamic executable, cwd escape, shell interpolation, oversized output, malformed/empty JSON, timeout, nonzero exit, and secret redaction.
- Assert no rejected case creates a process and all result cases produce stable typed reasons.

## Current Repo Behavior

The command adapter is new; `scripts/pipeline_driver.py:_execute_command` is the existing subprocess seam but has no dedicated adversarial adapter suite.

## Target Behavior

- Prove FR-010, FR-011, and FR-027 at the adapter boundary.
- Demonstrate every unsafe command fails before process creation and every runner/output failure cannot become success.
- Keep tests unit/contract labeled; T026 retains live-engine proof.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_speckit_command_adapter.py` (new).

## Reuse Candidates

- Repository unit script-loader and monkeypatch patterns.
- T009a–T009c typed contracts as the only test inputs/outputs.

## Required Edits

- Add fixture builders for reviewed commands and fake completed-process results.
- Add named tests for every required adversarial class and stdout/stderr/redaction contract.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_speckit_command_adapter.py:test_rejects_dynamic_executable_and_cwd_escape`
- `tests/unit/test_speckit_command_adapter.py:test_rejects_untrusted_interpolation`
- `tests/unit/test_speckit_command_adapter.py:test_classifies_timeout_output_and_json_failures`
- `tests/unit/test_speckit_command_adapter.py:test_redacts_result_envelope`

### Preserve

- No test executes shell text or external systems.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_speckit_command_adapter.py`.

## Acceptance Criteria

- Every required unsafe/exhaustion case has a deterministic failure assertion.
- Tests prove rejected requests do not reach subprocess and result envelopes do not expose secrets.

## Done Criteria

- Focused pytest passes.
- The test module clearly remains fake-backed unit coverage pending T026 live verification.

## Constraints And Invariants

- Never invoke real shell or network in tests.
- Do not mask policy failures with mocks.
- Preserve protected-effect and output-channel boundaries.

## Implementation Checklist

- [ ] Build reviewed command and fake subprocess fixtures.
- [ ] Cover policy rejections.
- [ ] Cover runner/output failure normalization.
- [ ] Cover redaction and run pytest guard.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
