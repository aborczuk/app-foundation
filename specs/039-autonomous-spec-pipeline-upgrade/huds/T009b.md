---
feature_id: "039"
task_id: "T009b"
task_summary: "Implement the validated-request runner in scripts/speckit_command_adapter.py with root-cwd execution, bounded timeout and output capture, and a single structured JSON stdout result with bounded stderr diagnostics."
story_id: "US2"
primary_edit_seam: "scripts/speckit_command_adapter.py:run_approved_command"
related_paths: ["scripts/pipeline_driver.py", "tests/unit/test_speckit_command_adapter.py"]
---

# HUD: T009b - Approved command runner

## Objective

Deliver `T009b` so the task scope satisfies: Implement the validated-request runner in scripts/speckit_command_adapter.py with root-cwd execution, bounded timeout and output capture, and a single structured JSON stdout result with bounded stderr diagnostics..

## Relevant Domains

- security: process starts only from T009a approved argv data.
- resilience: enforce bounded timeout/output capture and deterministic failure classification.
- observability: produce one structured result channel with bounded diagnostics.

## Candidate Design Slices

- **PL-03 — Build Safe Command and Shell Adapter Contract.** This split owns the actual reviewed process runner, after policy validation.

## Proposed Solution

- Implement `scripts/speckit_command_adapter.py:run_approved_command` to accept only T009a `ReviewedCommand` values.
- Execute executable plus argv with `shell=False`, resolved repository-root cwd, explicit timeout, captured streams, and byte caps that terminate/mark oversized output deterministically.
- Parse control-flow stdout as exactly one JSON object and return a typed result carrying exit status, bounded stderr diagnostics, and artifact pointers; do not redact here beyond transport-safe truncation.

## Current Repo Behavior

`scripts/pipeline_driver.py:_execute_command` is the current subprocess seam. It is not yet constrained by a reviewed-command model or a shell-step result envelope.

## Target Behavior

- Preserve FR-010/FR-011/FR-027: reviewed commands get root-cwd timeout/caps and one parseable stdout object; timeout, malformed JSON, empty output, or cap overflow fail deterministically.
- Specialize PL-03 by keeping process execution separate from policy and redaction.
- The runner never accepts a raw command string.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_command_adapter.py:run_approved_command` (new).

## Reuse Candidates

- `scripts/pipeline_driver.py:_execute_command` for safe `subprocess` execution mechanics, adapted only behind the approved request type.
- T009a `ReviewedCommand` as the sole input contract.

## Required Edits

- Add bounded process execution, timeout handling, output byte caps, JSON result parsing, and typed runner failures.
- Keep stdout JSON parsing strict and direct progress/diagnostics to bounded stderr data.

## Touched Symbols

### Modify

- `scripts/speckit_command_adapter.py:run_approved_command` after T009a creates the module.

### Create

- `scripts/speckit_command_adapter.py:CommandRunResult`
- `scripts/speckit_command_adapter.py:CommandRunFailure`

### Preserve

- Policy validation in T009a and redaction/envelope finalization in T009c.

## Tests To Add Or Update

- T010 covers timeout, oversized output, malformed JSON, and valid structured output.
- Use local deterministic test commands only; no shell invocation.

## Acceptance Criteria

- An approved deterministic command returns one typed structured result within declared limits.
- Timeout, cap overflow, nonzero exit, empty output, or malformed JSON return classified failure without executing a second command.

## Done Criteria

- T010 runner tests pass.
- No code path invokes `shell=True` or accepts raw command text.

## Constraints And Invariants

- Input must be an immutable approved request.
- Enforce repository-root cwd and explicit timeout/output cap.
- Preserve full output only behind bounded artifact pointers; do not leak secrets.

## Implementation Checklist

- [ ] Accept only ReviewedCommand.
- [ ] Execute with shell disabled and root cwd.
- [ ] Cap/parse result channels deterministically.
- [ ] Return typed success or failure for T009c.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
