---
feature_id: "039"
task_id: "T001"
task_summary: "Add pinned Spec Kit capability preflight and version contract in scripts/speckit_workflow_preflight.py covering `specify version --features --json`, approved release `v0.12.9`, project root, feature directory, and no floating `latest` values."
story_id: "US4"
primary_edit_seam: "scripts/speckit_workflow_preflight.py:main"
related_paths: ["scripts/bootstrap_session.py", "scripts/speckit_plan_step.py", "tests/unit/test_speckit_workflow_preflight.py"]
---

# HUD: T001 - Pinned Spec Kit capability preflight

## Objective

Deliver `T001` so the task scope satisfies: Add pinned Spec Kit capability preflight and version contract in scripts/speckit_workflow_preflight.py covering `specify version --features --json`, approved release `v0.12.9`, project root, feature directory, and no floating `latest` values..

## Relevant Domains

- build pipeline: the preflight establishes a reproducible, pinned upgrade baseline before any migration action.
- environment: CLI version, capability data, project root, and feature directory must be explicit and machine-verifiable.
- code patterns: this is a narrow Python adapter with typed inputs and bounded subprocess I/O.
- testing: all rejection branches need deterministic unit coverage before downstream migration work.

## Candidate Design Slices

- **PL-01 — Pin and Rehearse Spec Kit Upgrade.** Implement its non-mutating capability preflight seam first. This task owns only the pinned CLI/version/capability contract; inventory, rehearsal, and rollback evidence belong to T003.

## Proposed Solution

- Create `scripts/speckit_workflow_preflight.py` with typed `run_preflight` and `main` entry points.
- Use a fixed argv for `specify version --features --json`; parse one JSON response and validate the exact `v0.12.9` release, declared workflow capabilities, explicit in-root project/feature paths, and no floating version values.
- Emit one compact stdout result; every rejection returns detected evidence, a stable reason code, one remediation action, and no mutation.

## Current Repo Behavior

`scripts/bootstrap_session.py:bootstrap_session` prepares UV/codegraph state only; it does not inspect the CLI or validate capabilities. `scripts/speckit_plan_step.py:_resolve_feature_paths` provides the existing feature-directory resolution pattern, but no upgrade preflight exists.

## Target Behavior

- Preserve US4/AS-4.2: a wrong CLI, schema, integration, or capability blocks before mutation and reports detected-version evidence plus remediation.
- Preserve the US4 independent proof: a machine-readable, non-mutating preflight supports isolated rehearsal.
- Specialize PL-01: pin `v0.12.9` and validate capability JSON before inventory or migration operations.
- `scripts/speckit_workflow_preflight.py:run_preflight` must decide root and feature identity only from explicit validated inputs.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_workflow_preflight.py:run_preflight` (new), exposed by `scripts/speckit_workflow_preflight.py:main`.

## Reuse Candidates

- `scripts/speckit_plan_step.py:_resolve_feature_paths` for deterministic feature resolution.
- `scripts/bootstrap_session.py:bootstrap_session` for result-summary conventions; do not couple capability success to codegraph health.

## Required Edits

- Add typed result/error models, fixed CLI argv, strict JSON parsing, capability/pin checks, and explicit root/feature validation.
- Route bounded diagnostics to stderr, put only the result envelope on stdout, and never append events, touch ledgers, or modify artifacts.

## Touched Symbols

### Modify

- None; this task introduces the new preflight seam.

### Create

- `scripts/speckit_workflow_preflight.py:run_preflight`
- `scripts/speckit_workflow_preflight.py:main`
- `scripts/speckit_workflow_preflight.py:PreflightResult`

### Preserve

- `scripts/speckit_plan_step.py:_resolve_feature_paths` until a later task deliberately shares it.
- `.speckit` ledgers: never read or write ledger JSONL directly.
- The separate implementation workflow boundary.

## Tests To Add Or Update

- T002 adds `tests/unit/test_speckit_workflow_preflight.py` for pass, missing capability, wrong release, malformed JSON, and ambiguous root/feature cases.
- Run the preflight CLI only against deterministic test doubles after T002 supplies them.

## Acceptance Criteria

- An approved `v0.12.9` response with required capabilities and explicit in-root paths yields one parseable pass envelope and no mutation.
- Wrong release, floating `latest`, malformed JSON, absent capability, ambiguous root, or out-of-root feature yields a stable reason, evidence, and non-mutating remediation.

## Done Criteria

- `tests/unit/test_speckit_workflow_preflight.py` passes after T002.
- JSON mode emits exactly one stdout envelope; no preflight path mutates files, ledgers, tasks, HUDs, or upstream state.

## Constraints And Invariants

- Pin only `v0.12.9`; never use `latest`, `main`, or inferred upstream state.
- Use fixed argv and Python subprocess handling; never execute shell text or untrusted interpolation.
- Preserve FR-001, FR-002, FR-004, FR-021, and FR-027; do not upgrade Spec Kit or start implementation.
- Bound and redact diagnostics; stdout is one machine-readable object.

## Implementation Checklist

- [ ] Define the minimum typed capability contract from the CLI JSON shape.
- [ ] Implement explicit root/feature resolution and fixed-argv invocation.
- [ ] Add stable pass/failure envelopes with evidence and remediation.
- [ ] Verify the seam is mutation-free, then satisfy T002's rejection matrix.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
