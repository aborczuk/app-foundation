---
feature_id: "039"
task_id: "T003"
task_summary: "Add upgrade inventory and rehearsal support in scripts/speckit_upgrade_inventory.py for CLI/project/customization files, hashes, clean-checkout reproduction inputs, and rollback evidence."
story_id: "US4"
primary_edit_seam: "scripts/speckit_upgrade_inventory.py:collect_inventory"
related_paths: ["scripts/git_diff_guard.py", "tests/unit/test_speckit_upgrade_inventory.py"]
---

# HUD: T003 - Upgrade inventory and rehearsal support

## Objective

Deliver `T003` so the task scope satisfies: Add upgrade inventory and rehearsal support in scripts/speckit_upgrade_inventory.py for CLI/project/customization files, hashes, clean-checkout reproduction inputs, and rollback evidence..

## Relevant Domains

- build pipeline: captures reproducible provenance and rollback inputs before migration.
- environment: records explicit CLI/project/customization sources and clean-checkout inputs.
- security: inventories paths and hashes without leaking secrets.
- testing: creates deterministic evidence consumed by T004 and T027.

## Candidate Design Slices

- **PL-01 — Pin and Rehearse Spec Kit Upgrade.** This task owns immutable inventory, rehearsal inputs, and rollback evidence after T001 establishes capability compatibility.

## Proposed Solution

- Create `scripts/speckit_upgrade_inventory.py` with typed inventory entries and `collect_inventory`/`build_rehearsal_plan` functions.
- Enumerate only declared CLI, project, and customization paths; resolve them under an explicit repository root; hash readable files; classify upstream-managed versus repo-owned customization; and write a versioned inventory artifact.
- Produce a clean-checkout reproduction input set and rollback plan that restores upgrade-managed files only, preserving feature artifacts and ledger history.

## Current Repo Behavior

`scripts/git_diff_guard.py:_build_parser` demonstrates a bounded repository command wrapper, but the repository has no dedicated immutable Spec Kit inventory, customization classifier, or clean-checkout rollback artifact.

## Target Behavior

- Preserve US4/AS-4.1 and AS-4.3: maintainers receive a bounded semantic inventory before production mutation and can reproduce approved customization in a clean checkout.
- Specialize PL-01: inventory must be non-mutating with hashes, classification, rehearsal inputs, and rollback evidence.
- `collect_inventory` rejects path escape and records no ledger state.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_upgrade_inventory.py:collect_inventory` (new), with `build_rehearsal_plan` for the derived proof artifact.

## Reuse Candidates

- `scripts/git_diff_guard.py` for the repository convention of bounded Git-facing helper input.
- `scripts/speckit_plan_step.py:_resolve_feature_paths` for explicit feature/repository path error behavior.

## Required Edits

- Add typed immutable inventory and rollback-evidence models, constrained path enumeration, SHA-256 fingerprints, ownership classification, and clean-checkout input serialization.
- Define a rollback plan that explicitly excludes `.speckit` ledgers and feature artifacts from restoration.

## Touched Symbols

### Modify

- None.

### Create

- `scripts/speckit_upgrade_inventory.py:collect_inventory`
- `scripts/speckit_upgrade_inventory.py:build_rehearsal_plan`
- `scripts/speckit_upgrade_inventory.py:InventoryEntry`
- `scripts/speckit_upgrade_inventory.py:RollbackEvidence`

### Preserve

- Feature specs, tasks, HUDs, and both ledgers.
- Upstream core files until the later approved migration workflow.

## Tests To Add Or Update

- T004 adds `tests/unit/test_speckit_upgrade_inventory.py` for classification, stable hashing, invalid paths, and rollback exclusions.
- T027 supplies clean-checkout and rollback integration proof; do not claim it from this helper alone.

## Acceptance Criteria

- Given declared in-root paths, output contains deterministic hashes, classifications, source provenance, and clean-checkout reproduction inputs.
- Given an unsafe path or protected artifact/ledger, collection blocks with a reason and rollback never selects it for modification.

## Done Criteria

- T004 unit suite passes.
- The inventory/rehearsal artifact is immutable, machine-readable, bounded to declared inputs, and contains a no-ledger/no-feature-artifact rollback exclusion.

## Constraints And Invariants

- No blind refresh, destructive checkout, ledger mutation, or feature artifact rewrite.
- Hash only declared readable paths and redact secret-bearing values.
- Preserve FR-002, FR-003, FR-021, FR-027 and the separate implementation boundary.

## Implementation Checklist

- [ ] Define allowlisted inventory roots and ownership classes.
- [ ] Implement stable fingerprint and classification collection.
- [ ] Serialize rehearsal and rollback evidence with protected-path exclusions.
- [ ] Cover classification and exclusion behavior in T004.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
