---
feature_id: "039"
task_id: "T004"
task_summary: "Add unit coverage for inventory classification and rollback metadata in tests/unit/test_speckit_upgrade_inventory.py."
story_id: "US4"
primary_edit_seam: "tests/unit/test_speckit_upgrade_inventory.py"
related_paths: ["scripts/speckit_upgrade_inventory.py", "tests/unit/test_speckit_tasks_gate.py"]
---

# HUD: T004 - Upgrade inventory unit coverage

## Objective

Deliver `T004` so the task scope satisfies: Add unit coverage for inventory classification and rollback metadata in tests/unit/test_speckit_upgrade_inventory.py..

## Relevant Domains

- testing: validates deterministic inventory and rollback safety.
- security: proves unsafe paths and protected ledgers cannot enter rollback.
- build pipeline: keeps inventory evidence reproducible.

## Candidate Design Slices

- **PL-01 — Pin and Rehearse Spec Kit Upgrade.** Test the inventory/rehearsal contract introduced by T003.

## Proposed Solution

- Add `tests/unit/test_speckit_upgrade_inventory.py` with temporary declared roots and deterministic file contents.
- Test ownership classification, stable hashes, clean-checkout inputs, path escape rejection, and rollback exclusion for feature artifacts and ledgers.
- Keep all filesystem fixtures local; live clean-checkout proof remains T027.

## Current Repo Behavior

The T003 inventory seam is new. Existing `tests/unit/test_speckit_tasks_gate.py` demonstrates temporary-path unit tests and direct script-module loading without live repository mutation.

## Target Behavior

- Prove US4 inventory and rollback evidence is deterministic and protected paths are never selected.
- Specialize PL-01 by making inventory classification and rollback metadata machine-testable before rehearsal integration.
- Failure cases must identify an invalid path/classification condition without mutation.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_speckit_upgrade_inventory.py` (new), targeting `scripts/speckit_upgrade_inventory.py:collect_inventory` and `build_rehearsal_plan`.

## Reuse Candidates

- `tests/unit/test_speckit_tasks_gate.py:_load_script_module` for isolated script loading.
- `tmp_path` fixtures for hermetic inventory trees.

## Required Edits

- Add fixtures for upstream-managed, repo-owned, missing, and unsafe paths.
- Assert stable SHA-256 output, expected classification, protected rollback exclusions, and one deterministic failure contract.

## Touched Symbols

### Modify

- None.

### Create

- `tests/unit/test_speckit_upgrade_inventory.py:test_collect_inventory_classifies_declared_paths`
- `tests/unit/test_speckit_upgrade_inventory.py:test_build_rehearsal_plan_excludes_ledgers_and_feature_artifacts`
- `tests/unit/test_speckit_upgrade_inventory.py:test_collect_inventory_rejects_path_escape`

### Preserve

- T027 owns live clean-checkout proof; this module remains unit coverage.

## Tests To Add Or Update

- Run `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_speckit_upgrade_inventory.py`.

## Acceptance Criteria

- Identical declared input trees produce identical inventory/rehearsal metadata.
- Unsafe paths and protected rollback targets are rejected or excluded deterministically.

## Done Criteria

- Focused pytest passes without network, Git mutations, or live CLI.
- The tests cover classification, hash stability, clean-checkout input metadata, and rollback exclusions.

## Constraints And Invariants

- Do not create a live checkout or invoke an upstream installer.
- Never write ledger JSONL or change feature artifacts.
- Preserve the fake-only label and leave end-to-end proof to T027.

## Implementation Checklist

- [ ] Build local fixture tree.
- [ ] Assert classifications and hashes.
- [ ] Assert protected exclusions and path rejection.
- [ ] Run the focused pytest guard.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
