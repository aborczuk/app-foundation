---
feature_id: "039"
task_id: "T013b"
task_summary: "Register and resolve the ordered `orchestrate -> extract -> scaffold -> LLM Action -> validate -> emit/handoff` phase contracts in scripts/speckit_workflow_adapter.py, rejecting missing or incompatible contract declarations before a phase runs."
story_id: "US1"
primary_edit_seam: "scripts/speckit_workflow_adapter.py:resolve_phase_contract"
related_paths: ["command-manifest.yaml", "scripts/pipeline_driver.py"]
---

# HUD: T013b - Phase contract registry and resolution

## Objective

Deliver `T013b` so the task scope satisfies: Register and resolve the ordered `orchestrate -> extract -> scaffold -> LLM Action -> validate -> emit/handoff` phase contracts in scripts/speckit_workflow_adapter.py, rejecting missing or incompatible contract declarations before a phase runs..

## Relevant Domains

- resilience: contract lookup must reject drift before a phase starts.
- ops governance: one ordered registry owns canonical phases.
- code patterns: registration remains pure and deterministic.

## Candidate Design Slices

- **PL-04 — Implement Pre-Implementation Phase Workflow Adapters.** Register and resolve the T013a declarations in their required lifecycle order.

## Proposed Solution

- Add `scripts/speckit_workflow_adapter.py:PhaseAdapterRegistry` and `resolve_phase_contract` over T013a contracts.
- Enforce canonical sequence `orchestrate, extract, scaffold, LLM Action, validate, emit/handoff`; validate matching manifest version/provenance, unique phase names, compatible predecessor/successor, and declared artifact paths.
- Return a typed drift or missing-contract outcome before a runner is selected.

## Current Repo Behavior

T013a introduces phase contract data; `scripts/pipeline_driver.py` currently resolves execution behavior without a single phase-contract registry.

## Target Behavior

- Preserve FR-004, FR-005, FR-006, and FR-028: route selection is explicit, ordered, version-compatible, and single-owned.
- Specialize PL-04 by rejecting missing/incompatible declarations before execution.
- Registry resolution never runs a phase or appends an event.

## Primary Edit Seam

**File:Symbol**: `scripts/speckit_workflow_adapter.py:resolve_phase_contract` (new).

## Reuse Candidates

- T013a `PhaseAdapterContract` list.
- T005 manifest version/provenance and canonical routes.

## Required Edits

- Add immutable ordered registry, canonical phase order constant, and resolver checks for uniqueness, predecessor, version, and required declarations.
- Expose structured diagnostics and non-mutating next action for missing/drift cases.

## Touched Symbols

### Modify

- `scripts/speckit_workflow_adapter.py:build_phase_contracts`

### Create

- `scripts/speckit_workflow_adapter.py:PhaseAdapterRegistry`
- `scripts/speckit_workflow_adapter.py:resolve_phase_contract`

### Preserve

- Runner/validator invocation and event bridge for T013c.

## Tests To Add Or Update

- T016 verifies valid ordering and missing/incompatible registration rejection.
- Add no-runner assertions for resolution failure.

## Acceptance Criteria

- Each canonical phase resolves exactly one compatible contract in order.
- Duplicate, missing, out-of-order, or version-drift contracts fail deterministically before execution.

## Done Criteria

- T016 registry tests pass.
- Resolution returns no callable execution path on drift/failure.

## Constraints And Invariants

- Registry is read-only and does not infer phase from cwd/branch.
- Preserve manifest as source of ownership/version.
- No success event during lookup.

## Implementation Checklist

- [ ] Define canonical order.
- [ ] Register T013a contracts once.
- [ ] Validate drift/order/uniqueness.
- [ ] Return typed contract or blocked outcome.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
