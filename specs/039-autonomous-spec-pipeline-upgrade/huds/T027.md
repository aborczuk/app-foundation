---
feature_id: "039"
task_id: "T027"
task_summary: "Add clean-checkout and rollback rehearsal tests in tests/integration/test_speckit_upgrade_rehearsal.py."
story_id: "US5"
primary_edit_seam: "tests/integration/test_speckit_upgrade_rehearsal.py"
related_paths: ["scripts/speckit_upgrade_inventory.py", "scripts/speckit_workflow_preflight.py"]
---

# HUD: T027 - Clean checkout and rollback rehearsal

## Objective

Deliver `T027` so the task scope satisfies: Add clean-checkout and rollback rehearsal tests in tests/integration/test_speckit_upgrade_rehearsal.py..

## Relevant Domains

- build pipeline: clean checkout proves reproducible upgrade/customization packaging.
- resilience: rollback evidence preserves protected artifacts and ledgers.
- testing: rehearsal must use isolated real install path.

## Candidate Design Slices

- **PL-01 — Pin and Rehearse Spec Kit Upgrade** and **PL-07 — Prove Live Prompt-to-Handoff Safety.** Prove isolated upgrade reproduction and rollback.

## Proposed Solution

- Create `tests/integration/test_speckit_upgrade_rehearsal.py` using T003 inventory/rehearsal plan in a disposable clean checkout.
- Install only pinned upstream/project customization at supported seam, verify commands/templates/gates reproduce without hand-editing upstream core, then execute declared rollback.
- Assert feature artifacts and both ledger histories remain untouched and output contains semantic migration/rollback evidence.

## Current Repo Behavior

T003 defines inventory/rehearsal metadata but there is no live clean-checkout/rollback integration proof.

## Target Behavior

- Preserve US4/AS-4.1, AS-4.3, and FR-002/FR-003/FR-032.
- Upgrade rehearsal is isolated, pinned, reproducible, and rollback-safe.
- No production workspace or active nonterminal run is modified.

## Primary Edit Seam

**File:Symbol**: `tests/integration/test_speckit_upgrade_rehearsal.py` (new).

## Reuse Candidates

- T001 preflight, T003 inventory/rollback plan, and T026a live prerequisite fixtures.

## Required Edits

- Add clean checkout fixture, pinned install/reproduction assertion, semantic comparison evidence, rollback executor/assertions, and protected-path snapshot.
- Fail if customization needs upstream core hand edits or rollback changes ledgers/spec artifacts.

## Touched Symbols

### Modify

- None.

### Create

- `tests/integration/test_speckit_upgrade_rehearsal.py:test_clean_checkout_reproduces_pinned_customization`
- `tests/integration/test_speckit_upgrade_rehearsal.py:test_rollback_preserves_feature_and_ledger_history`

### Preserve

- Production repository and active workflows.

## Tests To Add Or Update

- Run documented integration selector against real pinned installer/runner prerequisites.

## Acceptance Criteria

- Clean checkout reproduces declared commands/templates/gates from versioned customization.
- Rollback evidence restores only permitted upgrade files and preserves feature artifacts/ledgers.

## Done Criteria

- Real clean-checkout selector passes.
- Test artifacts include bounded migration and rollback proof.

## Constraints And Invariants

- No blind refresh or production mutation.
- Explicit temporary checkout only.
- Pin `v0.12.9`; no floating source.

## Implementation Checklist

- [ ] Create disposable checkout.
- [ ] Run pinned preflight/install/reproduction.
- [ ] Compare declared semantics.
- [ ] Run rollback and assert protected-path preservation.

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
