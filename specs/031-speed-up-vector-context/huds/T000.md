---
feature_id: "031"
task_id: "T000"
task_summary: "Define the accepted scoped, broad, and markdown benchmark corpus in `specs/031-speed-up-vector-context/tasks.md` and `specs/031-speed-up-vector-context/plan.md`"
primary_edit_seam: "specs/031-speed-up-vector-context/plan.md:Research Baseline"
related_paths: ["specs/031-speed-up-vector-context/tasks.md", "specs/031-speed-up-vector-context/plan.md"]
---

# HUD: T000 - Define the accepted scoped, broad, and markdown benchmark corpus in `specs/031-speed-up-vector-context/tasks.md` and `specs/031-speed-up-vector-context/plan.md`

## Objective

Deliver `T000` so the feature has an explicit benchmark corpus for scoped code queries, broad code-plus-markdown discovery, and markdown-first reads before any routing changes land.

This task locks the acceptance examples and validation commands the later implementation and regression tasks must preserve.

## Relevant Domains

- `Spec and Process-First`: this task turns the plan baseline into the concrete benchmark corpus the rest of the feature will measure against.
- `Code Reading and Discovery`: the corpus must cover the exact query shapes that exposed the current latency and routing problems.
- `Test Driven Verification First`: later tasks need the accepted queries and expected seams before they can claim speed or correctness improvements.

## Candidate Design Slices

- `Slice PL-03 - Benchmark and Regression Coverage` — add benchmark cases for scoped code queries, broad code-plus-markdown discovery, markdown-oriented reads, and stale-trust escalation. Primary seams: `specs/031-speed-up-vector-context/plan.md:Research Baseline` and `specs/031-speed-up-vector-context/tasks.md`.

## Proposed Solution

- Expand the feature artifacts to name the exact benchmark queries, expected seam classes, and validation commands that the implementation tasks must preserve.
- Keep the corpus split into scoped, broad, and markdown-oriented buckets so later tasks can refer to the right benchmark set without re-deriving it.
- Change the following symbols/artifacts in this task: `specs/031-speed-up-vector-context/tasks.md` and `specs/031-speed-up-vector-context/plan.md:Research Baseline`.

## Current Repo Behavior

- `tasks.md` names the feature-level goals and independent tests but does not yet enumerate a stable accepted benchmark corpus.
- `plan.md` contains the timing baseline and the high-level design slices, but the benchmark queries and acceptance examples are not yet locked into a task-local artifact.

## Target Behavior

- The feature artifacts explicitly name the scoped, broad, and markdown benchmark queries the implementation must preserve.
- The corpus includes expected validation commands and the timing baseline already captured in `plan.md`.
- The task fulfills `Slice PL-03` by making the later regression and benchmark work measurable from approved artifacts instead of memory.

## Primary Edit Seam

**File:Symbol**: `specs/031-speed-up-vector-context/plan.md:Research Baseline`

## Reuse Candidates

- Reuse the measured timings already captured in `specs/031-speed-up-vector-context/spec.md` and `specs/031-speed-up-vector-context/plan.md`.
- Reuse the story independent-test language already present in `tasks.md` rather than inventing a second acceptance contract.

## Required Edits

- Update `specs/031-speed-up-vector-context/tasks.md` to enumerate the accepted benchmark corpus and validation expectations.
- Update `specs/031-speed-up-vector-context/plan.md` research or benchmark notes so the tasks and plan stay aligned.

## Touched Symbols

### Modify

- `specs/031-speed-up-vector-context/tasks.md` - add the explicit benchmark corpus and validation expectations.
- `specs/031-speed-up-vector-context/plan.md:Research Baseline` - keep the accepted timings and benchmark examples aligned with the task list.

### Create

- None.

### Preserve

- Preserve the existing story ordering and design slice mapping.
- Preserve the measured baseline numbers already accepted in the spec and plan.

## Tests To Add Or Update

- No new automated test module in this task.
- Validation command: `uv run python scripts/speckit_tasks_gate.py validate-format --tasks-file specs/031-speed-up-vector-context/tasks.md --json`.
- Manual artifact check: confirm the benchmark corpus in `tasks.md` matches the accepted baseline in `plan.md`.

## Acceptance Criteria

- The feature artifacts explicitly define scoped, broad, and markdown benchmark queries.
- The artifacts identify the validation commands later tasks will use to prove no regression.
- The task does not widen into implementation logic; it only locks the benchmark contract.

## Done Criteria

- `tasks.md` includes the accepted benchmark corpus and validation expectations.
- `plan.md` remains aligned with that corpus and the measured baseline.
- `uv run python scripts/speckit_tasks_gate.py validate-format --tasks-file specs/031-speed-up-vector-context/tasks.md --json` passes after the edit.

## Constraints And Invariants

- Do not change the implementation plan or task sequencing in this task.
- Preserve the existing measured timings rather than re-estimating them here.
- Keep the benchmark corpus narrow and tied to the feature goals.

## Implementation Checklist

- [ ] Confirm the current plan and tasks mention goals but not a fully explicit benchmark corpus.
- [ ] Add the accepted benchmark categories and validation commands to the feature artifacts.
- [ ] Verify the updated artifacts remain aligned with the approved plan and spec.

## Dependencies

- None.
- Unblocks all later tasks by providing the accepted benchmark corpus they must preserve.

## Process Checklist

- [ ] current_behavior_verified from bounded reads of `tasks.md` and `plan.md`
- [ ] implementation_directive_complete for `Slice PL-03 - Benchmark and Regression Coverage`
- [ ] touched_symbols_verified against the feature artifacts
- [ ] tests_specified with concrete validation commands

