---
feature_id: "031"
task_id: "T004"
task_summary: "Add scoped fast-path regression coverage for exact-symbol and file-local context reads in `tests/unit/test_read_code_shortlist.py` and `tests/unit/test_read_code_index_refresh.py`"
story_id: "US1"
primary_edit_seam: "tests/unit/test_read_code_shortlist.py:_query_semantic_anchor_candidate"
related_paths: ["tests/unit/test_read_code_shortlist.py", "tests/unit/test_read_code_index_refresh.py"]
---

# HUD: T004 - Add scoped fast-path regression coverage for exact-symbol and file-local context reads in `tests/unit/test_read_code_shortlist.py` and `tests/unit/test_read_code_index_refresh.py`

## Objective

Deliver `T004` so scoped fast-path behavior is covered by explicit regression tests before the retrieval and trust shortcuts are implemented.

This task protects exact-symbol and file-local reads from accidental seam regressions while the routing changes land.

## Relevant Domains

- `Test Driven Verification First`: this task defines the regression coverage the scoped implementation tasks must satisfy.
- `Code Reading and Discovery`: the coverage must exercise the real scoped query shapes that currently pay broad costs.
- `Resilience`: the tests need to catch both correctness regression and accidental fallback to the slower broad path.

## Candidate Design Slices

- `Slice PL-03 - Benchmark and Regression Coverage` — add validation coverage for scoped code queries and stale-trust escalation. Primary seams: `tests/unit/test_read_code_shortlist.py` and `tests/unit/test_read_code_index_refresh.py`.

## Proposed Solution

- Extend `tests/unit/test_read_code_shortlist.py` with scoped-query fixtures that prove exact-symbol and file-local reads keep selecting the same top candidate while markdown retrieval can be skipped later.
- Extend `tests/unit/test_read_code_index_refresh.py` with targeted assertions around the read-preflight trust contract for scoped requests.
- Keep the tests narrow and feature-specific so they assert request class, seam stability, and preflight routing without duplicating broader integration coverage.

## Current Repo Behavior

- `tests/unit/test_read_code_shortlist.py` already covers shortlist and semantic-anchor behavior but does not yet lock the scoped fast-path contract described in this feature.
- `tests/unit/test_read_code_index_refresh.py` already includes refresh/read-preflight coverage, including persisted probe-cache behavior, but does not yet cover the new scoped trust-routing expectations.

## Target Behavior

- The unit test suite explicitly covers exact-symbol and file-local scoped reads.
- The tests prove seam selection stays stable while the implementation begins skipping unnecessary broad work.
- The task fulfills `Slice PL-03` by establishing the scoped regression harness before or alongside the implementation tasks.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_read_code_shortlist.py:_query_semantic_anchor_candidate`

## Reuse Candidates

- Reuse existing shortlist test helpers and fixtures in `tests/unit/test_read_code_shortlist.py`.
- Reuse persisted probe-cache and refresh helpers already exercised in `tests/unit/test_read_code_index_refresh.py`.

## Required Edits

- Add scoped-query tests in `tests/unit/test_read_code_shortlist.py`.
- Add scoped-preflight assertions in `tests/unit/test_read_code_index_refresh.py`.
- Keep assertions centered on seam stability, request classification, and trust routing for scoped reads.

## Touched Symbols

### Modify

- `tests/unit/test_read_code_shortlist.py` - add scoped exact-symbol and file-local fast-path regression tests.
- `tests/unit/test_read_code_index_refresh.py` - add scoped trust-routing assertions for read preflight.

### Create

- None unless a small local fixture/helper is required inside the same test modules.

### Preserve

- Preserve existing shortlist and refresh coverage that is unrelated to this feature.
- Preserve the test modules' offline/unit testing boundaries.

## Tests To Add Or Update

- `tests/unit/test_read_code_shortlist.py`
- `tests/unit/test_read_code_index_refresh.py`
- Validation command: `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_read_code_shortlist.py tests/unit/test_read_code_index_refresh.py -k "context or refresh_indexes_for_read"`.

## Acceptance Criteria

- The unit suite covers exact-symbol and file-local scoped reads.
- The suite includes assertions that detect regressions in scoped seam selection and scoped trust routing.
- The tests are in place before later scoped implementation tasks are considered complete.

## Done Criteria

- The two unit modules contain the new scoped regression coverage.
- The targeted `pytest_guard.py` command passes.
- The assertions are specific enough to fail if the implementation falls back to broad behavior for scoped reads.

## Constraints And Invariants

- Keep this task in unit/contract coverage; do not widen into integration or performance testing.
- Do not duplicate broad-discovery assertions that belong to `T008`.
- Preserve the current offline/unit testing pattern in both modules.

## Implementation Checklist

- [ ] Confirm the current unit suites do not fully cover the new scoped contract.
- [ ] Add scoped seam-selection assertions.
- [ ] Add scoped trust-routing assertions.
- [ ] Verify the targeted unit command passes.

## Dependencies

- Depends on `T000` for the accepted benchmark corpus.
- Can proceed alongside the first scoped implementation tasks.
- Feeds `T005`, `T006`, and `T007` by defining their regression harness.

## Process Checklist

- [ ] current_behavior_verified from bounded reads of the two unit test modules
- [ ] implementation_directive_complete for `Slice PL-03 - Benchmark and Regression Coverage`
- [ ] touched_symbols_verified against the listed test files
- [ ] tests_specified with the concrete unit command

