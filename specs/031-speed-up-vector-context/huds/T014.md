---
feature_id: "031"
task_id: "T014"
task_summary: "Capture the post-change benchmark evidence and accepted validation commands in `specs/031-speed-up-vector-context/tasks.md` and `tests/integration/test_codebase_vector_index_performance.py`"
primary_edit_seam: "tests/integration/test_codebase_vector_index_performance.py:module"
related_paths: ["specs/031-speed-up-vector-context/tasks.md", "tests/integration/test_codebase_vector_index_performance.py"]
---

# HUD: T014 - Capture the post-change benchmark evidence and accepted validation commands in `specs/031-speed-up-vector-context/tasks.md` and `tests/integration/test_codebase_vector_index_performance.py`

## Objective

Deliver `T014` so the feature closes with recorded benchmark evidence and the accepted validation commands for the final behavior.

This task is the post-change evidence capture for the whole feature.

## Relevant Domains

- `Spec and Process-First`: the feature should end with explicit benchmark evidence, not just an implementation claim.
- `Test Driven Verification First`: the accepted validation commands must be recorded for future reruns.
- `Code Reading and Discovery`: the evidence must reflect the final scoped, broad, and stale-escalation behavior.

## Candidate Design Slices

- `Slice PL-03 - Benchmark and Regression Coverage` — capture benchmark evidence and settled validation commands. Primary seams: `tests/integration/test_codebase_vector_index_performance.py:module` and `specs/031-speed-up-vector-context/tasks.md`.

## Proposed Solution

- Record the post-change benchmark evidence for the accepted scoped, broad, and stale-escalation corpus.
- Update the feature artifacts so the final validation commands and measured outcome are explicit.
- If the integration performance module needs a feature-local assertion or note for the final evidence path, add it there rather than creating a separate ad hoc script.
- Change the following artifacts in this task: `tests/integration/test_codebase_vector_index_performance.py` and `specs/031-speed-up-vector-context/tasks.md`.

## Current Repo Behavior

- `tests/integration/test_codebase_vector_index_performance.py` already contains timing-budget and performance-oriented integration coverage.
- `tasks.md` describes the feature goals and validation direction but does not yet capture the final post-change evidence bundle.
- The feature currently has measured pre-change numbers in the spec and plan, but not the final settled outcome artifact.

## Target Behavior

- The final benchmark evidence for scoped, broad, and escalation behavior is recorded.
- The feature artifacts list the accepted validation commands that should be rerun in the future.
- The task fulfills `Slice PL-03` by turning the performance and regression work into a final evidence bundle.

## Primary Edit Seam

**File:Symbol**: `tests/integration/test_codebase_vector_index_performance.py:module`

## Reuse Candidates

- Reuse the performance harness in `tests/integration/test_codebase_vector_index_performance.py`.
- Reuse the benchmark corpus defined in `T000` and exercised by the earlier test tasks.
- Reuse the measured baseline from the plan and spec rather than recomputing the starting point.

## Required Edits

- Update the integration performance module only as needed to anchor the final benchmark evidence.
- Update `tasks.md` with the settled validation commands and evidence references.
- Keep the evidence tied to the accepted corpus and not a new ad hoc benchmark.

## Touched Symbols

### Modify

- `tests/integration/test_codebase_vector_index_performance.py:module` - capture or support the final feature-relevant benchmark evidence path.
- `specs/031-speed-up-vector-context/tasks.md` - record the final validation commands and evidence references.

### Create

- None.

### Preserve

- Preserve the existing performance-test harness.
- Preserve the accepted benchmark corpus categories from `T000`.

## Tests To Add Or Update

- `tests/integration/test_codebase_vector_index_performance.py`
- Final validation commands should include the targeted unit and integration commands settled by the implementation tasks.
- At minimum, rerun the final performance and regression selectors through `pytest_guard.py` and record them in the artifact.

## Acceptance Criteria

- The post-change benchmark evidence is explicitly recorded.
- The feature artifacts list the accepted validation commands.
- The evidence references the same scoped, broad, and escalation corpus approved earlier in the feature.

## Done Criteria

- The integration performance artifact and `tasks.md` record the final evidence path.
- The final validation commands are explicit and reusable.
- The recorded evidence reflects the final implementation rather than the pre-change baseline.

## Constraints And Invariants

- Do not widen this task into more implementation logic.
- Keep evidence tied to the accepted corpus and existing performance harness.
- Preserve the feature's task ordering and story structure.

## Implementation Checklist

- [ ] Confirm the current artifacts do not yet record final benchmark evidence.
- [ ] Update the performance artifact or module to anchor the final evidence path.
- [ ] Update `tasks.md` with final validation commands and evidence references.
- [ ] Verify the recorded commands match the actual implemented behavior.

## Dependencies

- Depends on `T004` through `T013`.
- Can run in parallel with `T013` once implementation is stable.
- Closes the feature's evidence trail before `solution` finalization.

## Process Checklist

- [ ] current_behavior_verified from bounded reads of the performance module and tasks artifact
- [ ] implementation_directive_complete for the final evidence-capture task
- [ ] touched_symbols_verified against the listed artifacts
- [ ] tests_specified with final validation commands

