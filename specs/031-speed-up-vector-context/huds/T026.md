# HUD: T026 - Add failing contract and live-backend persistence coverage for the project-local MCP stdio server in `tests/unit/test_persistence_probe_server.py` and `tests/integration/test_codebase_vector_index_performance.py`

## Objective

Deliver `T026` so the project-local MCP stdio server persistence check becomes an explicit acceptance gate for the remaining startup-migration work.

This task must prove, with live backend evidence, that the platform-owned MCP server keeps one stable process identity across separate agent turns. That proof is the prerequisite for replacing the per-invocation worker path in later tasks.

## Relevant Domains

- `Code Reading and Discovery`: the persistence proof exists only to unblock moving `read_code` backend ownership behind the MCP server.
- `Testing`: this task is primarily about live-backend verification, with a small unit contract extension for the probe server.
- `Governance`: the task establishes the accepted transport boundary that later implementation tasks must obey.

## Candidate Design Slices

- `Slice PL-05 - Daemon Lifecycle, Observability, and Live Proof` — keep a live proof seam for the long-lived backend identity rather than treating transport viability as assumed.
- `Slice PL-07 - Daemon-Owned Remaining First-Search Startup` — the first-search startup migration is allowed only after the warm backend is proven to persist across sandboxed turns.

## Proposed Solution

- Extend `src/mcp_codebase/persistence_probe_server.py` only as needed to keep the identity contract explicit and stable: server name, `project_root`, `pid`, and `started_at`.
- Add unit coverage in `tests/unit/test_persistence_probe_server.py` that locks the identity shape and the one-time startup capture behavior.
- Add live verification in `tests/integration/test_codebase_vector_index_performance.py` that compares the MCP probe identity against the previously recorded identity artifact and fails when the process changes across turns.
- Reuse the existing probe artifact path and helper script instead of inventing a second persistence check mechanism.

## Current Repo Behavior

- `src/mcp_codebase/persistence_probe_server.py` already exposes one FastMCP tool, `get_process_identity`, that returns `name`, `project_root`, `pid`, and `started_at`.
- `tests/unit/test_persistence_probe_server.py` currently verifies only that server construction captures one PID and one startup timestamp.
- The repo has a local probe script that showed the per-invocation stdio worker does not persist across fresh `uv run ... scripts/read_code.py ...` calls, but there is no canonical integration test yet that treats the MCP server identity as the transport gate for the migration.

## Target Behavior

- The persistence probe server contract is explicit and stable enough that later tasks can depend on it without rereading implementation details.
- A live integration test proves the same MCP server `pid` and `started_at` survive across separate agent turns once the platform has reloaded `.codex/config.toml`.
- The task preserves the distinction between the accepted MCP-persistent route and the rejected per-invocation worker route.
- The exact task seam at `src/mcp_codebase/persistence_probe_server.py:get_process_identity` continues to return bounded identity metadata only; it does not grow into query or rerank behavior in this task.

## Primary Edit Seam

**File:Symbol**: `src/mcp_codebase/persistence_probe_server.py:get_process_identity`

## Reuse Candidates

- Reuse `scripts/probe_read_code_worker_persistence.py` and its artifact contract for the comparison shape rather than inventing a new result format.
- Reuse the existing live-performance test module, `tests/integration/test_codebase_vector_index_performance.py`, as the canonical live-backend verification surface for `read_code` infrastructure.
- Reuse the existing FastMCP server wrapper in `src/mcp_codebase/persistence_probe_server.py`.

## Required Edits

- Extend `tests/unit/test_persistence_probe_server.py` so it asserts the exact identity keys and one-time capture behavior exposed by `get_process_identity`.
- Add a live integration test in `tests/integration/test_codebase_vector_index_performance.py` that records and compares MCP identity across runs/turns and fails if `pid` or `started_at` change unexpectedly.
- Keep `src/mcp_codebase/persistence_probe_server.py` bounded to identity reporting, but tighten any helper or return contract needed to support the tests cleanly.

## Touched Symbols

### Modify

- `src/mcp_codebase/persistence_probe_server.py:PersistenceProbeServer._register_tools` - keep the identity response bounded and explicit.
- `tests/unit/test_persistence_probe_server.py:test_create_server_captures_one_stable_identity` - extend it from construction-only assertions to identity-contract assertions.
- `tests/integration/test_codebase_vector_index_performance.py` - add the MCP persistence proof test that compares current and recorded identity.

### Create

- `tests/integration/test_codebase_vector_index_performance.py::test_live_project_local_mcp_server_persists_across_turns` - opt-in live verification for the accepted transport.

### Preserve

- `src/mcp_codebase/persistence_probe_server.py:create_server` - keep server creation simple and side-effect free beyond capturing identity once.
- `.codex/config.toml` server registration for `read_code_persistence_probe`.

## Tests To Add Or Update

- Update `tests/unit/test_persistence_probe_server.py`
- Add/update one live test in `tests/integration/test_codebase_vector_index_performance.py`
- Reuse targeted lint on the touched Python files

## Acceptance Criteria

- The unit test locks the exact identity contract exposed by `get_process_identity`.
- The live integration test proves the project-local MCP server keeps the same `pid` and `started_at` across separate turns in this environment.
- The task leaves a clear pass/fail gate for later migration tasks: if the MCP identity changes, later tasks must not switch `read_code` to the MCP-owned backend.

## Done Criteria

- Targeted unit and live tests pass.
- Task evidence clearly states the observed identity values and whether they matched across turns.
- The task ledger records a pass-worthy QA result before closeout.

## Constraints And Invariants

- Do not reintroduce socket transport or file-RPC in this task.
- Do not move query or rerank work onto the MCP server yet; this task proves persistence only.
- Keep the live test opt-in and bounded so ordinary local test runs do not require a platform MCP lifecycle.

## Implementation Checklist

- Read the probe server seam and existing unit test once.
- Add the unit contract assertions.
- Add the live cross-turn persistence proof.
- Run targeted validation and capture the identity values in the task evidence.

## Dependencies

- Prior registered daemon-worker branch tasks `T021` through `T025` are closed as superseded.
- This task gates `T027` through `T030`.

## Process Checklist

- Append `task_started` before implementation handoff.
- Hand the HUD to the builder subagent as the authoritative implementation ticket.
- Send builder output and changed files to the QA subagent for offline QA before commit/closeout.
