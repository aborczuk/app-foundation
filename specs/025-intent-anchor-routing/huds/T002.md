---
feature_id: "025"
task_id: "T002"
---

# HUD: T002 — Introduce a normalized composite confidence score on a 0-100 scale and widen semantic retrieval to top_k = 20

## Working Memory

**File:Symbol**: `scripts/read_code.py:_vector_anchor_rank`  
**Callers**: `read_code_context`, `read_code_window`  
**Reuse path**: reuse the current ranking seam and add the normalized score contract there.

## Solution Sketch

- Modify the ranker to combine the existing signals into one normalized composite confidence score.
- Widen semantic retrieval to `top_k = 20` so shortlist selection has enough recall.
- Keep the ranking deterministic and bounded.

## Test Specification

Assert that ranking emits a `0-100` composite score and retrieval uses the wider candidate pool before shortlist capping.

## Security Review

N/A — local ranking and retrieval logic only.

## Functional Goal

**Story Goal**: Establish the shared confidence contract for all shortlist and body decisions.  
**Acceptance Criteria**: The helper can rank with a normalized score and retrieve enough candidates for shortlist formation.

## Quality Guards

- The score must be explicit and normalized.
- Retrieval must stay bounded even when widened.
- Domains 13, 14, and 17 remain part of the review set.

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
