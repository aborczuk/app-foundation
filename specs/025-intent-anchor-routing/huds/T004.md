---
feature_id: "025"
task_id: "T004"
---

# HUD: T004 — Add top-candidate body selection logic so the highest-confidence candidate returns its indexed body inline when confidence is at least 90/100

## Working Memory

**File:Symbol**: `scripts/read_code.py:read_code_context`  
**Callers**: `read_code_context` internals  
**Reuse path**: reuse the current context resolver and add the body-first branch on top of it.

## Solution Sketch

- Normalize the top candidate score onto the shared `0-100` scale.
- Inline the indexed body when the top candidate clears the `90/100` cutoff.
- Keep the shortlist alongside the body payload.

## Test Specification

Assert that a high-confidence top candidate returns its body inline while preserving the shortlist.

## Security Review

N/A — indexed body retrieval only.

## Functional Goal

**Story Goal**: Let the agent learn directly from the most confident symbol body when the score is strong enough.  
**Acceptance Criteria**: The top item body is returned inline at or above the threshold.

## Quality Guards

- Body-first is additive, not a replacement.
- Confidence cutoff stays normalized and explicit.
- Domains 13, 14, and 17 remain included.

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
