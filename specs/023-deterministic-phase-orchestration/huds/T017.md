---
feature_id: "023"
task_id: "T017"
---

# HUD: T017 — Add contract tests for route-mode normalization and emit-contract schema validity in `tests/contract/test_pipeline_driver_contract.py`

## Working Memory

**File:Symbol**: `tests/contract/test_pipeline_driver_contract.py:test_normalize_driver_mode_aliases` and `tests/contract/test_pipeline_driver_contract.py:test_load_driver_routes_normalizes_mode_and_emit_contracts`
**Callers**: `scripts/pipeline_driver_contracts.py:load_driver_routes` calls `normalize_driver_mode`; `resolve_step_mapping`, `append_pipeline_success_event`, and `validate_command_coverage` call `load_driver_routes`
**Reuse path**: Reuse the existing contract fixture style in the same file and keep the new assertions focused on schema normalization rather than runtime behavior

## Solution Sketch

**Modify**: `tests/contract/test_pipeline_driver_contract.py`
**Create**: none
**Reuse**: existing `contracts` import, `pytest`, and `Path` fixture style already in the contract file
**Composition**: cover the canonical route-mode aliases and verify manifest route loading trims emit contract fields into the normalized `emit_contracts` structure
**Test Specification**:
- `normalize_driver_mode(None)` returns `legacy`
- `normalize_driver_mode("script")` returns `deterministic`
- `normalize_driver_mode("template")` returns `generative`
- `load_driver_routes()` trims `required_fields`, resolves relative `script_path`, and preserves canonical emit contract schema
**Domains touched**: contract verification, schema governance

## Test Specification

The contract file should prove that route metadata is normalized before orchestration uses it:

- route mode aliases collapse to canonical values
- manifest emit contracts are stored as trimmed field lists
- a route without a driver block still resolves to legacy mode
- the test should only assert contract shape, not perform any runtime emission

## Security Review

N/A — test-only change with no external source or security-sensitive logic

## Functional Goal

**Story Goal**: Command docs emit producer payload contracts only; orchestration/validation/emission remain driver-owned.
**Acceptance Criteria**: Execute a migrated command flow where command docs return completion payload only and verify validation and event emission are performed by orchestration components.

## Quality Guards

- Domain 13: contract assertions must mirror the canonical route contract and remain deterministic
- Domain 14: manifest-derived contract data must not drift from orchestration-owned behavior
- Domain 17: emitted route metadata must stay schema-valid and trim whitespace from required field names

## Process Checklist

- [X] discovery_completed
- [X] lld_recorded
- [X] quality_guards_passed
- [X] functional_goal_achieved
- [X] tests_passed
