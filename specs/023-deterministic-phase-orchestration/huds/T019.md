---
feature_id: "023"
task_id: "T019"
---

# HUD: T019 — Add command-script coverage regression for manifest/doc alignment in `tests/unit/test_validate_command_script_coverage.py`

## Working Memory

**File:Symbol**: `tests/unit/test_validate_command_script_coverage.py:test_validate_command_script_coverage_passes_with_required_scripts`
**Callers**: `scripts/validate_command_script_coverage.py:main` calls `validate_command_script_coverage`
**Reuse path**: Reuse the existing manifest fixture helpers and the coverage validator module already loaded in the test file

## Solution Sketch

trivial — no sketch required

## Test Specification

The coverage regression should prove the validator accepts a manifest/doc set when the required script references are present:

- the canonical manifest and legacy mirror manifest both exist
- `pipeline-scaffold.py` exists at the expected `.specify/scripts` path
- the required bash scripts `create-new-feature.sh` and `setup-plan.sh` exist
- `validate_command_script_coverage(...)` returns `ok == True`
- `uncovered_commands` is empty and `reasons` is empty
- a missing required scaffold reference should still be reported as uncovered in the companion negative test

## Security Review

N/A — unit-test-only regression with no external source

## Functional Goal

**Story Goal**: Command docs emit producer payload contracts only; orchestration/validation/emission remain driver-owned.
**Acceptance Criteria**: Execute a migrated command flow where command docs return completion payload only and verify validation and event emission are performed by orchestration components.

## Quality Guards

- Domain 13: manifest and script coverage must stay deterministic
- Domain 14: coverage validation should match the canonical manifest contract, not a hand-built one-off
- Domain 17: uncovered commands must surface precise missing scaffold reasons

## Process Checklist

- [X] discovery_completed
- [X] quality_guards_passed
- [X] functional_goal_achieved
- [X] tests_passed
