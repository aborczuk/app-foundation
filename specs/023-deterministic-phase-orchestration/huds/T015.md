---
feature_id: "023"
task_id: "T015"
---

# HUD: T015 — Strengthen phase-state reconciliation and machine-readable drift reasons in `scripts/pipeline_driver_state.py`

## Working Memory

**File:Symbol**: `scripts/pipeline_driver_state.py:resolve_phase_state`  
**Callers**: `scripts/pipeline_driver.py:main`, `tests/unit/test_pipeline_driver.py:test_resolve_phase_state_prefers_ledger_authority`  
**Reuse path**: Reuse the existing ledger reader and sequence validator; extend the return shape rather than replacing the resolver.

## Solution Sketch

Keep ledger authority as the source of truth, but add machine-readable drift metadata alongside the existing human-readable reason strings. Preserve `drift_reasons` for compatibility, and add structured codes/details for phase-hint conflicts, missing artifacts, and ledger read failures.

## Test Specification

Assert that a stale hinted phase is overridden by the ledger, that `drift_reason_codes` is stable and sorted, and that `drift_reason_details` includes the code plus the relevant context fields for the phase-hint conflict.

## Security Review

No external inputs are added. The change only exposes more structured state from the existing ledger reconciliation path.

## Functional Goal

**Story Goal**: User Story 2 - Approval-Gated Side-Effect Execution  
**Acceptance Criteria**: Phase state is ledger-authoritative, and drift output is deterministic and machine-readable.

## Quality Guards

- Ledger remains authoritative over hinted phase fields.
- Drift reasons must be deterministic and machine-readable.
- Existing compatibility fields stay intact.
- Domains 13, 14, and 17 remain in scope.

## Process Checklist

- [x] discovery_completed
- [x] lld_recorded
- [x] quality_guards_passed
- [x] functional_goal_achieved
- [x] tests_passed
