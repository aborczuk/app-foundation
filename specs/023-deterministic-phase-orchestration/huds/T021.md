---
feature_id: "023"
task_id: "T021"
---

# HUD: T021 — Update producer-only contract language and remove implicit append ownership in `.claude/commands/speckit.solution.md`

## Working Memory

**File:Symbol**: `.claude/commands/speckit.solution.md:Compact Contract (Load First)` and `.claude/commands/speckit.solution.md:Behavior rules`
**Callers**: `tests/unit/test_speckit_solution_command_doc.py:test_speckit_solution_doc_removes_direct_ledger_append_ownership`
**Reuse path**: Reuse the existing compact/expanded command-doc structure and keep the solution doc aligned with the producer-only wording pattern

## Solution Sketch

trivial — no sketch required

## Test Specification

The command doc should describe the solution phase as a producer-only payload contract:

- the doc keeps the compact/expanded heading pattern used by the markdown helper
- the top description says the phase produces the `solution_approved` payload before analyze
- the compact contract says the command produces the payload for orchestration instead of writing to `.speckit/pipeline-ledger.jsonl`
- the behavior rules forbid claiming direct ledger append ownership in the command doc

The regression should fail if the direct ledger file path returns to the doc body.

## Security Review

N/A — documentation-only change with a unit-test regression

## Functional Goal

**Story Goal**: Command docs emit producer payload contracts only; orchestration/validation/emission remain driver-owned.
**Acceptance Criteria**: Execute a migrated command flow where command docs return completion payload only and verify validation and event emission are performed by orchestration components.

## Quality Guards

- Domain 13: command-doc language must stay deterministic and helper-parseable
- Domain 14: producer-only wording must not leak direct ledger ownership back into the doc
- Domain 17: the solution payload wording must remain stable for downstream tooling

## Process Checklist

- [X] discovery_completed
- [X] quality_guards_passed
- [X] functional_goal_achieved
- [X] tests_passed
