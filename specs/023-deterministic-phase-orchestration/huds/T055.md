---
feature_id: "023"
task_id: "T055"
---

# HUD: T055 - Harden markdown contract validator scope to enforce required command set and reject executable procedure markers in enforced sections

## Working Memory

**File:Symbol**: `scripts/validate_markdown_doc_shapes.py:validate_markdown_doc_shape`
**Callers**: Resolve during implement discovery via codegraph caller query for this symbol.
**Reuse path**: Reuse existing behavior in `scripts/validate_markdown_doc_shapes.py` and extend deterministically for this task.

## Solution Sketch

**Modify**: `scripts/validate_markdown_doc_shapes.py:validate_markdown_doc_shape` to satisfy this task contract from `tasks.md`.
**Create**: none (introduce net-new helpers only if reuse proves insufficient during implementation).
**Reuse**: Existing contracts and helpers already present in the target module/test harness.
**Composition**: Harden markdown contract validator scope to enforce required command set and reject executable procedure markers in enforced sections
**Failing test assertion**: Current behavior does not deterministically satisfy this task contract.
**Domains touched**: deterministic orchestration plus module-local behavior for `scripts/validate_markdown_doc_shapes.py`.

## Test Specification

Assert task `T055` deterministically validates: Harden markdown contract validator scope to enforce required command set and reject executable procedure markers in enforced sections.

## Security Review

Pending implement-phase security review; no external secret/material handling expected for this task scope.

## Functional Goal

**Story Goal**: Phase 8: Recovery Delta - Contract Realignment
**Acceptance Criteria**: Harden markdown contract validator scope to enforce required command set and reject executable procedure markers in enforced sections is implemented at `scripts/validate_markdown_doc_shapes.py:validate_markdown_doc_shape` and verified by deterministic PASS/FAIL checks.

## Quality Guards

- Preserve deterministic behavior and explicit failure reasons.
- Prevent side effects outside declared artifact/event boundaries.
- Keep required domain baselines (10, 13, 14, 17) where applicable to this seam.

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded  <- [include only for 3+ point tasks]
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
