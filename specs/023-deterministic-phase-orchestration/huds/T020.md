---
feature_id: "023"
task_id: "T020"
---

# HUD: T020 — Normalize manifest route and emit contracts for `speckit.solution` driver ownership in `./command-manifest.yaml`

## Working Memory

**File:Symbol**: `command-manifest.yaml:commands.speckit.solution`
**Callers**: `scripts/pipeline_driver_contracts.py:load_driver_routes` via `resolve_step_mapping`, `append_pipeline_success_event`, and `validate_command_coverage`
**Reuse path**: Reuse the existing manifest contract shape from `speckit.sketch` and keep the existing `solution_approved` emit contract intact

## Solution Sketch

trivial — no sketch required

## Test Specification

The manifest route should be normalized so the solution phase is recognized as driver-managed:

- `speckit.solution` declares explicit `driver.mode: generative`
- `load_driver_routes()` reports `driver_managed == True`
- the real-manifest route test includes `speckit.solution` in the generative command set
- `solution_approved` stays the emitted event contract and remains tied to the same required fields

## Security Review

N/A — manifest and unit-test change only, no external source

## Functional Goal

**Story Goal**: Command docs emit producer payload contracts only; orchestration/validation/emission remain driver-owned.
**Acceptance Criteria**: Execute a migrated command flow where command docs return completion payload only and verify validation and event emission are performed by orchestration components.

## Quality Guards

- Domain 13: manifest routes must stay deterministic and explicit about driver ownership
- Domain 14: command coverage must remain aligned with the canonical manifest entry
- Domain 17: emitted solution contracts must remain schema-stable for the route loader

## Process Checklist

- [X] discovery_completed
- [X] quality_guards_passed
- [X] functional_goal_achieved
- [X] tests_passed
