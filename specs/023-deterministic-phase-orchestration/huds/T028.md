---
feature_id: "023"
task_id: "T028"
---

# HUD: T028 — Add rollback and no-partial-write regression coverage for FR-013 in tests/unit/test_pipeline_ledger_sequence.py

## Working Memory

**File:Symbol**: `tests/unit/test_pipeline_ledger_sequence.py:test_append_rejects_partial_write_and_preserves_state`
**Callers**: Resolve during implement discovery via codegraph caller query for this symbol.
**Reuse path**: Reuse the existing ledger-sequence test harness and extend it with rollback/no-partial-write assertions.

## Solution Sketch

**Modify**: `tests/unit/test_pipeline_ledger_sequence.py:test_append_rejects_partial_write_and_preserves_state` to assert partial writes do not persist or advance the feature state.
**Create**: none.
**Reuse**: Existing sequence validator and append guard behavior.
**Composition**: simulate a failed append or partial-write condition, then assert the ledger state remains unchanged and the event stream is not advanced.
**Failing test assertion**: append failure still mutates state or advances the feature sequence.
**Domains touched**: storage integrity, governance state machine, testing.

## Test Specification

Assert that append failure or partial-write behavior does not mutate persisted ledger state, does not advance the sequence, and remains deterministic on rerun.

## Security Review

Pending implement-phase security review; no external secret/material handling expected for this task scope.

## Functional Goal

**Story Goal**: Final deterministic regression lock and explicit operator runbook boundary.
**Acceptance Criteria**: FR-013 rollback and no-partial-write behavior are covered by a deterministic regression oracle.

## Quality Guards

- Preserve deterministic ledger mutation semantics.
- Keep rollback and no-partial-write assertions explicit and repeatable.
- Include required baseline guards for domains 13, 14, and 17.

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded  ← [include only for 3+ point tasks]
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
