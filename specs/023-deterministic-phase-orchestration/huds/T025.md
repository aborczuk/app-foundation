---
feature_id: "023"
task_id: "T025"
---

# HUD: T025 — Add duplicate terminal-event retry prevention coverage across driver and ledger seams in `tests/unit/test_pipeline_driver.py`

## Working Memory

**File:Symbol**: `tests/unit/test_pipeline_driver.py:test_idempotent_terminal_event_retry`  
**Callers**: `scripts/pipeline_driver.py:append_pipeline_success_event`  
**Reuse path**: Reuse the existing idempotent append guard and manifest route loader.

## Solution Sketch

**Modify**: `tests/unit/test_pipeline_driver.py:test_idempotent_terminal_event_retry` to assert a duplicate terminal-event append is a no-op.  
**Create**: none.  
**Reuse**: the existing `append_pipeline_success_event` idempotency guard.  
**Composition**: seed the ledger with an already-recorded terminal event, invoke the append helper again, and assert no duplicate line is written.  
**Failing test assertion**: the retry appends a second terminal event or loses the `event_already_recorded` no-op reason.  
**Domains touched**: deterministic routing, audit-trail idempotency, testing.

## Test Specification

Assert that repeated terminal-event appends keep the ledger unchanged and return `appended=False` with `reason=event_already_recorded`.

## Security Review

[Result from security review step — or "N/A — net-new code, no external source"]

## Functional Goal

**Story Goal**: [from tasks.md phase header]  
**Acceptance Criteria**: [Independent Test Criteria for this story — acceptance test in .speckit/acceptance-tests/]

## Quality Guards

- Preserve terminal-event idempotency across driver and ledger seams.
- Keep the retry outcome deterministic and auditable.
- Include required baseline guards for domains 13, 14, and 17.

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded  ← [include only for 3+ point tasks]
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
