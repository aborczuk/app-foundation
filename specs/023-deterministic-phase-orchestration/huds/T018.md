---
feature_id: "023"
task_id: "T018"
---

# HUD: T018 — Add markdown/doc-shape regression for producer-only command contract wording in `tests/unit/test_validate_markdown_doc_shapes.py`

## Working Memory

**File:Symbol**: `tests/unit/test_validate_markdown_doc_shapes.py:test_validate_markdown_doc_shape_accepts_compact_expanded`
**Callers**: `validator.validate_markdown_doc_shape` is exercised directly by the unit test
**Reuse path**: Reuse the existing `validator` fixture loader and the doc-shape helper in the same file

## Solution Sketch

trivial — no sketch required

## Test Specification

The regression should prove the new compact/expanded command-doc shape is accepted by the validator:

- a markdown file containing `User Input`, `Compact Contract (Load First)`, `Expanded Guidance (Load On Demand)`, and `Behavior rules` validates cleanly
- the validator reports `matched_shape == "compact_expanded"`
- the normalized heading list preserves the expected headings in order
- a legacy heading set should still fail the compact/expanded shape check

This task is documentation-shape only; it should not change runtime orchestration behavior.

## Security Review

N/A — unit-test-only regression with no external source

## Functional Goal

**Story Goal**: Command docs emit producer payload contracts only; orchestration/validation/emission remain driver-owned.
**Acceptance Criteria**: Execute a migrated command flow where command docs return completion payload only and verify validation and event emission are performed by orchestration components.

## Quality Guards

- Domain 13: markdown contract shapes must stay deterministic
- Domain 14: doc wording must match the producer-only boundary described by the phase
- Domain 17: the validator should emit a canonical `compact_expanded` match instead of an ambiguous shape

## Process Checklist

- [X] discovery_completed
- [X] quality_guards_passed
- [X] functional_goal_achieved
- [X] tests_passed
