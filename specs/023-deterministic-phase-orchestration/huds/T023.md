---
feature_id: "023"
task_id: "T023"
---

# HUD: T023 — Fail fast on unknown route modes and malformed step payload contracts in `scripts/pipeline_driver_contracts.py`

## Working Memory

**File:Symbol**: `scripts/pipeline_driver_contracts.py:parse_step_result`, `scripts/pipeline_driver_contracts.py:normalize_driver_mode`
**Callers**: `scripts/pipeline_driver.py:resolve_step_mapping`, `scripts/pipeline_driver.py:append_pipeline_success_event`, `scripts/pipeline_driver.py:validate_coverage_for_migration`
**Reuse path**: Reuse the existing route-contract constants and the current step-result envelope shape; tighten validation without changing the overall payload schema

## Solution Sketch

**Modify**: `scripts/pipeline_driver_contracts.py`
**Create**: none
**Reuse**: deterministic route-mode aliases, existing gate/reason constants, and the current envelope validation helpers
**Composition**: fail fast when `ok` disagrees with `exit_code`, keep unknown route modes rejected by `normalize_driver_mode`, and preserve the existing success/blocked/error schema contracts
**Test Specification**:
- unknown route modes raise `ValueError` instead of falling back
- success envelopes require `ok=True`
- blocked/error envelopes require `ok=False`
- valid envelopes still parse with the canonical payload shape
**Domains touched**: contract validation, routing determinism, state/error handling

## Test Specification

The parser should reject malformed step envelopes deterministically:

- `normalize_driver_mode()` should continue to raise on unsupported driver modes
- `parse_step_result()` should reject mismatched `ok` and `exit_code` combinations
- a success envelope with `ok=False` must fail
- blocked and error envelopes with `ok=True` must fail
- the existing success/blocked/error fixtures should continue to pass when the payload is well formed

## Security Review

N/A — contract and validation tightening only

## Functional Goal

**Story Goal**: Command docs emit producer payload contracts only; orchestration/validation/emission remain driver-owned.
**Acceptance Criteria**: Execute a migrated command flow where command docs return completion payload only and verify validation and event emission are performed by orchestration components.

## Quality Guards

- Domain 13: route and step-result contracts must remain deterministic and explicit
- Domain 14: malformed payloads should fail fast instead of falling back to legacy behavior
- Domain 17: error reasons and route-mode aliases must stay schema-stable for downstream tooling

## Process Checklist

- [X] discovery_completed
- [X] lld_recorded
- [X] quality_guards_passed
- [X] functional_goal_achieved
- [X] tests_passed
