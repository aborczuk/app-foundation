---
feature_id: "029"
task_id: "T002"
task_summary: "Create typed gameplay state symbols in `src/clickup_control_plane/tetris/models.py` and piece definitions in `src/clickup_control_plane/tetris/pieces.py` for board geometry, tetromino orientations, score state, and session status required by PL-02."
primary_edit_seam: "src/clickup_control_plane/tetris/models.py"
related_paths: ["src/clickup_control_plane/tetris/models.py", "src/clickup_control_plane/tetris/pieces.py"]
---

# HUD: T002 - Create typed gameplay state symbols in `src/clickup_control_plane/tetris/models.py` and piece definitions in `src/clickup_control_plane/tetris/pieces.py` for board geometry, tetromino orientations, score state, and session status required by PL-02.

## Objective

Deliver `T002` so the task scope satisfies: Create typed gameplay state symbols in `src/clickup_control_plane/tetris/models.py` and piece definitions in `src/clickup_control_plane/tetris/pieces.py` for board geometry, tetromino orientations, score state, and session status required by PL-02..

## Relevant Domains

- Game-State Modeling: this task defines the typed state contracts every gameplay transition will consume.
- Rules Engine Foundations: piece definitions and board geometry must be explicit before movement, lock, or scoring logic can be deterministic.

## Candidate Design Slices

- PL-02 - Authoritative Game-State Engine: create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.

## Proposed Solution

- Create the typed state layer in `models.py` and the tetromino catalog/orientation data in `pieces.py` so later engine logic can operate on explicit immutable or copy-safe contracts.
- Introduce symbols for board geometry, active piece state, score/session status, and piece definitions that `engine.py` and `service.py` will consume without embedding ad-hoc dict shapes.

## Current Repo Behavior

The `src/clickup_control_plane/tetris/` domain modules do not exist yet. Repo reads show no existing gameplay-state model layer or reusable piece-definition seam in the current codebase.

## Target Behavior

- Provide the typed gameplay symbols required by PL-02 before any transition logic is implemented.
- Keep the phase checkpoint true: the Tetris package owns a typed game-state engine surface rather than UI-owned rules.
- Satisfy PL-02 by making board, piece, score, and session status first-class typed Python symbols.
- The primary seam should expose explicit state contracts that later transitions can mutate or return deterministically.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/models.py`

Use `models.py` as the primary task seam because it owns the typed state contracts this task introduces; `pieces.py` remains a tightly coupled secondary seam for canonical tetromino data.

## Reuse Candidates

- None validated from repo reads for gameplay models or piece catalogs; this is a net-new domain seam.

## Required Edits

- Create `src/clickup_control_plane/tetris/models.py` with board/session/game-state symbols.
- Create `src/clickup_control_plane/tetris/pieces.py` with tetromino orientation definitions and spawn metadata.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/models.py` - define typed gameplay-state contracts.
- `src/clickup_control_plane/tetris/pieces.py` - define canonical tetromino orientation data.

### Create

- `GameState`, `SessionStatus`, and piece-definition symbols planned for the feature domain.

### Preserve

- Preserve deterministic, server-owned state contracts with no browser-only rule shapes.

## Tests To Add Or Update

- `tests/unit/test_tetris_engine.py` should eventually assert the model/piece contracts through engine behavior such as spawn shape, geometry bounds, and status transitions.

## Acceptance Criteria

- `models.py` and `pieces.py` define the board, piece, score, and session contracts needed by the engine/service layer.
- Later engine work can consume typed symbols instead of unstructured dict literals.

## Done Criteria

- The typed gameplay-state symbols and tetromino catalog exist and are importable by later engine/service tasks.
- The scaffolded HUD remains validator-clean after the task plan is filled.

## Constraints And Invariants

- Keep gameplay state authoritative in Python, not the browser.
- Model geometry and status explicitly enough for deterministic tests.
- Preserve narrow ownership to `models.py` and `pieces.py` in this task.

## Implementation Checklist

- [ ] Current repo behavior captured: no existing gameplay-model seam exists.
- [ ] Reuse candidate explicitly recorded as none validated from repo reads.
- [ ] Concrete model and piece-definition symbols listed for implementation.

## Dependencies

- T001

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified

