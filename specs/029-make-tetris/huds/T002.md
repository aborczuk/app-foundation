---
feature_id: "029"
task_id: "T002"
---

# HUD: T002 - Wire an isolated Tetris route into the FastAPI app without disturbing existing endpoints in `src/clickup_control_plane/app.py` and `src/clickup_control_plane/tetris/router.py`

## Objective

Deliver `T002` so the declared seam satisfies: Wire an isolated Tetris route into the FastAPI app without disturbing existing endpoints in `src/clickup_control_plane/app.py` and `src/clickup_control_plane/tetris/router.py`.

## Relevant Domains

- `edge delivery` - The repo has no existing browser app, so the feature needs an intentional route/static-asset delivery seam from the existing FastAPI runtime.

## Current Repo Behavior

Verified `src/clickup_control_plane/app.py` exists in the repo worktree. Verified `app` is already referenced in `src/clickup_control_plane/app.py`.

## Target Behavior

- `PL-01` (medium) - Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.
- Task summary: Wire an isolated Tetris route into the FastAPI app without disturbing existing endpoints in `src/clickup_control_plane/app.py` and `src/clickup_control_plane/tetris/router.py`.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/app.py:app`

## Reuse Candidates

- `src/clickup_control_plane/app.py:app` - extend the existing `app` seam in `src/clickup_control_plane/app.py`.
- `src/clickup_control_plane/app.py` - keep the change inside this existing feature seam rather than introducing a parallel path.

## Required Edits

- In `src/clickup_control_plane/app.py:app`, implement the task contract described by `Wire an isolated Tetris route into the FastAPI app without disturbing existing endpoints in `src/clickup_control_plane/app.py` and `src/clickup_control_plane/tetris/router.py`` without widening the seam beyond the declared files.
- In `src/clickup_control_plane/tetris/router.py`, make the supporting seam change needed for `Wire an isolated Tetris route into the FastAPI app without disturbing existing endpoints in `src/clickup_control_plane/app.py` and `src/clickup_control_plane/tetris/router.py`` and keep file ownership aligned with the task contract.
- Honor PL-01 by keeping the implementation aligned with: Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.

## Touched Symbols

### Modify

- `src/clickup_control_plane/app.py:app` - In `src/clickup_control_plane/app.py:app`, implement the task contract described by `Wire an isolated Tetris route into the FastAPI app without disturbing existing endpoints in `src/clickup_control_plane/app.py` and `src/clickup_control_plane/tetris/router.py`` without widening the seam beyond the declared files.
- `src/clickup_control_plane/tetris/router.py` - supporting seam for the same task contract.

### Create

- None.

### Preserve

- Preserve task ownership within the declared files; do not spread restart/game-state logic into unrelated endpoints or assets.
- Preserve deterministic state transitions and story-visible behavior outside the new seam.

## Tests To Add Or Update

### Test 1

**File**: `BLOCKED`  
**Name**: `insufficient_test_context`

Given:
- no test seam was validated from repo reads

When:
- preparing the task HUD

Then assert:
- test ownership must be clarified before implement starts

## Security Review

No secret/material handling is implied, but keep browser/runtime seams limited to the declared route, payload, and session behavior for this task.

## Done Criteria

- Target task contract `T002` is fully implemented in the declared seam without placeholder behavior or undocumented side paths.
- The resulting HUD no longer relies on generic reuse, quality-guard, or discovery placeholders.

## Constraints And Invariants

- Rapid repeated input must not move pieces outside board bounds or through locked blocks.
- Clearing multiple rows at once must update the board and score consistently.
- Game over must trigger only when a new piece cannot spawn legally, not during normal stacking.
- Restart must fully reset the play session and not carry over stale board or score state.

## Implementation Checklist

- [ ] In `src/clickup_control_plane/app.py:app`, implement the task contract described by `Wire an isolated Tetris route into the FastAPI app without disturbing existing endpoints in `src/clickup_control_plane/app.py` and `src/clickup_control_plane/tetris/router.py`` without widening the seam beyond the declared files
- [ ] In `src/clickup_control_plane/tetris/router.py`, make the supporting seam change needed for `Wire an isolated Tetris route into the FastAPI app without disturbing existing endpoints in `src/clickup_control_plane/app.py` and `src/clickup_control_plane/tetris/router.py`` and keep file ownership aligned with the task contract
- [ ] Honor PL-01 by keeping the implementation aligned with: Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints
- [ ] Confirm prerequisite task outputs are present before editing: T001.

## Dependencies

- T001

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
