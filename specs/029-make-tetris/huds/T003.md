---
feature_id: "029"
task_id: "T003"
task_summary: "Implement deterministic state transitions in `src/clickup_control_plane/tetris/engine.py` and `src/clickup_control_plane/tetris/service.py` for spawn, move, rotate, gravity tick, lock, line-clear preparation, score accumulation hooks, and restartable session orchestration from PL-02."
primary_edit_seam: "src/clickup_control_plane/tetris/engine.py"
related_paths: ["src/clickup_control_plane/tetris/engine.py", "src/clickup_control_plane/tetris/service.py"]
---

# HUD: T003 - Implement deterministic state transitions in `src/clickup_control_plane/tetris/engine.py` and `src/clickup_control_plane/tetris/service.py` for spawn, move, rotate, gravity tick, lock, line-clear preparation, score accumulation hooks, and restartable session orchestration from PL-02.

## Objective

Deliver `T003` so the task scope satisfies: Implement deterministic state transitions in `src/clickup_control_plane/tetris/engine.py` and `src/clickup_control_plane/tetris/service.py` for spawn, move, rotate, gravity tick, lock, line-clear preparation, score accumulation hooks, and restartable session orchestration from PL-02..

## Relevant Domains

- Rules Engine: this task owns the deterministic transition logic for movement, rotation, gravity, lock, and session orchestration.
- Service Orchestration: the browser and route layers need one authoritative service seam instead of calling engine internals directly.

## Candidate Design Slices

- PL-02 - Authoritative Game-State Engine: create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.

## Proposed Solution

- Implement the rule transitions in `engine.py` and wrap them in `service.py` so callers interact through a stable session/service API instead of rebuilding rule logic in routes or JavaScript.
- Add spawn, move, rotate, gravity tick, lock preparation, score hooks, and restartable session orchestration as explicit engine/service symbols.

## Current Repo Behavior

There is currently no Tetris engine or service seam in the repo. The only validated runtime seam is `create_app()`, so gameplay transitions are entirely absent today.

## Target Behavior

- Deliver the authoritative transition core every story depends on before UI wiring starts.
- Keep the Phase 2 checkpoint true: the Tetris package owns a typed game-state engine the browser shell can call without embedding rules in UI code.
- Satisfy PL-02 by centralizing spawn, move, rotate, lock, clear-preparation, and restart semantics in deterministic Python functions.
- `engine.py` should become the primary rule-transition seam, with `service.py` exposing session-oriented entrypoints for routes.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/engine.py`

Use `engine.py` as the primary seam because the task's main obligation is deterministic rule transitions; `service.py` wraps those transitions for session callers.

## Reuse Candidates

- Reuse the typed state and piece definitions from `src/clickup_control_plane/tetris/models.py` and `src/clickup_control_plane/tetris/pieces.py` once T002 lands.
- None validated from repo reads for an existing service orchestration seam in this domain.

## Required Edits

- Create `src/clickup_control_plane/tetris/engine.py` with deterministic state-transition helpers.
- Create `src/clickup_control_plane/tetris/service.py` with session-oriented wrappers around engine transitions.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/engine.py` - implement deterministic gameplay transitions.
- `src/clickup_control_plane/tetris/service.py` - expose session orchestration around engine transitions.

### Create

- Planned engine symbols for spawn/move/rotate/tick/lock and service symbols for session lifecycle.

### Preserve

- Preserve server-owned rules and deterministic transitions with no UI-owned gameplay logic.

## Tests To Add Or Update

- `tests/unit/test_tetris_engine.py` should cover movement, rotation, gravity, lock, and restart behavior against the engine/service seams introduced here.

## Acceptance Criteria

- `engine.py` and `service.py` provide deterministic spawn/move/rotate/tick/lock/session orchestration behavior.
- Later route and browser work can drive gameplay solely through the authoritative Python seams.

## Done Criteria

- The authoritative engine/service seam exists and is sufficient for the browser shell to call without embedding rules.
- Rule transitions are decomposed into testable Python symbols rather than route- or JS-local logic.

## Constraints And Invariants

- Keep gameplay correctness in deterministic Python transitions.
- Do not couple route serialization or browser rendering concerns into the engine.
- Preserve clear separation between low-level rules and service/session orchestration.

## Implementation Checklist

- [ ] Current repo behavior captured: gameplay transitions are absent today.
- [ ] Reuse path anchored to T002 state/piece definitions and explicit absence of an existing domain service.
- [ ] Engine and service seam responsibilities listed concretely.

## Dependencies

- T001
- T002

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified

