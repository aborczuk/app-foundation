---
feature_id: "029"
task_id: "T006"
---

# HUD: T006 - Add route/runtime integration coverage for serving the Tetris experience from the FastAPI surface in `tests/integration/test_tetris_route.py`

## Objective

Deliver `T006` so the declared seam satisfies: Add route/runtime integration coverage for serving the Tetris experience from the FastAPI surface in `tests/integration/test_tetris_route.py`.

## Relevant Domains

- `testing` - Gameplay rules, edge collisions, line clears, and restart/game-over transitions need deterministic regression coverage.
- `edge delivery` - The repo has no existing browser app, so the feature needs an intentional route/static-asset delivery seam from the existing FastAPI runtime.

## Current Repo Behavior

BLOCKED: current behavior not validated from repo reads.

## Target Behavior

- Fulfill US1 (Start and Play a Tetris Game (Priority: P1)): A user opens the Tetris feature and begins playing immediately with standard movement and rotation controls.
- Keep the story-level proof true: Open the feature, begin a session, move and rotate pieces, and confirm the board updates correctly until play continues without blocking errors.
- `PL-04` (medium) - Add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state.
- Task summary: Add route/runtime integration coverage for serving the Tetris experience from the FastAPI surface in `tests/integration/test_tetris_route.py`.

## Primary Edit Seam

**File:Symbol**: `tests/integration/test_tetris_route.py:test_tetris_route_serves_playable_shell`

## Reuse Candidates

- `None validated from repo reads.`

## Required Edits

- In `tests/integration/test_tetris_route.py:test_tetris_route_serves_playable_shell`, implement the task contract described by `Add route/runtime integration coverage for serving the Tetris experience from the FastAPI surface in `tests/integration/test_tetris_route.py`` without widening the seam beyond the declared files.
- Honor PL-04 by keeping the implementation aligned with: Add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state.

## Touched Symbols

### Modify

- `tests/integration/test_tetris_route.py:test_tetris_route_serves_playable_shell` - In `tests/integration/test_tetris_route.py:test_tetris_route_serves_playable_shell`, implement the task contract described by `Add route/runtime integration coverage for serving the Tetris experience from the FastAPI surface in `tests/integration/test_tetris_route.py`` without widening the seam beyond the declared files.

### Create

- None.

### Preserve

- Preserve task ownership within the declared files; do not spread restart/game-state logic into unrelated endpoints or assets.
- Preserve deterministic state transitions and story-visible behavior outside the new seam.

## Tests To Add Or Update

### Test 1

**File**: `tests/unit/test_tetris_engine.py`  
**Name**: `test_spawn_and_movement_rules`

Given:
- 1. **Given** the user opens the Tetris feature, **When** the game loads, **Then** a new playable session is shown with an empty board and an active falling piece.
- 3. **Given** active gameplay, **When** time advances, **Then** the current piece continues to fall until it lands or locks.

When:
- execute the seam owned by `T006`

Then assert:
- `T005` passes for the target task contract
- the behavior described by `T006` is deterministic at `tests/unit/test_tetris_engine.py:test_spawn_and_movement_rules`
- the story-level verification remains true: Open the feature, begin a session, move and rotate pieces, and confirm the board updates correctly until play continues without blocking errors.

### Test 2

**File**: `tests/integration/test_tetris_route.py`  
**Name**: `test_tetris_route_serves_playable_shell`

Given:
- 1. **Given** the user opens the Tetris feature, **When** the game loads, **Then** a new playable session is shown with an empty board and an active falling piece.
- 3. **Given** active gameplay, **When** time advances, **Then** the current piece continues to fall until it lands or locks.

When:
- execute the seam owned by `T006`

Then assert:
- `T006` passes for the target task contract
- the behavior described by `T006` is deterministic at `tests/integration/test_tetris_route.py:test_tetris_route_serves_playable_shell`
- the story-level verification remains true: Open the feature, begin a session, move and rotate pieces, and confirm the board updates correctly until play continues without blocking errors.

## Security Review

N/A - doc/test scope only; no auth, secret, or external-system material change is implied.

## Done Criteria

- Target task contract `T006` is fully implemented in the declared seam without placeholder behavior or undocumented side paths.
- Relevant regression remains green at `tests/unit/test_tetris_engine.py` for `test_spawn_and_movement_rules`.
- Relevant regression remains green at `tests/integration/test_tetris_route.py` for `test_tetris_route_serves_playable_shell`.
- The resulting HUD no longer relies on generic reuse, quality-guard, or discovery placeholders.

## Constraints And Invariants

- 1. **Given** the user opens the Tetris feature, **When** the game loads, **Then** a new playable session is shown with an empty board and an active falling piece.
- 2. **Given** a piece is falling, **When** the user presses movement or rotation controls, **Then** the piece responds correctly within board boundaries and collision rules.
- Rapid repeated input must not move pieces outside board bounds or through locked blocks.
- Clearing multiple rows at once must update the board and score consistently.
- Game over must trigger only when a new piece cannot spawn legally, not during normal stacking.

## Implementation Checklist

- [ ] In `tests/integration/test_tetris_route.py:test_tetris_route_serves_playable_shell`, implement the task contract described by `Add route/runtime integration coverage for serving the Tetris experience from the FastAPI surface in `tests/integration/test_tetris_route.py`` without widening the seam beyond the declared files
- [ ] Honor PL-04 by keeping the implementation aligned with: Add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state
- [ ] Keep story-aligned regressions ready: T005, T006.
- [ ] Confirm prerequisite task outputs are present before editing: T002, T003, T004.

## Dependencies

- T002, T003, T004

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
