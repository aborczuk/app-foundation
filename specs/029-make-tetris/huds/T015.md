---
feature_id: "029"
task_id: "T015"
---

# HUD: T015 - Implement restart endpoint/state reset and browser restart affordance in `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/templates/tetris.html`, and `src/clickup_control_plane/tetris/static/tetris.js`

## Objective

Deliver `T015` so the declared seam satisfies: Implement restart endpoint/state reset and browser restart affordance in `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/templates/tetris.html`, and `src/clickup_control_plane/tetris/static/tetris.js`.

## Relevant Domains

- `client/UI` - The feature is an interactive game experience with keyboard controls, visible score, game-over state, and restart flow.
- `edge delivery` - The repo has no existing browser app, so the feature needs an intentional route/static-asset delivery seam from the existing FastAPI runtime.
- `code patterns` - Tetris needs a deterministic game-state engine with explicit transitions for spawn, move, lock, clear, score, and restart.

## Current Repo Behavior

BLOCKED: current behavior not validated from repo reads.

## Target Behavior

- Fulfill US3 (Reach Game Over and Restart (Priority: P3)): A user can recognize a lost game and immediately start a fresh one.
- Keep the story-level proof true: Play until no new piece can spawn, confirm a game-over state appears, and restart into a fresh session.
- `PL-01` (medium) - Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.
- `PL-02` (high) - Create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.
- `PL-03` (high) - Build the thinnest browser shell that can render board state, collect keyboard input, display score/game-over state, and synchronize with the authoritative gameplay flow.
- Task summary: Implement restart endpoint/state reset and browser restart affordance in `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/templates/tetris.html`, and `src/clickup_control_plane/tetris/static/tetris.js`.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/router.py:restart_tetris_session`

## Reuse Candidates

- `None validated from repo reads.`

## Required Edits

- In `src/clickup_control_plane/tetris/router.py:restart_tetris_session`, route restart through the authoritative gameplay service so a fresh session resets board state, active piece state, score, and terminal flags.
- In `src/clickup_control_plane/tetris/templates/tetris.html`, render the UI state and controls this task adds, including any game-over or restart affordances required by the story.
- In `src/clickup_control_plane/tetris/static/tetris.js`, wire the browser event flow so the page calls the declared route/service contract and rerenders deterministic state updates.
- Preserve ended-session immutability until a validated restart path creates the fresh session.
- Honor PL-01 by keeping the implementation aligned with: Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.
- Honor PL-02 by keeping the implementation aligned with: Create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/router.py:restart_tetris_session` - In `src/clickup_control_plane/tetris/router.py:restart_tetris_session`, route restart through the authoritative gameplay service so a fresh session resets board state, active piece state, score, and terminal flags.
- `src/clickup_control_plane/tetris/templates/tetris.html` - supporting seam for the same task contract.
- `src/clickup_control_plane/tetris/static/tetris.js` - supporting seam for the same task contract.

### Create

- None.

### Preserve

- Preserve task ownership within the declared files; do not spread restart/game-state logic into unrelated endpoints or assets.
- Preserve deterministic state transitions and story-visible behavior outside the new seam.

## Tests To Add Or Update

### Test 1

**File**: `tests/unit/test_tetris_engine.py`  
**Name**: `test_game_over_and_restart`

Given:
- 1. **Given** the stack reaches the spawn area, **When** a new piece cannot be placed legally, **Then** the session enters a game-over state.
- 3. **Given** the session is over, **When** the user chooses restart, **Then** a new session begins with reset board state and reset score.

When:
- execute the seam owned by `T015`

Then assert:
- `T012` passes for the target task contract
- the behavior described by `T015` is deterministic at `tests/unit/test_tetris_engine.py:test_game_over_and_restart`
- the story-level verification remains true: Play until no new piece can spawn, confirm a game-over state appears, and restart into a fresh session.

### Test 2

**File**: `tests/integration/test_tetris_route.py`  
**Name**: `test_game_over_and_restart_flow`

Given:
- 1. **Given** the stack reaches the spawn area, **When** a new piece cannot be placed legally, **Then** the session enters a game-over state.
- 3. **Given** the session is over, **When** the user chooses restart, **Then** a new session begins with reset board state and reset score.

When:
- execute the seam owned by `T015`

Then assert:
- `T013` passes for the target task contract
- the behavior described by `T015` is deterministic at `tests/integration/test_tetris_route.py:test_game_over_and_restart_flow`
- the story-level verification remains true: Play until no new piece can spawn, confirm a game-over state appears, and restart into a fresh session.

## Security Review

No secret/material handling is implied, but keep browser/runtime seams limited to the declared route, payload, and session behavior for this task.

## Done Criteria

- Target task contract `T015` is fully implemented in the declared seam without placeholder behavior or undocumented side paths.
- Relevant regression remains green at `tests/unit/test_tetris_engine.py` for `test_game_over_and_restart`.
- Relevant regression remains green at `tests/integration/test_tetris_route.py` for `test_game_over_and_restart_flow`.
- The resulting HUD no longer relies on generic reuse, quality-guard, or discovery placeholders.

## Constraints And Invariants

- 1. **Given** the stack reaches the spawn area, **When** a new piece cannot be placed legally, **Then** the session enters a game-over state.
- 2. **Given** the session is over, **When** the user views the end state, **Then** the score remains visible and active controls no longer mutate the ended board.
- Rapid repeated input must not move pieces outside board bounds or through locked blocks.
- Clearing multiple rows at once must update the board and score consistently.
- Game over must trigger only when a new piece cannot spawn legally, not during normal stacking.

## Implementation Checklist

- [ ] In `src/clickup_control_plane/tetris/router.py:restart_tetris_session`, route restart through the authoritative gameplay service so a fresh session resets board state, active piece state, score, and terminal flags
- [ ] In `src/clickup_control_plane/tetris/templates/tetris.html`, render the UI state and controls this task adds, including any game-over or restart affordances required by the story
- [ ] In `src/clickup_control_plane/tetris/static/tetris.js`, wire the browser event flow so the page calls the declared route/service contract and rerenders deterministic state updates
- [ ] Keep story-aligned regressions ready: T012, T013.
- [ ] Confirm prerequisite task outputs are present before editing: T010, T011.

## Dependencies

- T010, T011

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
