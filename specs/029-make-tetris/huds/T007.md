---
feature_id: "029"
task_id: "T007"
task_summary: "Complete line-clear and scoring behavior in `src/clickup_control_plane/tetris/engine.py` and `src/clickup_control_plane/tetris/service.py` so locked pieces trigger row detection, collapse logic, score updates, and next-piece continuation consistent with PL-02."
story_id: "US2"
primary_edit_seam: "src/clickup_control_plane/tetris/engine.py"
related_paths: ["src/clickup_control_plane/tetris/engine.py", "src/clickup_control_plane/tetris/service.py"]
---

# HUD: T007 - Complete line-clear and scoring behavior in `src/clickup_control_plane/tetris/engine.py` and `src/clickup_control_plane/tetris/service.py` so locked pieces trigger row detection, collapse logic, score updates, and next-piece continuation consistent with PL-02.

## Objective

Deliver `T007` so the task scope satisfies: Complete line-clear and scoring behavior in `src/clickup_control_plane/tetris/engine.py` and `src/clickup_control_plane/tetris/service.py` so locked pieces trigger row detection, collapse logic, score updates, and next-piece continuation consistent with PL-02..

## Relevant Domains

- Rules Engine: this task extends the transition core with row detection, collapse logic, score updates, and next-piece continuation.
- Session Continuity: scoring and clear behavior must compose with the same authoritative session flow introduced earlier.

## Candidate Design Slices

- PL-02 - Authoritative Game-State Engine: create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.

## Proposed Solution

- Extend the engine and service seams so locked pieces trigger deterministic row detection, collapse, score updates, and next-piece continuation without resetting the session.
- Keep `engine.py` as the primary rules seam and use `service.py` only to orchestrate session-visible results.

## Current Repo Behavior

Before this task, the repo has no validated line-clear or score behavior because the Tetris engine/service domain is net-new. Current repo reads only establish that no gameplay logic exists today.

## Target Behavior

- Meet the US2 independent test: completed rows disappear, the board collapses correctly, and the score rises without resetting the session.
- Satisfy PL-02 by keeping clear/score behavior in deterministic Python transitions.
- Preserve continuation into the next piece within the same authoritative session.
- `engine.py` should own row detection/collapse/score transitions, with `service.py` exposing the updated session state.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/engine.py`

Use `engine.py` as the primary seam because row detection, collapse, and scoring are rule transitions first; `service.py` surfaces the resulting authoritative session state.

## Reuse Candidates

- Reuse the spawn/move/rotate/lock/session seams introduced in `engine.py` and `service.py` by T003.
- Reuse typed state contracts from `models.py` for score and board updates.

## Required Edits

- Extend `src/clickup_control_plane/tetris/engine.py` with row detection, collapse, score, and next-piece continuation logic.
- Extend `src/clickup_control_plane/tetris/service.py` so session-facing responses expose the updated board and score state.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/engine.py` - add line-clear and score transitions.
- `src/clickup_control_plane/tetris/service.py` - expose cleared-board and updated-score session behavior.

### Create

- Planned clear/score helper symbols and service-facing response helpers as needed.

### Preserve

- Preserve deterministic continuation of the same session after a clear.

## Tests To Add Or Update

- `tests/unit/test_tetris_engine.py` should cover single-line and multi-line clears, score updates, and next-piece continuation for the authoritative engine/service seams.

## Acceptance Criteria

- Locked pieces trigger deterministic row detection, collapse behavior, score updates, and continuation into the next piece.
- Clearing rows does not reset or corrupt the current authoritative session.

## Done Criteria

- The rule engine supports clear/score behavior needed for US2.
- Session-facing responses can surface updated board and score state after clears.

## Constraints And Invariants

- Keep scoring and clear logic authoritative in Python.
- Preserve deterministic board collapse behavior for tests.
- Do not move score bookkeeping into the browser or route layer.

## Implementation Checklist

- [ ] Current repo behavior recorded as absent gameplay logic prior to the net-new engine domain.
- [ ] Reuse path through T003 engine/service and T002 model seams identified.
- [ ] Clear/score transition work listed concretely by file and responsibility.

## Dependencies

- T001
- T002
- T003
- T004
- T005

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified
