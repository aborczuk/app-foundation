---
feature_id: "029"
task_id: "T007"
task_summary: "Add deterministic unit coverage in `tests/unit/test_tetris_engine.py` for boundary movement, blocked rotation, single/multi-line clears, score updates, and next-piece continuation required by PL-04."
story_id: "US2"
primary_edit_seam: "tests/unit/test_tetris_engine.py"
related_paths: ["tests/unit/test_tetris_engine.py"]
---

# HUD: T007 - Add deterministic unit coverage in `tests/unit/test_tetris_engine.py` for boundary movement, blocked rotation, single/multi-line clears, score updates, and next-piece continuation required by PL-04.

## Objective

Deliver `T007` so the task scope satisfies: Add deterministic unit coverage in `tests/unit/test_tetris_engine.py` for boundary movement, blocked rotation, single/multi-line clears, score updates, and next-piece continuation required by PL-04..

## Relevant Domains

- Deterministic Verification: this task turns the rule-heavy engine into a regression-checked domain.
- Gameplay Correctness: the unit suite must pin the exact movement, rotation, clear, and score invariants the engine promises.

## Candidate Design Slices

- PL-04 - Deterministic Verification Gates: add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state.

## Proposed Solution

- Add focused unit coverage around the engine/service seams so movement boundaries, blocked rotation, clears, score updates, and continuation behavior are pinned independently of the browser shell.
- Use one dedicated `tests/unit/test_tetris_engine.py` file as the authoritative regression seam for gameplay rules.

## Current Repo Behavior

The repo currently has no Tetris unit suite and no existing gameplay-rule tests to extend. Repo reads found no `TestClient` or browser-route tests, so deterministic rule coverage must start at the engine seam.

## Target Behavior

- Satisfy the PL-04 requirement for deterministic gameplay-rule tests.
- Prove movement boundaries, blocked rotations, line clears, score changes, and next-piece continuation independently of the UI.
- Preserve the engine/service layer as the unit-test focus instead of route/browser code.
- `tests/unit/test_tetris_engine.py` should become the primary verification seam for authoritative gameplay rules.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_tetris_engine.py`

Use the dedicated unit test file as the primary seam because this task's scope is deterministic verification of the authoritative engine/service behavior.

## Reuse Candidates

- Reuse the engine/service/model seams introduced by T002, T003, and T006.
- None validated from repo reads for an existing gameplay unit-test file.

## Required Edits

- Create `tests/unit/test_tetris_engine.py` with deterministic rule-transition tests covering boundary movement, blocked rotation, line clears, score updates, and continuation behavior.

## Touched Symbols

### Modify

- `tests/unit/test_tetris_engine.py` - add deterministic gameplay-rule tests.

### Create

- Planned test functions for movement boundaries, blocked rotation, line clears, scoring, and continuation cases.

### Preserve

- Preserve direct verification of authoritative engine/service behavior rather than UI side effects.

## Tests To Add Or Update

- `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_tetris_engine.py`

## Acceptance Criteria

- The unit suite deterministically covers movement bounds, blocked rotation, clear behavior, score updates, and next-piece continuation.
- The tests exercise authoritative Python seams rather than browser behavior.

## Done Criteria

- `tests/unit/test_tetris_engine.py` exists and provides deterministic regression coverage for the rule-heavy engine.
- The unit verification seam is sufficient to catch gameplay-rule regressions before route/browser tests run.

## Constraints And Invariants

- Keep tests deterministic and engine-focused.
- Avoid route/browser coupling in this unit suite.
- Preserve direct traceability to PL-04 and the US2 rule scenarios.

## Implementation Checklist

- [ ] Current repo behavior recorded: no gameplay unit suite exists.
- [ ] Reuse path through engine/service/model seams called out explicitly.
- [ ] Concrete unit-test coverage scenarios listed by rule category.

## Dependencies

- T002
- T003
- T006

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified

