---
feature_id: "029"
task_id: "T007"
---

# HUD: T007 - Implement session bootstrap and per-tick state progression handlers in `src/clickup_control_plane/tetris/service.py` and `src/clickup_control_plane/tetris/router.py`

## Objective

Deliver `T007` so the declared seam satisfies: Implement session bootstrap and per-tick state progression handlers in `src/clickup_control_plane/tetris/service.py` and `src/clickup_control_plane/tetris/router.py`.

## Relevant Domains

- `edge delivery` - The repo has no existing browser app, so the feature needs an intentional route/static-asset delivery seam from the existing FastAPI runtime.
- `code patterns` - Tetris needs a deterministic game-state engine with explicit transitions for spawn, move, lock, clear, score, and restart.

## Current Repo Behavior

BLOCKED: current behavior not validated from repo reads.

## Target Behavior

- Fulfill US1 (Start and Play a Tetris Game (Priority: P1)): A user opens the Tetris feature and begins playing immediately with standard movement and rotation controls.
- Keep the story-level proof true: Open the feature, begin a session, move and rotate pieces, and confirm the board updates correctly until play continues without blocking errors.
- `PL-01` (medium) - Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.
- `PL-02` (high) - Create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.
- Task summary: Implement session bootstrap and per-tick state progression handlers in `src/clickup_control_plane/tetris/service.py` and `src/clickup_control_plane/tetris/router.py`.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/router.py:create_tetris_session`

## Reuse Candidates

- `None validated from repo reads.`

## Required Edits

- In `src/clickup_control_plane/tetris/router.py:create_tetris_session`, implement the task contract described by `Implement session bootstrap and per-tick state progression handlers in `src/clickup_control_plane/tetris/service.py` and `src/clickup_control_plane/tetris/router.py`` without widening the seam beyond the declared files.
- In `src/clickup_control_plane/tetris/service.py`, make the supporting seam change needed for `Implement session bootstrap and per-tick state progression handlers in `src/clickup_control_plane/tetris/service.py` and `src/clickup_control_plane/tetris/router.py`` and keep file ownership aligned with the task contract.
- Honor PL-01 by keeping the implementation aligned with: Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.
- Honor PL-02 by keeping the implementation aligned with: Create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/router.py:create_tetris_session` - In `src/clickup_control_plane/tetris/router.py:create_tetris_session`, implement the task contract described by `Implement session bootstrap and per-tick state progression handlers in `src/clickup_control_plane/tetris/service.py` and `src/clickup_control_plane/tetris/router.py`` without widening the seam beyond the declared files.
- `src/clickup_control_plane/tetris/service.py` - supporting seam for the same task contract.

### Create

- None.

### Preserve

- Preserve task ownership within the declared files; do not spread restart/game-state logic into unrelated endpoints or assets.
- Preserve deterministic state transitions and story-visible behavior outside the new seam.

## Tests To Add Or Update

### Test 1

**File**: `tests/unit/test_tetris_engine.py`  
**Name**: `test_spawn_and_movement_rules`

Given:
- 1. **Given** the user opens the Tetris feature, **When** the game loads, **Then** a new playable session is shown with an empty board and an active falling piece.
- 3. **Given** active gameplay, **When** time advances, **Then** the current piece continues to fall until it lands or locks.

When:
- execute the seam owned by `T007`

Then assert:
- `T005` passes for the target task contract
- the behavior described by `T007` is deterministic at `tests/unit/test_tetris_engine.py:test_spawn_and_movement_rules`
- the story-level verification remains true: Open the feature, begin a session, move and rotate pieces, and confirm the board updates correctly until play continues without blocking errors.

### Test 2

**File**: `tests/integration/test_tetris_route.py`  
**Name**: `test_tetris_route_serves_playable_shell`

Given:
- 1. **Given** the user opens the Tetris feature, **When** the game loads, **Then** a new playable session is shown with an empty board and an active falling piece.
- 3. **Given** active gameplay, **When** time advances, **Then** the current piece continues to fall until it lands or locks.

When:
- execute the seam owned by `T007`

Then assert:
- `T006` passes for the target task contract
- the behavior described by `T007` is deterministic at `tests/integration/test_tetris_route.py:test_tetris_route_serves_playable_shell`
- the story-level verification remains true: Open the feature, begin a session, move and rotate pieces, and confirm the board updates correctly until play continues without blocking errors.

## Security Review

No secret/material handling is implied for this seam; keep behavior deterministic and feature-local.

## Done Criteria

- Target task contract `T007` is fully implemented in the declared seam without placeholder behavior or undocumented side paths.
- Relevant regression remains green at `tests/unit/test_tetris_engine.py` for `test_spawn_and_movement_rules`.
- Relevant regression remains green at `tests/integration/test_tetris_route.py` for `test_tetris_route_serves_playable_shell`.
- The resulting HUD no longer relies on generic reuse, quality-guard, or discovery placeholders.

## Constraints And Invariants

- 1. **Given** the user opens the Tetris feature, **When** the game loads, **Then** a new playable session is shown with an empty board and an active falling piece.
- 2. **Given** a piece is falling, **When** the user presses movement or rotation controls, **Then** the piece responds correctly within board boundaries and collision rules.
- Rapid repeated input must not move pieces outside board bounds or through locked blocks.
- Clearing multiple rows at once must update the board and score consistently.
- Game over must trigger only when a new piece cannot spawn legally, not during normal stacking.

## Implementation Checklist

- [ ] In `src/clickup_control_plane/tetris/router.py:create_tetris_session`, implement the task contract described by `Implement session bootstrap and per-tick state progression handlers in `src/clickup_control_plane/tetris/service.py` and `src/clickup_control_plane/tetris/router.py`` without widening the seam beyond the declared files
- [ ] In `src/clickup_control_plane/tetris/service.py`, make the supporting seam change needed for `Implement session bootstrap and per-tick state progression handlers in `src/clickup_control_plane/tetris/service.py` and `src/clickup_control_plane/tetris/router.py`` and keep file ownership aligned with the task contract
- [ ] Honor PL-01 by keeping the implementation aligned with: Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints
- [ ] Keep story-aligned regressions ready: T005, T006.
- [ ] Confirm prerequisite task outputs are present before editing: T002, T003, T004.

## Dependencies

- T002, T003, T004

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
