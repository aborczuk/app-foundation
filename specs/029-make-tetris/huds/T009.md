---
feature_id: "029"
task_id: "T009"
---

# HUD: T009 - Add unit tests for single-line, multi-line, and score-update transitions in `tests/unit/test_tetris_engine.py`

## Objective

Deliver `T009` so the declared seam satisfies: Add unit tests for single-line, multi-line, and score-update transitions in `tests/unit/test_tetris_engine.py`.

## Relevant Domains

- `testing` - Gameplay rules, edge collisions, line clears, and restart/game-over transitions need deterministic regression coverage.
- `code patterns` - Tetris needs a deterministic game-state engine with explicit transitions for spawn, move, lock, clear, score, and restart.

## Current Repo Behavior

BLOCKED: current behavior not validated from repo reads.

## Target Behavior

- Fulfill US2 (Clear Lines and Track Score (Priority: P2)): A user clears completed rows and sees the score update during the same session.
- Keep the story-level proof true: Play until a row is completed, confirm the row is removed, confirm the board collapses correctly, and verify the score increases.
- `PL-04` (medium) - Add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state.
- Task summary: Add unit tests for single-line, multi-line, and score-update transitions in `tests/unit/test_tetris_engine.py`.

## Primary Edit Seam

**File:Symbol**: `tests/unit/test_tetris_engine.py:test_line_clear_and_score_updates`

## Reuse Candidates

- `None validated from repo reads.`

## Required Edits

- In `tests/unit/test_tetris_engine.py:test_line_clear_and_score_updates`, implement the task contract described by `Add unit tests for single-line, multi-line, and score-update transitions in `tests/unit/test_tetris_engine.py`` without widening the seam beyond the declared files.
- Honor PL-04 by keeping the implementation aligned with: Add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state.

## Touched Symbols

### Modify

- `tests/unit/test_tetris_engine.py:test_line_clear_and_score_updates` - In `tests/unit/test_tetris_engine.py:test_line_clear_and_score_updates`, implement the task contract described by `Add unit tests for single-line, multi-line, and score-update transitions in `tests/unit/test_tetris_engine.py`` without widening the seam beyond the declared files.

### Create

- None.

### Preserve

- Preserve task ownership within the declared files; do not spread restart/game-state logic into unrelated endpoints or assets.
- Preserve deterministic state transitions and story-visible behavior outside the new seam.

## Tests To Add Or Update

### Test 1

**File**: `tests/unit/test_tetris_engine.py`  
**Name**: `test_line_clear_and_score_updates`

Given:
- 1. **Given** a row becomes fully occupied, **When** the active piece locks, **Then** the completed row is cleared before normal play resumes.
- 3. **Given** the session continues after a clear, **When** the next piece enters play, **Then** the board state reflects the cleared rows accurately.

When:
- execute the seam owned by `T009`

Then assert:
- `T009` passes for the target task contract
- the behavior described by `T009` is deterministic at `tests/unit/test_tetris_engine.py:test_line_clear_and_score_updates`
- the story-level verification remains true: Play until a row is completed, confirm the row is removed, confirm the board collapses correctly, and verify the score increases.

## Security Review

N/A - doc/test scope only; no auth, secret, or external-system material change is implied.

## Done Criteria

- Target task contract `T009` is fully implemented in the declared seam without placeholder behavior or undocumented side paths.
- Relevant regression remains green at `tests/unit/test_tetris_engine.py` for `test_line_clear_and_score_updates`.
- The resulting HUD no longer relies on generic reuse, quality-guard, or discovery placeholders.

## Constraints And Invariants

- 1. **Given** a row becomes fully occupied, **When** the active piece locks, **Then** the completed row is cleared before normal play resumes.
- 2. **Given** one or more rows are cleared, **When** scoring is recalculated, **Then** the player sees an updated score in the game UI.
- Rapid repeated input must not move pieces outside board bounds or through locked blocks.
- Clearing multiple rows at once must update the board and score consistently.
- Game over must trigger only when a new piece cannot spawn legally, not during normal stacking.

## Implementation Checklist

- [ ] In `tests/unit/test_tetris_engine.py:test_line_clear_and_score_updates`, implement the task contract described by `Add unit tests for single-line, multi-line, and score-update transitions in `tests/unit/test_tetris_engine.py`` without widening the seam beyond the declared files
- [ ] Honor PL-04 by keeping the implementation aligned with: Add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state
- [ ] Keep story-aligned regressions ready: T009.
- [ ] Confirm prerequisite task outputs are present before editing: T007, T008.

## Dependencies

- T007, T008

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
