---
feature_id: "029"
task_id: "T009"
task_summary: "Add end-to-end runtime verification in `tests/integration/test_tetris_routes.py` for opening the Tetris surface, progressing session state, reaching game over, and restarting through the FastAPI route/runtime seam defined by PL-04."
story_id: "US3"
primary_edit_seam: "tests/integration/test_tetris_routes.py"
related_paths: ["tests/integration/test_tetris_routes.py"]
---

# HUD: T009 - Add end-to-end runtime verification in `tests/integration/test_tetris_routes.py` for opening the Tetris surface, progressing session state, reaching game over, and restarting through the FastAPI route/runtime seam defined by PL-04.

## Objective

Deliver `T009` so the task scope satisfies: Add end-to-end runtime verification in `tests/integration/test_tetris_routes.py` for opening the Tetris surface, progressing session state, reaching game over, and restarting through the FastAPI route/runtime seam defined by PL-04..

## Relevant Domains

- Runtime Verification: this task proves the browser-facing FastAPI seam can actually start, progress, end, and restart a session.
- Integration Testing: the feature needs at least one live route/runtime verification seam because the state machine spans routes, service, and engine behavior.

## Candidate Design Slices

- PL-04 - Deterministic Verification Gates: add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state.

## Proposed Solution

- Add one dedicated route/runtime integration file that exercises the Tetris FastAPI surface end to end: open the page, progress session state, force game over, and restart cleanly.
- Use `tests/integration/test_tetris_routes.py` as the primary seam and drive the real `create_app()` + route/service/engine stack rather than mocking the runtime path away.

## Current Repo Behavior

Repo reads show no existing `TestClient` usage, no current browser-route integration seam, and no existing `tests/integration/test_tetris_routes.py` file. `create_app()` currently exposes only inline control-plane endpoints, so the Tetris runtime path does not exist yet.

## Target Behavior

- Satisfy the US3 independent test through the real FastAPI route/runtime seam.
- Satisfy PL-04 by proving the Tetris surface can start, progress, reach game over, and restart without corrupting session state.
- Keep this as a true integration seam over the authoritative runtime path, not a pure mock-contract test.
- `tests/integration/test_tetris_routes.py` should verify the real app-factory and route/service/engine composition.

## Primary Edit Seam

**File:Symbol**: `tests/integration/test_tetris_routes.py`

Use the integration test file as the primary seam because this task's obligation is end-to-end verification of the real app/route/service/engine runtime path.

## Reuse Candidates

- Reuse `src/clickup_control_plane/app.py:create_app` as the real runtime entrypoint under test.
- Reuse the session route surface from `src/clickup_control_plane/tetris/routes.py`.
- None validated from repo reads for an existing FastAPI `TestClient` harness in this repo.

## Required Edits

- Create `tests/integration/test_tetris_routes.py` with end-to-end route/runtime tests for open, progress, game over, and restart behavior.

## Touched Symbols

### Modify

- `tests/integration/test_tetris_routes.py` - add runtime integration tests for the Tetris surface.

### Create

- Planned integration test functions covering page open, session progression, game-over transition, and restart flow.

### Preserve

- Preserve real runtime coverage over the authoritative app/route/service/engine path.

## Tests To Add Or Update

- `uv run --no-sync python scripts/pytest_guard.py run -- tests/integration/test_tetris_routes.py`

## Acceptance Criteria

- The integration suite proves the Tetris surface can start, progress, reach game over, and restart through the real FastAPI runtime seam.
- The tests avoid collapsing the runtime path into pure mocks for the critical behavior under test.

## Done Criteria

- `tests/integration/test_tetris_routes.py` exists and covers the complete runtime lifecycle required by PL-04.
- The real app-factory route path is exercised end to end for this feature.

## Constraints And Invariants

- Keep at least one real runtime verification seam for this infrastructure-critical browser path.
- Do not reduce the critical route/runtime coverage to pure mocked contracts.
- Preserve traceability to US3 and PL-04.

## Implementation Checklist

- [ ] Current repo behavior recorded: no TestClient-based route/runtime seam exists today.
- [ ] Reuse path through `create_app()` and Tetris routes called out explicitly.
- [ ] Concrete integration scenarios listed for open, progress, game over, and restart.

## Dependencies

- T001
- T003
- T004
- T005
- T006
- T008

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified

