---
feature_id: "029"
task_id: "T008"
task_summary: "Finalize terminal-state and restart handling in `src/clickup_control_plane/tetris/engine.py`, `src/clickup_control_plane/tetris/service.py`, and `src/clickup_control_plane/tetris/routes.py` so illegal spawn transitions mark the session game over, post-game commands are inert, and restart creates a fresh authoritative session."
story_id: "US3"
primary_edit_seam: "src/clickup_control_plane/tetris/engine.py"
related_paths: ["src/clickup_control_plane/tetris/engine.py", "src/clickup_control_plane/tetris/service.py", "src/clickup_control_plane/tetris/routes.py"]
---

# HUD: T008 - Finalize terminal-state and restart handling in `src/clickup_control_plane/tetris/engine.py`, `src/clickup_control_plane/tetris/service.py`, and `src/clickup_control_plane/tetris/routes.py` so illegal spawn transitions mark the session game over, post-game commands are inert, and restart creates a fresh authoritative session.

## Objective

Deliver `T008` so the task scope satisfies: Finalize terminal-state and restart handling in `src/clickup_control_plane/tetris/engine.py`, `src/clickup_control_plane/tetris/service.py`, and `src/clickup_control_plane/tetris/routes.py` so illegal spawn transitions mark the session game over, post-game commands are inert, and restart creates a fresh authoritative session..

## Relevant Domains

- Rules Engine: this task closes the session lifecycle with terminal-state detection and restart semantics.
- Runtime Contracting: routes and browser code need a stable guarantee that ended sessions become inert until restart.

## Candidate Design Slices

- PL-02 - Authoritative Game-State Engine: create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.
- PL-03 - Playable Browser Shell: the shell must surface game-over and restart UX while remaining server-backed.

## Proposed Solution

- Finalize engine, service, and route behavior so illegal spawn transitions mark the session game over, post-game commands stop mutating state, and restart provisions a fresh authoritative session.
- Keep `engine.py` as the primary lifecycle-rules seam, with `service.py` and `routes.py` exposing inert-command and restart behavior to callers.

## Current Repo Behavior

The repo currently has no Tetris terminal-state or restart behavior because the feature domain is net-new. Existing repo reads validate only the absence of gameplay/session seams today.

## Target Behavior

- Meet the US3 independent test: a full board yields game over, ended sessions become inert, and restart creates a clean board with zeroed score.
- Satisfy PL-02 and PL-03 by keeping lifecycle transitions authoritative in Python while exposing restart UX through the runtime seam.
- Preserve the complete session loop on the server.
- `engine.py` should own illegal-spawn/game-over detection, while `service.py` and `routes.py` expose inert command handling and restart entrypoints.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/engine.py`

Use `engine.py` as the primary seam because illegal-spawn, game-over, and restart semantics are authoritative lifecycle rules; `service.py` and `routes.py` project those rules outward.

## Reuse Candidates

- Reuse the engine/service/session seams already introduced for spawn, tick, clear, and scoring.
- Reuse the route command surface from `src/clickup_control_plane/tetris/routes.py` for restart exposure.

## Required Edits

- Extend `src/clickup_control_plane/tetris/engine.py` with illegal-spawn and terminal-state transitions.
- Extend `src/clickup_control_plane/tetris/service.py` so post-game commands are inert and restart creates a fresh session.
- Extend `src/clickup_control_plane/tetris/routes.py` to expose restart behavior through the runtime seam.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/engine.py` - add game-over detection and restart transitions.
- `src/clickup_control_plane/tetris/service.py` - enforce inert post-game commands and restart provisioning.
- `src/clickup_control_plane/tetris/routes.py` - expose restart behavior through the route surface.

### Create

- Planned lifecycle helper symbols for terminal-state detection and restart handling.

### Preserve

- Preserve authoritative session ownership and inert ended-session behavior.

## Tests To Add Or Update

- `tests/unit/test_tetris_engine.py` should add terminal-state and restart cases.
- `tests/integration/test_tetris_routes.py` should verify game-over and restart through the FastAPI runtime seam.

## Acceptance Criteria

- Illegal spawn transitions mark the session game over, post-game commands stop mutating the ended session, and restart creates a fresh zeroed run.
- Restart behavior flows through the authoritative service/route seams rather than browser-local resets.

## Done Criteria

- The full play loop closes with stable game-over and restart behavior.
- Ended sessions are inert until the explicit restart path provisions a fresh session.

## Constraints And Invariants

- Keep terminal-state and restart behavior authoritative in Python.
- Preserve inert ended-session semantics for all post-game commands.
- Do not let browser code fabricate a new session locally.

## Implementation Checklist

- [ ] Current repo behavior recorded: no terminal-state or restart seams exist yet.
- [ ] Reuse path through existing engine/service/route session seams identified.
- [ ] Terminal-state, inert-command, and restart responsibilities listed concretely.

## Dependencies

- T001
- T002
- T003
- T004
- T005
- T006
- T007

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified

