---
feature_id: "029"
task_id: "T010"
task_summary: "Update `specs/029-make-tetris/tasks.md`, generated HUDs under `specs/029-make-tetris/huds/`, and any touched docstrings in `src/clickup_control_plane/tetris/*.py` so the solution artifact set preserves slice-to-task traceability, explicit constraints, and implement-ready acceptance criteria."
primary_edit_seam: "specs/029-make-tetris/huds/"
related_paths: ["specs/029-make-tetris/tasks.md", "specs/029-make-tetris/huds/", "src/clickup_control_plane/tetris/*.py"]
---

# HUD: T010 - Update `specs/029-make-tetris/tasks.md`, generated HUDs under `specs/029-make-tetris/huds/`, and any touched docstrings in `src/clickup_control_plane/tetris/*.py` so the solution artifact set preserves slice-to-task traceability, explicit constraints, and implement-ready acceptance criteria.

## Objective

Deliver `T010` so the task scope satisfies: Update `specs/029-make-tetris/tasks.md`, generated HUDs under `specs/029-make-tetris/huds/`, and any touched docstrings in `src/clickup_control_plane/tetris/*.py` so the solution artifact set preserves slice-to-task traceability, explicit constraints, and implement-ready acceptance criteria..

## Relevant Domains

- Artifact Hygiene: this task keeps the solution artifacts implementable and traceable after the technical tasks settle.
- Documentation/Traceability: the feature needs explicit slice-to-task mapping and up-to-date docstrings in the touched Tetris modules.

## Candidate Design Slices

- PL-04 - Deterministic Verification Gates: preserve plan-to-task trace for start/play/clear/lose/restart scenarios.
- PL-01 through PL-03 traceability: the artifact set must keep the runtime, engine, and browser shell seams explicit.

## Proposed Solution

- Update the solution artifacts and touched Tetris docstrings so tasks, HUDs, and implementation-facing docs remain aligned with the final seam decisions.
- Keep the generated HUD scaffold shape intact while filling each task with concrete slice, seam, constraint, and verification guidance.

## Current Repo Behavior

`specs/029-make-tetris/tasks.md` and the generated HUD scaffolds exist, but the HUDs were regenerated back to scaffold-only form. The Tetris implementation modules do not exist yet, so any touched docstrings will be net-new once those files land.

## Target Behavior

- Preserve slice-to-task traceability, explicit constraints, and implement-ready acceptance criteria across the solution artifact set.
- Keep the generated HUDs completed in place instead of replacing their scaffold framing.
- Preserve explicit docstrings on any touched `src/clickup_control_plane/tetris/*.py` functions or modules as implementation evolves.
- `specs/029-make-tetris/huds/` should be the primary seam for artifact completion and trace hygiene.

## Primary Edit Seam

**File:Symbol**: `specs/029-make-tetris/huds/`

Use the HUD directory as the primary seam because this task is centered on completing and preserving the generated solution artifacts in place, with docstring follow-through in touched Tetris modules.

## Reuse Candidates

- Reuse `scripts/speckit_remake_huds.py` for scaffold generation and `scripts/speckit_fill_huds.py` for in-place completion.
- Reuse the finalized `tasks.md`, `plan.md`, and `estimates.md` artifacts as the content source of truth.

## Required Edits

- Update `specs/029-make-tetris/tasks.md` only if final trace adjustments are required.
- Complete the generated HUDs under `specs/029-make-tetris/huds/` in place.
- Update touched docstrings in `src/clickup_control_plane/tetris/*.py` as implementation symbols land.

## Touched Symbols

### Modify

- `specs/029-make-tetris/tasks.md` - preserve final task/trace alignment if implementation changes require it.
- `specs/029-make-tetris/huds/` - complete each generated HUD with concrete solution guidance.
- `src/clickup_control_plane/tetris/*.py` - keep module/function docstrings aligned with behavior changes.

### Create

- None.

### Preserve

- Preserve deterministic scaffold framing and feature traceability across artifacts.

## Tests To Add Or Update

- `uv run python scripts/speckit_remake_huds.py validate --feature-dir specs/029-make-tetris --json`
- `uv run python scripts/speckit_tasks_gate.py validate-format --tasks-file specs/029-make-tetris/tasks.md --json`
- `uv run --no-sync python scripts/speckit_tasking_chain.py --feature-dir specs/029-make-tetris --json`

## Acceptance Criteria

- The solution artifacts preserve slice-to-task traceability, explicit constraints, and implement-ready acceptance criteria.
- The HUD set remains generator-valid while being completed in place, and touched Tetris docstrings stay current.

## Done Criteria

- HUD validation, task-format validation, and tasking-chain validation all pass for feature `029`.
- The artifact set is ready for implementation without unresolved scaffold placeholders.

## Constraints And Invariants

- Preserve the generated HUD scaffold framing and fill it in place.
- Keep docstrings current for any new or changed Tetris functions.
- Preserve end-to-end traceability from plan slices to tasks and HUDs.

## Implementation Checklist

- [ ] Current artifact state captured: HUDs are scaffold-only after regeneration.
- [ ] Reuse path through the scaffold generator/fill helper and existing plan/task artifacts called out explicitly.
- [ ] Concrete artifact validation commands and docstring obligations listed.

## Dependencies

- T001
- T002
- T003
- T004
- T005
- T006
- T007
- T008
- T009

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified

