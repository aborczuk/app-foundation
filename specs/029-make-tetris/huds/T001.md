---
feature_id: "029"
task_id: "T001"
---

# HUD: T001 - Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`

## Objective

Deliver `T001` so the declared seam satisfies: Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`.

## Relevant Domains

- `client/UI` - The feature is an interactive game experience with keyboard controls, visible score, game-over state, and restart flow.
- `edge delivery` - The repo has no existing browser app, so the feature needs an intentional route/static-asset delivery seam from the existing FastAPI runtime.
- `code patterns` - Tetris needs a deterministic game-state engine with explicit transitions for spawn, move, lock, clear, score, and restart.

## Current Repo Behavior

BLOCKED: current behavior not validated from repo reads.

## Target Behavior

- `PL-01` (medium) - Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.
- `PL-02` (high) - Create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.
- Task summary: Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/__init__.py:package`

## Reuse Candidates

- `None validated from repo reads.`

## Required Edits

- In `src/clickup_control_plane/tetris/__init__.py:package`, implement the task contract described by `Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`` without widening the seam beyond the declared files.
- In `src/clickup_control_plane/tetris/router.py`, make the supporting seam change needed for `Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`` and keep file ownership aligned with the task contract.
- In `src/clickup_control_plane/tetris/models.py`, make the supporting seam change needed for `Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`` and keep file ownership aligned with the task contract.
- In `src/clickup_control_plane/tetris/service.py`, make the supporting seam change needed for `Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`` and keep file ownership aligned with the task contract.
- Honor PL-01 by keeping the implementation aligned with: Extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.
- Honor PL-02 by keeping the implementation aligned with: Create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/__init__.py:package` - In `src/clickup_control_plane/tetris/__init__.py:package`, implement the task contract described by `Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`` without widening the seam beyond the declared files.
- `src/clickup_control_plane/tetris/router.py` - supporting seam for the same task contract.
- `src/clickup_control_plane/tetris/models.py` - supporting seam for the same task contract.
- `src/clickup_control_plane/tetris/service.py` - supporting seam for the same task contract.

### Create

- None.

### Preserve

- Preserve task ownership within the declared files; do not spread restart/game-state logic into unrelated endpoints or assets.
- Preserve deterministic state transitions and story-visible behavior outside the new seam.

## Tests To Add Or Update

### Test 1

**File**: `BLOCKED`  
**Name**: `insufficient_test_context`

Given:
- no test seam was validated from repo reads

When:
- preparing the task HUD

Then assert:
- test ownership must be clarified before implement starts

## Security Review

No secret/material handling is implied for this seam; keep behavior deterministic and feature-local.

## Done Criteria

- Target task contract `T001` is fully implemented in the declared seam without placeholder behavior or undocumented side paths.
- The resulting HUD no longer relies on generic reuse, quality-guard, or discovery placeholders.

## Constraints And Invariants

- Rapid repeated input must not move pieces outside board bounds or through locked blocks.
- Clearing multiple rows at once must update the board and score consistently.
- Game over must trigger only when a new piece cannot spawn legally, not during normal stacking.
- Restart must fully reset the play session and not carry over stale board or score state.

## Implementation Checklist

- [ ] In `src/clickup_control_plane/tetris/__init__.py:package`, implement the task contract described by `Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`` without widening the seam beyond the declared files
- [ ] In `src/clickup_control_plane/tetris/router.py`, make the supporting seam change needed for `Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`` and keep file ownership aligned with the task contract
- [ ] In `src/clickup_control_plane/tetris/models.py`, make the supporting seam change needed for `Create the Tetris package scaffold for runtime, engine, and browser-shell ownership in `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/router.py`, `src/clickup_control_plane/tetris/models.py`, and `src/clickup_control_plane/tetris/service.py`` and keep file ownership aligned with the task contract

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
