---
feature_id: "029"
task_id: "T001"
task_summary: "Extend `src/clickup_control_plane/app.py:create_app` and create `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/routes.py`, and `src/clickup_control_plane/tetris/assets/` scaffolding so the FastAPI app has a dedicated Tetris mount seam for PL-01."
primary_edit_seam: "src/clickup_control_plane/app.py:create_app"
related_paths: ["src/clickup_control_plane/app.py", "src/clickup_control_plane/tetris/__init__.py", "src/clickup_control_plane/tetris/routes.py", "src/clickup_control_plane/tetris/assets/"]
---

# HUD: T001 - Extend `src/clickup_control_plane/app.py:create_app` and create `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/routes.py`, and `src/clickup_control_plane/tetris/assets/` scaffolding so the FastAPI app has a dedicated Tetris mount seam for PL-01.

## Objective

Deliver `T001` so the declared seam satisfies: Extend `src/clickup_control_plane/app.py:create_app` and create `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/routes.py`, and `src/clickup_control_plane/tetris/assets/` scaffolding so the FastAPI app has a dedicated Tetris mount seam for PL-01..

## Relevant Domains

- Runtime Surface: this task establishes the first browser-facing hosting seam inside the existing FastAPI app.
- Routing And Asset Delivery: the task introduces the package and static-asset layout that later route and browser work will depend on.

## Candidate Design Slices

- PL-01 - Tetris Runtime Surface: extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.

## Proposed Solution

- Extend `create_app()` with one isolated Tetris mount seam and create the `src/clickup_control_plane/tetris/` package scaffold that later tasks will fill in.
- Change `src/clickup_control_plane/app.py:create_app` and create `src/clickup_control_plane/tetris/__init__.py`, `src/clickup_control_plane/tetris/routes.py`, and the `src/clickup_control_plane/tetris/assets/` directory as the feature-owned runtime surface.

## Current Repo Behavior

`src/clickup_control_plane/app.py:create_app` currently builds the FastAPI app with inline `/control-plane/health`, `/control-plane/clickup/webhook`, and `/control-plane/workflow/completion` handlers only. There is no `src/clickup_control_plane/tetris/` package, and repo reads found no existing `StaticFiles`, `HTMLResponse`, or `APIRouter` seams to reuse.

## Target Behavior

- Enable a dedicated Tetris runtime seam before gameplay code lands so the feature has a stable mount point.
- Keep the plan checkpoint true: the repo has a dedicated Tetris package and a single runtime seam in `create_app()` for the feature.
- Satisfy PL-01 by isolating the browser-facing feature seam from current control-plane endpoints.
- `src/clickup_control_plane/app.py:create_app` should own the single integration point that exposes the Tetris surface without changing existing webhook/completion behavior.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/app.py:create_app`

## Reuse Candidates

- Reuse `src/clickup_control_plane/app.py:create_app` as the only existing FastAPI application-construction seam.
- None validated from repo reads for router/static/page helpers; those seams will be net-new.

## Required Edits

- Add one Tetris registration branch in `src/clickup_control_plane/app.py`.
- Create `src/clickup_control_plane/tetris/__init__.py` to mark the feature package.
- Create `src/clickup_control_plane/tetris/routes.py` with the initial route/export seam that later tasks can extend.
- Create `src/clickup_control_plane/tetris/assets/` as the static asset home for the browser shell.

## Touched Symbols

### Modify

- `src/clickup_control_plane/app.py:create_app` - register the isolated Tetris runtime seam.
- `src/clickup_control_plane/tetris/__init__.py` - export the package entrypoint for the feature.
- `src/clickup_control_plane/tetris/routes.py` - define the initial feature route/router seam.
- `src/clickup_control_plane/tetris/assets/` - reserve the static asset location for browser code.

### Create

- None.

### Preserve

- Preserve existing `/control-plane/*` behavior and status codes.
- Preserve a single app-factory integration point for the feature.

## Tests To Add Or Update

- `uv run --no-sync python scripts/pytest_guard.py run -- tests/unit/test_tetris_engine.py tests/integration/test_tetris_routes.py` once the later test files exist; this task mainly establishes the seam those tests will exercise.

## Acceptance Criteria

- The app factory exposes one dedicated Tetris mount seam and the feature package/asset scaffold exists on disk.
- No existing control-plane endpoint behavior changes as part of introducing the new mount seam.

## Done Criteria

- `uv run python scripts/speckit_remake_huds.py validate --feature-dir specs/029-make-tetris --json` passes after the scaffold is completed.
- The repo contains the `src/clickup_control_plane/tetris/` package plus a single `create_app()` integration seam for the feature.

## Constraints And Invariants

- Do not perturb existing control-plane endpoints while introducing the Tetris surface.
- Keep route ownership isolated to the feature package and `create_app()` seam.
- Preserve traceability back to PL-01.

## Implementation Checklist

- [ ] Current repo behavior in `create_app()` and the absence of Tetris/browser seams captured from repo reads.
- [ ] Reuse path limited to `create_app()`; net-new route/static helpers called out explicitly.
- [ ] Package scaffold and app-factory seam changes listed concretely.

## Dependencies

- None.

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified

