---
feature_id: "029"
task_id: "T000"
---

# HUD: T000 - Record External Ingress + Runtime Readiness Gate status, route ownership, and rollout note in `specs/029-make-tetris/tasks.md`

## Objective

Deliver `T000` so the declared seam satisfies: Record External Ingress + Runtime Readiness Gate status, route ownership, and rollout note in `specs/029-make-tetris/tasks.md`.

## Relevant Domains

- `edge delivery` - The repo has no existing browser app, so the feature needs an intentional route/static-asset delivery seam from the existing FastAPI runtime.

## Current Repo Behavior

Verified `specs/029-make-tetris/tasks.md` exists in the repo worktree. Verified `T000` is already referenced in `specs/029-make-tetris/tasks.md`.

## Target Behavior

- `PL-04` (medium) - Add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state.
- Task summary: Record External Ingress + Runtime Readiness Gate status, route ownership, and rollout note in `specs/029-make-tetris/tasks.md`.

## Primary Edit Seam

**File:Symbol**: `specs/029-make-tetris/tasks.md:T000`

## Reuse Candidates

- `specs/029-make-tetris/tasks.md:T000` - extend the existing `T000` seam in `specs/029-make-tetris/tasks.md`.
- `specs/029-make-tetris/tasks.md` - keep the change inside this existing feature seam rather than introducing a parallel path.

## Required Edits

- In `specs/029-make-tetris/tasks.md:T000`, implement the task contract described by `Record External Ingress + Runtime Readiness Gate status, route ownership, and rollout note in `specs/029-make-tetris/tasks.md`` without widening the seam beyond the declared files.
- Honor PL-04 by keeping the implementation aligned with: Add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state.

## Touched Symbols

### Modify

- `specs/029-make-tetris/tasks.md:T000` - In `specs/029-make-tetris/tasks.md:T000`, implement the task contract described by `Record External Ingress + Runtime Readiness Gate status, route ownership, and rollout note in `specs/029-make-tetris/tasks.md`` without widening the seam beyond the declared files.

### Create

- None.

### Preserve

- Preserve task ownership within the declared files; do not spread restart/game-state logic into unrelated endpoints or assets.
- Preserve deterministic state transitions and story-visible behavior outside the new seam.

## Tests To Add Or Update

### Test 1

**File**: `specs/029-make-tetris/tasks.md`  
**Name**: `traceability_review`

Given:
- the tasks artifact and generated HUDs are updated for the feature

When:
- reviewing trace notes and scenario references

Then assert:
- every story-owned implementation task points back to the relevant plan slice or acceptance scenario
- no generated HUD reintroduces placeholder sections or generic filler

## Security Review

N/A - doc/test scope only; no auth, secret, or external-system material change is implied.

## Done Criteria

- Target task contract `T000` is fully implemented in the declared seam without placeholder behavior or undocumented side paths.
- The resulting HUD no longer relies on generic reuse, quality-guard, or discovery placeholders.

## Constraints And Invariants

- Rapid repeated input must not move pieces outside board bounds or through locked blocks.
- Clearing multiple rows at once must update the board and score consistently.
- Game over must trigger only when a new piece cannot spawn legally, not during normal stacking.
- Restart must fully reset the play session and not carry over stale board or score state.

## Implementation Checklist

- [ ] In `specs/029-make-tetris/tasks.md:T000`, implement the task contract described by `Record External Ingress + Runtime Readiness Gate status, route ownership, and rollout note in `specs/029-make-tetris/tasks.md`` without widening the seam beyond the declared files
- [ ] Honor PL-04 by keeping the implementation aligned with: Add deterministic tests for rule transitions and at least one integration seam proving the Tetris surface can start, progress, reach game over, and restart without corrupting state

## Dependencies

- None.

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
