---
feature_id: "029"
task_id: "T005"
task_summary: "Build the playable browser shell in `src/clickup_control_plane/tetris/assets/tetris.js`, `src/clickup_control_plane/tetris/assets/tetris.css`, and the page response in `src/clickup_control_plane/tetris/routes.py` so the board, active piece, controls, score panel, and game loop render from server-backed session state."
story_id: "US1"
primary_edit_seam: "src/clickup_control_plane/tetris/assets/tetris.js"
related_paths: ["src/clickup_control_plane/tetris/assets/tetris.js", "src/clickup_control_plane/tetris/assets/tetris.css", "src/clickup_control_plane/tetris/routes.py"]
---

# HUD: T005 - Build the playable browser shell in `src/clickup_control_plane/tetris/assets/tetris.js`, `src/clickup_control_plane/tetris/assets/tetris.css`, and the page response in `src/clickup_control_plane/tetris/routes.py` so the board, active piece, controls, score panel, and game loop render from server-backed session state.

## Objective

Deliver `T005` so the task scope satisfies: Build the playable browser shell in `src/clickup_control_plane/tetris/assets/tetris.js`, `src/clickup_control_plane/tetris/assets/tetris.css`, and the page response in `src/clickup_control_plane/tetris/routes.py` so the board, active piece, controls, score panel, and game loop render from server-backed session state..

## Relevant Domains

- Browser Shell: this task owns the visible board, controls, game loop, and score panel.
- Runtime Synchronization: the browser must render and submit commands without becoming the source of truth for rules.

## Candidate Design Slices

- PL-03 - Playable Browser Shell: build the thinnest browser shell that can render board state, collect keyboard input, display score/game-over state, and synchronize with the authoritative gameplay flow.

## Proposed Solution

- Build the browser-facing page response, JavaScript, and CSS so the board and HUD render from server-backed session snapshots and commands flow back to the route/service seam.
- Change `tetris.js` as the primary client-control seam, with `tetris.css` and the `routes.py` page response supplying the presentation and asset wiring.

## Current Repo Behavior

The repo currently has no browser-facing page, no JavaScript asset seam, and no static asset delivery helpers. Repo reads found no existing `HTMLResponse`, `StaticFiles`, or feature-specific asset patterns to extend.

## Target Behavior

- Meet the US1 independent test: opening the page shows a board plus active piece, and controls/gravity keep play moving.
- Satisfy PL-03 by keeping the shell thin: render state, collect input, show score/game-over UI, and synchronize with the authoritative Python flow.
- Preserve server-backed session state as the source of truth while the browser only renders and submits commands.
- `tetris.js` should own input/render/game-loop coordination for the page without embedding gameplay rules.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/assets/tetris.js`

Use `tetris.js` as the primary seam because this task's central obligation is browser render/input/game-loop coordination; `tetris.css` and the page response in `routes.py` support that shell.

## Reuse Candidates

- Reuse the route/session command endpoints from `src/clickup_control_plane/tetris/routes.py`.
- None validated from repo reads for existing browser asset or page-rendering seams.

## Required Edits

- Create `src/clickup_control_plane/tetris/assets/tetris.js` for rendering and input synchronization.
- Create `src/clickup_control_plane/tetris/assets/tetris.css` for the board/HUD presentation.
- Extend `src/clickup_control_plane/tetris/routes.py` to return the page response and reference the assets.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/assets/tetris.js` - implement client-side render/input/game-loop coordination.
- `src/clickup_control_plane/tetris/assets/tetris.css` - define the board, piece, and HUD presentation.
- `src/clickup_control_plane/tetris/routes.py` - return the page response that binds the browser shell to the route surface.

### Create

- Planned browser helper symbols for render/update/command dispatch and page rendering helpers in `routes.py`.

### Preserve

- Preserve a thin browser shell with no authoritative gameplay logic.

## Tests To Add Or Update

- `tests/integration/test_tetris_routes.py` should eventually verify the page response and runtime command loop through the route seam.
- Manual browser verification from the US1 independent test remains part of task acceptance.

## Acceptance Criteria

- The page renders board state, active piece, controls, score, and game-loop behavior from server-backed session snapshots.
- The browser shell submits commands through the route/runtime seam instead of mutating gameplay state locally.

## Done Criteria

- The feature has a working browser shell backed by the authoritative session routes.
- The asset/page seam exists without introducing gameplay-rule duplication in JavaScript.

## Constraints And Invariants

- Keep the browser shell thin and server-backed.
- Do not embed authoritative rule transitions in JavaScript.
- Preserve route/page/asset traceability to PL-03.

## Implementation Checklist

- [ ] Current repo behavior captured: no browser shell or static asset seam exists.
- [ ] Reuse path anchored to route/service session APIs; absence of existing asset helpers noted.
- [ ] JS, CSS, and page-response responsibilities listed concretely.

## Dependencies

- T001
- T002
- T003
- T004

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified

