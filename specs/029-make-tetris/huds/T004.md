---
feature_id: "029"
task_id: "T004"
---

# HUD: T004 - Implement the authoritative game engine API for spawn, step, move, rotate, lock, clear, score, and restart in `src/clickup_control_plane/tetris/service.py`

## Objective

Deliver `T004` so the declared seam satisfies: Implement the authoritative game engine API for spawn, step, move, rotate, lock, clear, score, and restart in `src/clickup_control_plane/tetris/service.py`.

## Relevant Domains

- `code patterns` - Tetris needs a deterministic game-state engine with explicit transitions for spawn, move, lock, clear, score, and restart.

## Current Repo Behavior

BLOCKED: current behavior not validated from repo reads.

## Target Behavior

- `PL-02` (high) - Create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.
- Task summary: Implement the authoritative game engine API for spawn, step, move, rotate, lock, clear, score, and restart in `src/clickup_control_plane/tetris/service.py`.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/service.py:TetrisGameService`

## Reuse Candidates

- `None validated from repo reads.`

## Required Edits

- In `src/clickup_control_plane/tetris/service.py:TetrisGameService`, route restart through the authoritative gameplay service so a fresh session resets board state, active piece state, score, and terminal flags.
- Honor PL-02 by keeping the implementation aligned with: Create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/service.py:TetrisGameService` - In `src/clickup_control_plane/tetris/service.py:TetrisGameService`, route restart through the authoritative gameplay service so a fresh session resets board state, active piece state, score, and terminal flags.

### Create

- None.

### Preserve

- Preserve task ownership within the declared files; do not spread restart/game-state logic into unrelated endpoints or assets.
- Preserve deterministic state transitions and story-visible behavior outside the new seam.

## Tests To Add Or Update

### Test 1

**File**: `BLOCKED`  
**Name**: `insufficient_test_context`

Given:
- no test seam was validated from repo reads

When:
- preparing the task HUD

Then assert:
- test ownership must be clarified before implement starts

## Security Review

No secret/material handling is implied for this seam; keep behavior deterministic and feature-local.

## Done Criteria

- Target task contract `T004` is fully implemented in the declared seam without placeholder behavior or undocumented side paths.
- The resulting HUD no longer relies on generic reuse, quality-guard, or discovery placeholders.

## Constraints And Invariants

- Rapid repeated input must not move pieces outside board bounds or through locked blocks.
- Clearing multiple rows at once must update the board and score consistently.
- Game over must trigger only when a new piece cannot spawn legally, not during normal stacking.
- Restart must fully reset the play session and not carry over stale board or score state.
- Restart must create a fresh playable state instead of mutating the ended session in place.

## Implementation Checklist

- [ ] In `src/clickup_control_plane/tetris/service.py:TetrisGameService`, route restart through the authoritative gameplay service so a fresh session resets board state, active piece state, score, and terminal flags
- [ ] Honor PL-02 by keeping the implementation aligned with: Create typed Python symbols for gameplay state and transition operations so spawn, move, rotate, lock, clear, score, and restart can be verified deterministically
- [ ] Confirm prerequisite task outputs are present before editing: T001.

## Dependencies

- T001

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
