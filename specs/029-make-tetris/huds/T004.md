---
feature_id: "029"
task_id: "T004"
task_summary: "Add the initial game page and session endpoints in `src/clickup_control_plane/tetris/routes.py` and wire them from `src/clickup_control_plane/app.py:create_app` so the browser can fetch a fresh authoritative session and tick/move/rotate commands through PL-01 and PL-03 seams."
story_id: "US1"
primary_edit_seam: "src/clickup_control_plane/tetris/routes.py"
related_paths: ["src/clickup_control_plane/tetris/routes.py", "src/clickup_control_plane/app.py"]
---

# HUD: T004 - Add the initial game page and session endpoints in `src/clickup_control_plane/tetris/routes.py` and wire them from `src/clickup_control_plane/app.py:create_app` so the browser can fetch a fresh authoritative session and tick/move/rotate commands through PL-01 and PL-03 seams.

## Objective

Deliver `T004` so the declared seam satisfies: Add the initial game page and session endpoints in `src/clickup_control_plane/tetris/routes.py` and wire them from `src/clickup_control_plane/app.py:create_app` so the browser can fetch a fresh authoritative session and tick/move/rotate commands through PL-01 and PL-03 seams..

## Relevant Domains

- Runtime Surface: this task turns the package seam into actual user-reachable routes.
- API Contracting: the browser shell needs authoritative session and command endpoints that preserve server ownership of game state.

## Candidate Design Slices

- PL-01 - Tetris Runtime Surface: extend the FastAPI runtime with an isolated Tetris route and supporting delivery helpers without perturbing existing control-plane endpoints.
- PL-03 - Playable Browser Shell: build the thinnest browser shell that can render board state and synchronize with the authoritative gameplay flow.

## Proposed Solution

- Extend `routes.py` with the initial page and command/session endpoints, then wire them through `create_app()` so the browser can create a session and submit tick/move/rotate commands against the service layer.
- Change `src/clickup_control_plane/tetris/routes.py` as the primary seam and modify `src/clickup_control_plane/app.py:create_app` only to attach the route surface.

## Current Repo Behavior

`create_app()` currently exposes only inline control-plane endpoints, and there is no `routes.py` behavior for browser-facing sessions or commands. Repo reads found no existing `APIRouter` seam to extend.

## Target Behavior

- Make the feature reachable as the MVP entry point for US1.
- Preserve the US1 independent test setup: opening the Tetris page can fetch a fresh authoritative session and send gameplay commands.
- Satisfy PL-01 and PL-03 by mounting an isolated route/runtime surface that brokers all gameplay through server-backed session state.
- `routes.py` should own the initial page/session/command seams, with `create_app()` only attaching them.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/app.py:create_app`

## Reuse Candidates

- Reuse `src/clickup_control_plane/app.py:create_app` as the app-factory mount seam.
- Reuse the `service.py` session orchestration seam from T003 for command handling.
- None validated from repo reads for an existing browser route module.

## Required Edits

- Extend `src/clickup_control_plane/tetris/routes.py` with initial page and session/command handlers.
- Update `src/clickup_control_plane/app.py` to attach those routes through the app factory seam.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/routes.py` - add page, session, tick, move, and rotate route handlers.
- `src/clickup_control_plane/app.py:create_app` - attach the Tetris route surface.

### Create

- Planned route handler symbols for page delivery and authoritative session commands.

### Preserve

- Preserve route isolation and keep gameplay decisions delegated to the service layer.

## Tests To Add Or Update

- `tests/integration/test_tetris_routes.py` should exercise page open, session creation, and move/tick/rotate command flow through the FastAPI runtime seam.

## Acceptance Criteria

- The browser can open the Tetris surface and talk to authoritative session endpoints through the app runtime.
- Route handlers do not embed gameplay rules; they delegate to the service seam.

## Done Criteria

- The MVP route/runtime path exists from `create_app()` to `routes.py` and into the session service.
- The feature surface is reachable without changing existing control-plane routes.

## Constraints And Invariants

- Keep all gameplay state authoritative on the server.
- Do not perturb existing `/control-plane/*` behavior.
- Preserve route/service separation so UI code never owns rule transitions.

## Implementation Checklist

- [ ] Current route behavior captured from `create_app()` and missing `routes.py` runtime seams.
- [ ] Reuse path through `create_app()` and `service.py` called out explicitly.
- [ ] Concrete page/session/command route changes listed.

## Dependencies

- T001
- T002
- T003

## Process Checklist

- [x] current_behavior_verified
- [x] implementation_directive_complete
- [x] touched_symbols_verified
- [x] tests_specified
- [x] constraints_verified

