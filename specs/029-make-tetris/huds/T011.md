---
feature_id: "029"
task_id: "T011"
---

# HUD: T011 - Surface score and cleared-board updates in the browser shell contract in `src/clickup_control_plane/tetris/templates/tetris.html` and `src/clickup_control_plane/tetris/static/tetris.js`

## Objective

Deliver `T011` so the declared seam satisfies: Surface score and cleared-board updates in the browser shell contract in `src/clickup_control_plane/tetris/templates/tetris.html` and `src/clickup_control_plane/tetris/static/tetris.js`.

## Relevant Domains

- `client/UI` - The feature is an interactive game experience with keyboard controls, visible score, game-over state, and restart flow.
- `code patterns` - Tetris needs a deterministic game-state engine with explicit transitions for spawn, move, lock, clear, score, and restart.

## Current Repo Behavior

BLOCKED: current behavior not validated from repo reads.

## Target Behavior

- Fulfill US2 (Clear Lines and Track Score (Priority: P2)): A user clears completed rows and sees the score update during the same session.
- Keep the story-level proof true: Play until a row is completed, confirm the row is removed, confirm the board collapses correctly, and verify the score increases.
- `PL-03` (high) - Build the thinnest browser shell that can render board state, collect keyboard input, display score/game-over state, and synchronize with the authoritative gameplay flow.
- Task summary: Surface score and cleared-board updates in the browser shell contract in `src/clickup_control_plane/tetris/templates/tetris.html` and `src/clickup_control_plane/tetris/static/tetris.js`.

## Primary Edit Seam

**File:Symbol**: `src/clickup_control_plane/tetris/static/tetris.js:renderScoreboard`

## Reuse Candidates

- `None validated from repo reads.`

## Required Edits

- In `src/clickup_control_plane/tetris/static/tetris.js:renderScoreboard`, implement the task contract described by `Surface score and cleared-board updates in the browser shell contract in `src/clickup_control_plane/tetris/templates/tetris.html` and `src/clickup_control_plane/tetris/static/tetris.js`` without widening the seam beyond the declared files.
- In `src/clickup_control_plane/tetris/templates/tetris.html`, render the UI state and controls this task adds, including any game-over or restart affordances required by the story.
- Honor PL-03 by keeping the implementation aligned with: Build the thinnest browser shell that can render board state, collect keyboard input, display score/game-over state, and synchronize with the authoritative gameplay flow.

## Touched Symbols

### Modify

- `src/clickup_control_plane/tetris/static/tetris.js:renderScoreboard` - In `src/clickup_control_plane/tetris/static/tetris.js:renderScoreboard`, implement the task contract described by `Surface score and cleared-board updates in the browser shell contract in `src/clickup_control_plane/tetris/templates/tetris.html` and `src/clickup_control_plane/tetris/static/tetris.js`` without widening the seam beyond the declared files.
- `src/clickup_control_plane/tetris/templates/tetris.html` - supporting seam for the same task contract.

### Create

- None.

### Preserve

- Preserve task ownership within the declared files; do not spread restart/game-state logic into unrelated endpoints or assets.
- Preserve deterministic state transitions and story-visible behavior outside the new seam.

## Tests To Add Or Update

### Test 1

**File**: `tests/unit/test_tetris_engine.py`  
**Name**: `test_line_clear_and_score_updates`

Given:
- 1. **Given** a row becomes fully occupied, **When** the active piece locks, **Then** the completed row is cleared before normal play resumes.
- 3. **Given** the session continues after a clear, **When** the next piece enters play, **Then** the board state reflects the cleared rows accurately.

When:
- execute the seam owned by `T011`

Then assert:
- `T009` passes for the target task contract
- the behavior described by `T011` is deterministic at `tests/unit/test_tetris_engine.py:test_line_clear_and_score_updates`
- the story-level verification remains true: Play until a row is completed, confirm the row is removed, confirm the board collapses correctly, and verify the score increases.

## Security Review

No secret/material handling is implied, but keep browser/runtime seams limited to the declared route, payload, and session behavior for this task.

## Done Criteria

- Target task contract `T011` is fully implemented in the declared seam without placeholder behavior or undocumented side paths.
- Relevant regression remains green at `tests/unit/test_tetris_engine.py` for `test_line_clear_and_score_updates`.
- The resulting HUD no longer relies on generic reuse, quality-guard, or discovery placeholders.

## Constraints And Invariants

- 1. **Given** a row becomes fully occupied, **When** the active piece locks, **Then** the completed row is cleared before normal play resumes.
- 2. **Given** one or more rows are cleared, **When** scoring is recalculated, **Then** the player sees an updated score in the game UI.
- Rapid repeated input must not move pieces outside board bounds or through locked blocks.
- Clearing multiple rows at once must update the board and score consistently.
- Game over must trigger only when a new piece cannot spawn legally, not during normal stacking.

## Implementation Checklist

- [ ] In `src/clickup_control_plane/tetris/static/tetris.js:renderScoreboard`, implement the task contract described by `Surface score and cleared-board updates in the browser shell contract in `src/clickup_control_plane/tetris/templates/tetris.html` and `src/clickup_control_plane/tetris/static/tetris.js`` without widening the seam beyond the declared files
- [ ] In `src/clickup_control_plane/tetris/templates/tetris.html`, render the UI state and controls this task adds, including any game-over or restart affordances required by the story
- [ ] Honor PL-03 by keeping the implementation aligned with: Build the thinnest browser shell that can render board state, collect keyboard input, display score/game-over state, and synchronize with the authoritative gameplay flow
- [ ] Keep story-aligned regressions ready: T009.
- [ ] Confirm prerequisite task outputs are present before editing: T007, T008.

## Dependencies

- T007, T008

## Process Checklist

- [ ] current_behavior_verified
- [ ] implementation_directive_complete
- [ ] touched_symbols_verified
- [ ] tests_specified
- [ ] constraints_verified
