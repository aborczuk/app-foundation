---
feature_id: "019"
task_id: "T049"
---

# HUD: T049 — Wire add-to-backlog pipeline event emission by updating .claude/commands/speckit.addtobacklog.md and .specify/command-manifest.yaml so `/speckit.addtobacklog` emits `backlog_registered` via scripts/pipeline_ledger.py

## Working Memory

**File:Symbol**: `.claude/commands/speckit.addtobacklog.md:Outline`  
**Current line**: `1`  
**Callers**: operator workflow + manifest-driven governance validators  
**Reuse path**: reuse existing `backlog_registered` pipeline event and `pipeline_ledger.py append` command

## Solution Sketch

trivial

## Functional Goal

**Story Goal**: Keep deterministic pipeline event trails complete for all speckit entry paths.  
**Acceptance Criteria**: `/speckit.addtobacklog` explicitly emits `backlog_registered` and manifest declares the event contract.

## Quality Guards

Domain 16, Domain 13, Domain 14, Domain 17

## Process Checklist

- [ ] discovery_completed
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
