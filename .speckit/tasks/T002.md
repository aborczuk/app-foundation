---
feature_id: "022"
task_id: "T002"
---

# HUD: T002 — Add the MCP health tool registration to `src/mcp_codebase/server.py` and serialize the shared health result there with run-scoped JSONL logging

## Working Memory

**File:Symbol**: `src/mcp_codebase/server.py:_register_tools`  
**Current line**: `src/mcp_codebase/server.py:74`  
**Callers**: `create_server() -> main()`  
**Reuse path**: the existing `FastMCP` registration pattern and `_JsonlFormatter` logging path

## Solution Sketch

**Modify**: `src/mcp_codebase/server.py:_register_tools` and `src/mcp_codebase/server.py:_setup_logging`.  
**Create**: none.  
**Reuse**: the existing `get_type` / `get_diagnostics` registration style plus the new `GraphHealthResult` contract from `src/mcp_codebase/health.py`.  
**Composition**: the MCP server asks the shared health seam for a result, serializes the same status/hint payload, and logs run metadata alongside the verdict.  
**Failing test assertion**: the integration test should fail until the health tool returns the shared status/hint payload and the JSONL log includes the expected run-scoped fields.  
**Domains touched**: `.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Test Specification

Assert that the MCP health tool returns the same status/hint object as the shared classifier and that the run log contains the run id plus health metadata.

## Security Review

N/A — the adapter stays local to the repo checkout and only exposes existing graph-health information.

## Functional Goal

**Story Goal**: User Story 1 - Graph Health Check for Developers  
**Acceptance Criteria**: The MCP tool exposes a deterministic health result with clear status and recovery guidance.

## Quality Guards

`.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
