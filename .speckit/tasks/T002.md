---
feature_id: "020"
task_id: "T002"
---

# HUD: T002 — Define index domain and config models

## Working Memory

**File:Symbol**: `src/mcp_codebase/index/domain.py:IndexScope`
**Current line**: new module / symbol does not exist yet
**Callers**: src/mcp_codebase/index/service.py:VectorIndexService, tests in tests/unit/ and tests/integration/
**Reuse path**: existing repo-root and path-validation conventions from `src/mcp_codebase/config.py` and `security.py`

## Solution Sketch
**Modify**: src/mcp_codebase/index/domain.py:IndexScope — Define index domain and config models
**Create**: as required by the slice; keep net-new surface minimal
**Reuse**: existing repo-root and path-validation conventions from `src/mcp_codebase/config.py` and `security.py`
**Composition**: Define the typed schema, freshness states, and repo-local index configuration before any extraction or persistence logic is built. The domain models become the shared contract for service, store, and adapters, while the config surface centralizes storage path, model id, and scope defaults.
**Failing test assertion**: Assert the models reject malformed freshness metadata and preserve typed fields for source path, line range, and scope.
**Domains touched**: .claude/domains/02_data_modeling_schemas.md, .claude/domains/03_data_storage_persistence.md, .claude/domains/09_environment_configuration.md, .claude/domains/17_code_patterns.md

## Test Specification

Assert the models reject malformed freshness metadata and preserve typed fields for source path, line range, and scope.

## Security Review

N/A — pure data modeling; no external inputs beyond local file metadata.

## Functional Goal

**Story Goal**: Phase 2: Foundational (Blocking Prerequisites)
**Acceptance Criteria**: The feature has typed domain objects for scopes, symbol records, markdown sections, metadata, and query results.

## Quality Guards

`.claude/domains/02_data_modeling_schemas.md`, `.claude/domains/03_data_storage_persistence.md`, `.claude/domains/09_environment_configuration.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
