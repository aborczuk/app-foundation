---
feature_id: "019"
task_id: "T046"
---

# HUD: T046 — Invoke post-generation artifact validation in scripts/pipeline_driver.py:main via validate_generated_artifact before returning success for generative routes

## Working Memory

**File:Symbol**: `scripts/pipeline_driver.py:main`  
**Callers**: CLI entrypoint (`scripts/pipeline_driver.py`)  
**Reuse path**: call existing `validate_generated_artifact(...)` in the generative success path before any success envelope is emitted

## Solution Sketch

Modify: `scripts/pipeline_driver.py:main` generative branch.  
Create: no net-new validator; reuse existing validator primitive and route mapper contracts.  
Reuse: `validate_generated_artifact`, blocked-reason contract, and canonical result envelope fields.  
Composition: run validation immediately after adapter output normalization; on validation failure, emit blocked contract and stop success progression.

## Test Specification

Assert that invalid or missing generated artifacts return deterministic blocked output and do not return success for generative routes.

## Security Review

Validation failure output must avoid leaking generated payload contents while preserving actionable blocked reasons.

## Functional Goal

**Story Goal**: Resolve phase state deterministically, dispatch mapped scripts, and return explicit LLM handoff only when generation is required.  
**Acceptance Criteria**: Generative route success is gated by explicit artifact validation.

## Quality Guards

Domain 07, Domain 13, Domain 14, Domain 17

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
