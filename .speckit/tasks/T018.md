---
feature_id: "020"
task_id: "T018"
---

# HUD: T018 — Implement staleness reporting in the service and adapters

## Working Memory

**File:Symbol**: `src/mcp_codebase/index/service.py:VectorIndexService.status`
**Current line**: new module / symbol does not exist yet
**Callers**: MCP adapter, CLI adapter, and test fixtures
**Reuse path**: existing doctor/status output patterns and the shared service factory

## Solution Sketch
**Modify**: src/mcp_codebase/index/service.py:VectorIndexService.status — Implement staleness reporting in the service and adapters
**Create**: as required by the slice; keep net-new surface minimal
**Reuse**: existing doctor/status output patterns and the shared service factory
**Composition**: Status reporting stays derived from stored metadata and HEAD comparison, then the MCP and CLI surfaces render that same freshness result to callers. The service owns the state math; the adapters only format it.
**Failing test assertion**: The status path reports `up to date` or `stale` with a recorded commit delta instead of a raw internal state dump.
**Domains touched**: .claude/domains/10_observability.md, .claude/domains/11_resilience_continuity.md, .claude/domains/16_ops_governance.md, .claude/domains/17_code_patterns.md

## Test Specification

The status path reports `up to date` or `stale` with a recorded commit delta instead of a raw internal state dump.

## Security Review

No new trust boundary; keep status derived from local metadata only.

## Functional Goal

**Story Goal**: Phase 6: User Story 4 - Staleness Check (Priority: P3)
**Acceptance Criteria**: The status path reports up-to-date or stale with a recorded commit delta instead of a raw internal state dump.

## Quality Guards

`.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
