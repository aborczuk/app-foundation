---
feature_id: "022"
task_id: "T015"
---

# HUD: T015 — Add unit coverage in tests/unit/test_query_tools.py for malformed input and query-failure handling in src/mcp_codebase/type_tool.py and src/mcp_codebase/diag_tool.py

## Working Memory

**File:Symbol**: `tests/unit/test_query_tools.py:test_query_failure_and_validation_contract`  
**Callers**: `Resolve via codegraph analyze_code_relationships during implementation`  
**Reuse path**: bounded unit coverage on existing query-tool contracts

## Solution Sketch

Modify: `src/mcp_codebase/type_tool.py:get_type_impl`, `src/mcp_codebase/pyright_client.py:hover`, and `tests/unit/test_query_tools.py`
Create: `tests/unit/test_query_tools.py`
Reuse: the existing validation helpers, the current diagnostic subprocess contract, and the type tool error-envelope pattern.
Composition: malformed input fails validation, missing symbols still report SYMBOL_NOT_FOUND, and a real query failure returns an explicit query-failure error instead of being misclassified.
Test Specification: assert that empty input is rejected, symbol-not-found remains distinct, and transport/subprocess failures surface as clear tool errors.
Domains touched: Domain 10, Domain 11, Domain 12, Domain 14, Domain 17

## Test Specification

Assert that malformed query input and transport failures are distinguished from missing-symbol results and do not crash the query tools.

## Security Review

N/A — in-repo primitives only; no secrets or external dependencies.

## Functional Goal

**Story Goal**: Make the query tools distinguish malformed input, missing symbols, and real query execution failures so agents get clear recovery guidance.  
**Acceptance Criteria**: Empty input is rejected, missing symbols remain SYMBOL_NOT_FOUND, and query failures report a dedicated failure code or unavailable state.

## Quality Guards

Domain 10, Domain 11, Domain 12, Domain 14, Domain 17

## Process Checklist

- [X] discovery_completed
- [X] lld_recorded
- [X] quality_guards_passed
- [X] functional_goal_achieved
- [X] tests_passed
