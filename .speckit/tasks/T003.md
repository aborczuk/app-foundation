---
feature_id: "020"
task_id: "T003"
---

# HUD: T003 — Implement Python and markdown extractors

## Working Memory

**File:Symbol**: `src/mcp_codebase/index/extractors/python.py:extract_python_symbols`
**Current line**: new module / symbol does not exist yet
**Callers**: src/mcp_codebase/index/service.py:VectorIndexService.build_full_index, tests in tests/unit/
**Reuse path**: existing path-safety conventions and markdown section-reading patterns

## Solution Sketch
**Modify**: src/mcp_codebase/index/extractors/python.py:extract_python_symbols — Implement Python and markdown extractors
**Create**: as required by the slice; keep net-new surface minimal
**Reuse**: existing path-safety conventions and markdown section-reading patterns
**Composition**: Build the Python and markdown extractors after the domain/config slice lands. Python extraction turns AST or tree-sitter output into symbol units; markdown extraction turns headings and content into breadcrumbed section units; both feed the same normalized content schema.
**Failing test assertion**: Assert a symbol with no docstring still produces a valid record, and generated artifacts or excluded paths are skipped instead of causing a failure.
**Domains touched**: .claude/domains/02_data_modeling_schemas.md, .claude/domains/12_testing_quality_gates.md, .claude/domains/14_security_controls.md, .claude/domains/17_code_patterns.md

## Test Specification

Assert a symbol with no docstring still produces a valid record, and generated artifacts or excluded paths are skipped instead of causing a failure.

## Security Review

N/A — source ingestion is local-only and path-bound.

## Functional Goal

**Story Goal**: Phase 2: Foundational (Blocking Prerequisites)
**Acceptance Criteria**: Python symbols and markdown sections are normalized into content units with provenance, hashes, and previews.

## Quality Guards

`.claude/domains/02_data_modeling_schemas.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
