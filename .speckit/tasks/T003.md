---
feature_id: "022"
task_id: "T003"
---

# HUD: T003 — Create `src/mcp_codebase/doctor.py` and the `scripts/cgc_doctor.sh` wrapper for a direct operator-facing health command

## Working Memory

**File:Symbol**: `src/mcp_codebase/doctor.py:main`  
**Current line**: new module / symbol does not exist yet  
**Callers**: `scripts/cgc_doctor.sh` and direct operator invocations  
**Reuse path**: `scripts/validate_doc_graph.sh` wrapper shape and `src/mcp_codebase/health.py` classifier logic

## Solution Sketch

**Modify**: none.  
**Create**: `src/mcp_codebase/doctor.py` and `scripts/cgc_doctor.sh`.  
**Reuse**: the shared health contract from `src/mcp_codebase/health.py` and the shell-wrapper conventions already used by repo maintenance scripts.  
**Composition**: the shell wrapper launches the Python CLI, the CLI calls the shared classifier, and the printed next-action text mirrors the MCP tool so operators and agents see the same guidance.  
**Failing test assertion**: the subprocess test should fail until the doctor command prints a clear healthy/stale/locked/unavailable verdict and exits non-zero on unhealthy states.  
**Domains touched**: `.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Test Specification

Assert that the operator-facing doctor command returns a clear status plus a recovery hint and does not crash on stale, locked, or unreadable graph state.

## Security Review

N/A — local operator tooling only; the command should not require new external credentials.

## Functional Goal

**Story Goal**: User Story 1 - Graph Health Check for Developers  
**Acceptance Criteria**: A developer can run a deterministic health check and see a clear pass/fail result with a reason.

## Quality Guards

`.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
