---
feature_id: "019"
task_id: "T050"
---

# HUD: T050 — Route add-to-backlog triage outcomes to the appropriate downstream phase by estimate/scope in .claude/commands/speckit.addtobacklog.md (specify/plan/solution/implement handoff guidance)

## Working Memory

**File:Symbol**: `.claude/commands/speckit.addtobacklog.md:Outline`  
**Current line**: `19`  
**Callers**: operator workflow routing after add-to-backlog triage  
**Reuse path**: reuse estimate and architectural-fit outcomes already computed during addtobacklog

## Solution Sketch

Modify: `.claude/commands/speckit.addtobacklog.md` to add explicit phase-routing matrix based on estimate/scope triage outcomes.  
Create: no new script in first pass; document deterministic routing rules in command contract.  
Reuse: existing command boundaries (`/speckit.specify`, `/speckit.plan`, `/speckit.solution`, `/speckit.implement`).  
Composition: once triage completes, emit one deterministic next-step command and avoid ambiguous “implement by default” guidance.

## Test Specification

Assert command guidance routes large/new-scope work to `specify` or `plan`, architecture-fit non-trivial work to `solution`, and only implement-ready work to `implement`.

## Security Review

Ensure routing guidance does not bypass security/quality gates tied to earlier phases.

## Functional Goal

**Story Goal**: Keep add-to-backlog as a reliable intake router that hands off to the correct phase.  
**Acceptance Criteria**: add-to-backlog output includes deterministic next-phase command guidance based on triage + estimate outcomes.

## Quality Guards

Domain 16, Domain 13, Domain 14, Domain 17

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
