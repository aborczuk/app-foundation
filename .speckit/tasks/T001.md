---
feature_id: "022"
task_id: "T001"
---

# HUD: T001 — Create `src/mcp_codebase/health.py` with the shared graph-readiness domain models and classifier seam

## Working Memory

**File:Symbol**: `src/mcp_codebase/health.py:classify_graph_health`  
**Current line**: new module / symbol does not exist yet  
**Callers**: `src/mcp_codebase/server.py:_register_tools`, `src/mcp_codebase/doctor.py:main`  
**Reuse path**: `src/mcp_codebase/type_tool.py:get_type_impl`, `src/mcp_codebase/diag_tool.py:get_diagnostics_impl`, and `src/mcp_codebase/security.py:validate_path`

## Solution Sketch

**Modify**: none.  
**Create**: `src/mcp_codebase/health.py` with `GraphHealthStatus`, `GraphHealthResult`, `GraphRecoveryHint`, and `classify_graph_health`.  
**Reuse**: the existing typed-result envelopes and local validation patterns from `type_tool.py` and `diag_tool.py`.  
**Composition**: the classifier reads local graph state, classifies it into a stable health status, and returns a recovery hint that the MCP server and doctor CLI can both render unchanged.  
**Failing test assertion**: the unit test should fail until healthy, stale, locked, and unreadable/query-failure fixtures each produce the expected status and recovery hint.  
**Domains touched**: `.claude/domains/02_data_modeling_schemas.md`, `.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/17_code_patterns.md`

## Test Specification

Assert that the shared classifier returns distinct status values and hints for healthy, stale, locked, and unavailable graph states.

## Security Review

N/A — local graph state only; no external source or privilege boundary introduced.

## Functional Goal

**Story Goal**: User Story 1 - Graph Health Check for Developers  
**Acceptance Criteria**: The health seam can explain whether the local graph is healthy, stale, locked, or unavailable.

## Quality Guards

`.claude/domains/02_data_modeling_schemas.md`, `.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
