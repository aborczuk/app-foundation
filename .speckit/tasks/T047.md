---
feature_id: "019"
task_id: "T047"
---

# HUD: T047 — Append success event and phase advancement for validated generative outputs in scripts/pipeline_driver.py:main using scripts/pipeline_ledger.py event contracts

## Working Memory

**File:Symbol**: `scripts/pipeline_driver.py:main`  
**Callers**: CLI entrypoint (`scripts/pipeline_driver.py`)  
**Reuse path**: reuse deterministic ledger append + phase advancement flow after generative validation passes

## Solution Sketch

Modify: `scripts/pipeline_driver.py:main` generative branch success path.  
Create: no new ledger schema; reuse existing `scripts/pipeline_ledger.py` event contracts and append helpers.  
Reuse: event type mapping, required-field enforcement, and phase state progression rules already used by deterministic routes.  
Composition: once adapter output is validated, append success event, advance phase state, then return canonical success envelope.

## Test Specification

Assert that validated generative outputs append the expected success event and advance phase, and that failures do not mutate ledger state.

## Security Review

Ensure ledger appends contain only contract-approved fields and never include raw generated content.

## Functional Goal

**Story Goal**: Resolve phase state deterministically, dispatch mapped scripts, and return explicit LLM handoff only when generation is required.  
**Acceptance Criteria**: Validated generative outputs are committed to the same success/event state machine as deterministic routes.

## Quality Guards

Domain 16, Domain 07, Domain 13, Domain 14, Domain 17

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
