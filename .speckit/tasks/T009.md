---
feature_id: "022"
task_id: "T009"
---

# HUD: T009 — Thread the recovery hint into the MCP adapter and CLI doctor adapter so the agent-facing and operator-facing outputs say the same next action

## Working Memory

**File:Symbol**: `src/mcp_codebase/server.py:get_graph_health`  
**Current line**: new helper / symbol does not exist yet  
**Callers**: `src/mcp_codebase/doctor.py:main` and the MCP tool registration path  
**Reuse path**: the shared `GraphHealthResult` / `GraphRecoveryHint` contract from `src/mcp_codebase/health.py`

## Solution Sketch

**Modify**: `src/mcp_codebase/server.py:get_graph_health` and `src/mcp_codebase/doctor.py:main`.  
**Create**: `get_graph_health` helper if it is not already present as an adapter seam.  
**Reuse**: the shared result and hint types from `src/mcp_codebase/health.py`.  
**Composition**: both the MCP adapter and the doctor CLI consume the same result object, so the next-action phrasing cannot drift between operator and agent surfaces.  
**Failing test assertion**: the test should fail until the CLI and MCP output match on recovery hint id and next-action wording.  
**Domains touched**: `.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Test Specification

Assert that the CLI and MCP outputs match on the same recovery hint and next-action wording.

## Security Review

N/A — local adapters only.

## Functional Goal

**Story Goal**: User Story 2 - Agent-Facing Recovery on Lock / Query Failure  
**Acceptance Criteria**: Failure modes are distinguishable and the same recovery guidance is visible to both agents and maintainers.

## Quality Guards

`.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
