---
feature_id: "019"
task_id: "T051"
---

# HUD: T051 — Project and emit prerequisite pipeline events from /speckit.addtobacklog so ledger readiness matches selected `NEXT_COMMAND` in .claude/commands/speckit.addtobacklog.md and .specify/command-manifest.yaml

## Working Memory

**File:Symbol**: `.claude/commands/speckit.addtobacklog.md:Outline`  
**Current line**: `86`  
**Callers**: operator intake path that triages ad-hoc requests and hands off to next pipeline phase  
**Reuse path**: reuse `scripts/pipeline_ledger.py append` and existing command-manifest event contracts

## Solution Sketch

Modify: `.claude/commands/speckit.addtobacklog.md` to add deterministic event projection/append rules keyed by selected `NEXT_COMMAND`.  
Modify: `.specify/command-manifest.yaml` to declare addtobacklog projected prerequisite emits.  
Reuse: existing event order and required fields from manifest contract (`backlog_registered` through readiness marker events).  
Composition: triage decides next phase, addtobacklog appends prerequisite event chain so ledger is phase-ready even without auto-executing the next command.

## Test Specification

Assert addtobacklog contract declares and documents deterministic prerequisite event projection so chosen next step can start without ledger-gap blockers.

## Security Review

Ensure projected event guidance does not bypass quality gates and only emits events already declared in canonical manifest contracts.

## Functional Goal

**Story Goal**: Keep addtobacklog as a ready-state intake that leaves ledger consistent for downstream gating.  
**Acceptance Criteria**: addtobacklog determines next phase and appends all required prior events (with required fields) up to readiness marker for that selected phase.

## Quality Guards

Domain 16, Domain 13, Domain 14, Domain 17

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
