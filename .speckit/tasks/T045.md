---
feature_id: "019"
task_id: "T045"
---

# HUD: T045 — Wire generative handoff execution adapter in scripts/pipeline_driver.py:main to call an LLM handoff runner and capture generated artifact output metadata

## Working Memory

**File:Symbol**: `scripts/pipeline_driver.py:main`  
**Callers**: CLI entrypoint (`scripts/pipeline_driver.py`)  
**Reuse path**: extend existing `resolve_step_mapping(...)->handoff` branch for adapter execution

## Solution Sketch

Modify: `scripts/pipeline_driver.py:main` generative branch.  
Create: helper only if needed to keep adapter invocation/test seams clean.  
Reuse: `resolve_step_mapping` handoff contract payload and canonical step-result envelope.  
Composition: execute adapter from handoff payload, normalize generated artifact metadata for downstream validation.

## Test Specification

Assert that generative route executes adapter and returns deterministic metadata contract for the generated artifact path/output.

## Security Review

No secrets in adapter payload/logs; preserve deterministic blocked reasons on adapter failure.

## Functional Goal

**Story Goal**: Resolve phase state deterministically, dispatch mapped scripts, and return explicit LLM handoff only when generation is required.  
**Acceptance Criteria**: Generative route executes through explicit handoff adapter rather than placeholder-only return.

## Quality Guards

Domain 07, Domain 13, Domain 14, Domain 17

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
