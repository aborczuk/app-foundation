---
feature_id: "020"
task_id: "T005"
---

# HUD: T005 — Wire the vector index into the MCP server and CLI adapter

## Working Memory

**File:Symbol**: `src/mcp_codebase/server.py:register_vector_index_tools`
**Current line**: new module / symbol does not exist yet
**Callers**: Claude / MCP runtime entrypoints and local operator workflows
**Reuse path**: existing MCP registration pattern in `server.py` and the repo’s CLI entry-point style

## Solution Sketch
**Modify**: src/mcp_codebase/server.py:register_vector_index_tools — Wire the vector index into the MCP server and CLI adapter
**Create**: as required by the slice; keep net-new surface minimal
**Reuse**: existing MCP registration pattern in `server.py` and the repo’s CLI entry-point style
**Composition**: Expose the vector index through the existing MCP server and a thin CLI adapter without changing the existing pyright tools. The server registers new tool handlers and the CLI delegates all decisions to the same service implementation.
**Failing test assertion**: Assert a query or refresh request routes through the shared service and preserves the existing pyright tools unchanged.
**Domains touched**: .claude/domains/10_observability.md, .claude/domains/16_ops_governance.md, .claude/domains/17_code_patterns.md

## Test Specification

Assert a query or refresh request routes through the shared service and preserves the existing pyright tools unchanged.

## Security Review

N/A — adapter wiring only; no new external trust boundary.

## Functional Goal

**Story Goal**: Phase 2: Foundational (Blocking Prerequisites)
**Acceptance Criteria**: Search/status/refresh are exposed through the existing MCP server and a thin CLI wrapper.

## Quality Guards

`.claude/domains/10_observability.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
