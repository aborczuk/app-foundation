---
feature_id: "019"
task_id: "T024"
---

# HUD: T024 — Implement default stdout suppression and three-line status emission in scripts/pipeline_driver.py:emit_human_status

## Working Memory

**File:Symbol**: `scripts/pipeline_driver.py:emit_human_status`  
**Callers**: `Resolve via codegraph analyze_code_relationships during implementation`  
**Reuse path**: net-new bounded implementation in existing architecture

## Solution Sketch

Modify: `scripts/pipeline_driver.py:emit_human_status`
Create: none (consumes shared status constants/renderer from `pipeline_driver_contracts.py`).
Reuse: all success/blocked/tooling paths emit status via the shared renderer contract.
Composition: `emit_human_status` delegates to shared contract module; no duplicated prefix formatting.
Test Specification: assert deterministic blocked/tooling outcomes and stable reason fields.
Domains touched: Domain 07, Domain 13, Domain 14, Domain 17

## Test Specification

Assert deterministic behavior for this task path including blocked/tooling routing and no hidden side effects.

## Security Review

N/A — in-repo primitives only; enforce input validation and no secret leakage.

## Functional Goal

**Story Goal**: Enforce canonical step-result envelopes and minimal human output while preserving structured diagnostics for drill-down.  
**Acceptance Criteria**: Exit-code-first parsing and strict Done/Next/Blocked output are preserved while diagnostics remain available via explicit drill-down.

## Quality Guards

Domain 07 (if orchestration code touched), Domain 12 (if tests touched), Domain 13, Domain 14, Domain 17

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
