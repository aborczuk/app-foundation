---
feature_id: "019"
task_id: "T048"
---

# HUD: T048 — Add preflight branch-sync stale-contract guard in scripts/speckit_implement_gate.py:_task_preflight (with reason-code mapping in docs/governance/gate-reason-codes.yaml) so `.implement` blocks when target task exists on `main` but not current feature branch

## Working Memory

**File:Symbol**: `scripts/speckit_implement_gate.py:_task_preflight`  
**Current line**: `54`  
**Callers**: `scripts/speckit_implement_gate.py:main`  
**Reuse path**: reuse existing preflight result envelope + deterministic reason-code reporting

## Solution Sketch

Modify: `scripts/speckit_implement_gate.py:_task_preflight` to detect task-contract divergence between current branch and `main`.  
Create: small helper for probing `main` task presence without mutating branch state.  
Reuse: existing preflight reasons and JSON gate payload structure.  
Composition: when current branch lacks task but `main` contains it, return blocked preflight with a stale-branch reason and remediation hint.

## Test Specification

Assert preflight returns `ok=false` with stale-branch reason when target task is missing on current branch but present on `main`, and returns normal pass path when branch task contract is up to date.

## Security Review

Use read-only git inspection only; never mutate refs or run destructive operations during preflight checks.

## Functional Goal

**Story Goal**: Keep deterministic pipeline execution aligned with authoritative feature task contracts.  
**Acceptance Criteria**: `.implement` blocks early on stale branch task-contract mismatch instead of entering reconcile loops.

## Quality Guards

Domain 16, Domain 13, Domain 14, Domain 17

## Process Checklist

- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
