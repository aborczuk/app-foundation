---
feature_id: "020"
task_id: "T008"
---

# HUD: T008 — Implement code-symbol query ranking and result shaping

## Working Memory

**File:Symbol**: `src/mcp_codebase/index/service.py:VectorIndexService.query`
**Current line**: new module / symbol does not exist yet
**Callers**: MCP adapter, CLI adapter, and integration tests
**Reuse path**: existing service orchestration and typed result contracts

## Solution Sketch
**Modify**: src/mcp_codebase/index/service.py:VectorIndexService.query — Implement code-symbol query ranking and result shaping
**Create**: as required by the slice; keep net-new surface minimal
**Reuse**: existing service orchestration and typed result contracts
**Composition**: Query ranking and scope filtering stay in the service layer, and the result model packages the file span and provenance needed to jump directly into the read tool. Empty queries return an empty result set rather than an exception.
**Failing test assertion**: A code-only query returns ranked results with complete metadata and an empty query path returns an empty list rather than an error.
**Domains touched**: .claude/domains/04_caching_performance.md, .claude/domains/10_observability.md, .claude/domains/14_security_controls.md, .claude/domains/17_code_patterns.md

## Test Specification

A code-only query returns ranked results with complete metadata and an empty query path returns an empty list rather than an error.

## Security Review

No new trust boundary; ensure the query path does not leak internal file contents beyond the returned metadata.

## Functional Goal

**Story Goal**: Phase 3: User Story 1 - Semantic Symbol Lookup (Priority: P1) 🎯 MVP
**Acceptance Criteria**: Code-only queries return ranked results with enough provenance to jump directly into read-code.

## Quality Guards

`.claude/domains/04_caching_performance.md`, `.claude/domains/10_observability.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
