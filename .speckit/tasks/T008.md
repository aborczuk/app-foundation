---
feature_id: "022"
task_id: "T008"
---

# HUD: T008 — Add regression tests for lock contention, unreadable graph state, and query-failure recovery hints in `tests/integration/test_codegraph_recovery.py`

## Working Memory

**File:Symbol**: `tests/integration/test_codegraph_recovery.py:test_lock_and_query_failure_modes`  
**Current line**: new test module / symbol does not exist yet  
**Callers**: `pytest`  
**Reuse path**: the safe index wrapper behavior in `scripts/cgc_safe_index.sh` and the health contract from `src/mcp_codebase/health.py`

## Solution Sketch

**Modify**: none.  
**Create**: `tests/integration/test_codegraph_recovery.py`.  
**Reuse**: the safe index wrapper and the shared health classifier.  
**Composition**: the test drives a deliberately unhealthy graph state and confirms that the recovery hint changes with the failure class rather than collapsing to a single opaque error.  
**Failing test assertion**: the test should fail until each failure mode emits its own recovery guidance and the command path remains deterministic.  
**Domains touched**: `.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/17_code_patterns.md`

## Test Specification

Assert that lock contention, unreadable graph state, and query failure each yield a distinct recovery hint.

## Security Review

N/A — local integration regression only.

## Functional Goal

**Story Goal**: User Story 2 - Agent-Facing Recovery on Lock / Query Failure  
**Acceptance Criteria**: The system reports the failure mode and the next action instead of a generic crash.

## Quality Guards

`.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
