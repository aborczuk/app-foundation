---
feature_id: "020"
task_id: "T011"
---

# HUD: T011 — Implement markdown query ranking and preview shaping

## Working Memory

**File:Symbol**: `src/mcp_codebase/index/service.py:VectorIndexService.query`
**Current line**: new module / symbol does not exist yet
**Callers**: MCP adapter, CLI adapter, and integration tests
**Reuse path**: existing service/query shape and markdown section contract

## Solution Sketch
**Modify**: src/mcp_codebase/index/service.py:VectorIndexService.query — Implement markdown query ranking and preview shaping
**Create**: as required by the slice; keep net-new surface minimal
**Reuse**: existing service/query shape and markdown section contract
**Composition**: Markdown query ranking mirrors code query behavior but returns breadcrumbed sections and previews, keeping the adapter contract consistent across content types. Scope filtering must continue to separate code and markdown results cleanly.
**Failing test assertion**: A markdown-only query returns section results with breadcrumb and preview while a code-only scope excludes markdown entries.
**Domains touched**: .claude/domains/04_caching_performance.md, .claude/domains/10_observability.md, .claude/domains/14_security_controls.md, .claude/domains/17_code_patterns.md

## Test Specification

A markdown-only query returns section results with breadcrumb and preview while a code-only scope excludes markdown entries.

## Security Review

No new trust boundary; keep content previews bounded and local-only.

## Functional Goal

**Story Goal**: Phase 4: User Story 2 - Markdown Section Discovery (Priority: P2)
**Acceptance Criteria**: Markdown-only queries return section results with breadcrumb and preview while code-only scope excludes markdown entries.

## Quality Guards

`.claude/domains/04_caching_performance.md`, `.claude/domains/10_observability.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
