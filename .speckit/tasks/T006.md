---
feature_id: "022"
task_id: "T006"
---

# HUD: T006 — Add integration coverage for the doctor command and the MCP health tool contract in `tests/integration/test_codegraph_health.py`

## Working Memory

**File:Symbol**: `tests/integration/test_codegraph_health.py:test_doctor_and_mcp_health_contract`  
**Current line**: new test module / symbol does not exist yet  
**Callers**: `pytest`  
**Reuse path**: the subprocess test style already present in `.speckit/acceptance-tests/` and the shared health contract from `src/mcp_codebase/health.py`

## Solution Sketch

**Modify**: none.  
**Create**: `tests/integration/test_codegraph_health.py`.  
**Reuse**: the shared health contract and subprocess-style contract tests already used elsewhere in the repo.  
**Composition**: the test runs the doctor command and the MCP health tool contract against the same repo state, then compares the emitted status and recovery hint.  
**Failing test assertion**: the test should fail until both surfaces produce the same next-action guidance for a healthy and unhealthy checkout.  
**Domains touched**: `.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Test Specification

Assert that the doctor command and the MCP health tool agree on status and recovery hint for the same repo state.

## Security Review

N/A — local subprocess / adapter contract test only.

## Functional Goal

**Story Goal**: User Story 1 - Graph Health Check for Developers  
**Acceptance Criteria**: The doctor command and MCP surface expose the same deterministic health result.

## Quality Guards

`.claude/domains/10_observability.md`, `.claude/domains/11_resilience_continuity.md`, `.claude/domains/12_testing_quality_gates.md`, `.claude/domains/13_identity_access_control.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/16_ops_governance.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
