---
feature_id: "020"
task_id: "T006"
---

# HUD: T006 — Add the code-symbol lookup integration test

## Working Memory

**File:Symbol**: `tests/integration/test_codebase_vector_index.py:test_code_symbol_lookup_returns_metadata`
**Current line**: new test file / symbol does not exist yet
**Callers**: future `VectorIndexService.query` and fixture setup
**Reuse path**: existing `tests/integration/` style and repo-local fixture patterns

## Solution Sketch
**Modify**: tests/integration/test_codebase_vector_index.py:test_code_symbol_lookup_returns_metadata — Add the code-symbol lookup integration test
**Create**: as required by the slice; keep net-new surface minimal
**Reuse**: existing `tests/integration/` style and repo-local fixture patterns
**Composition**: The integration test builds the index over representative source, exercises code-symbol search, and checks that rank, top-K, and metadata are actionable for `read_code_context` follow-up. It should fail until the query path and result shaping exist.
**Failing test assertion**: Querying a known code concern returns the correct symbol in the top results with file path, line range, and symbol type.
**Domains touched**: .claude/domains/12_testing_quality_gates.md, .claude/domains/14_security_controls.md, .claude/domains/17_code_patterns.md

## Test Specification

Querying a known code concern returns the correct symbol in the top results with file path, line range, and symbol type.

## Security Review

Fixture-only; no external services.

## Functional Goal

**Story Goal**: Phase 3: User Story 1 - Semantic Symbol Lookup (Priority: P1) 🎯 MVP
**Acceptance Criteria**: Index built over a repo fixture; a semantic code-symbol query returns ranked results with full metadata and empty-result behavior.

## Quality Guards

`.claude/domains/12_testing_quality_gates.md`, `.claude/domains/14_security_controls.md`, `.claude/domains/17_code_patterns.md`

## Process Checklist
- [ ] discovery_completed
- [ ] lld_recorded
- [ ] quality_guards_passed
- [ ] functional_goal_achieved
- [ ] tests_passed
