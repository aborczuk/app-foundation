---
feature_id: "022"
task_id: "T014"
---

# HUD: T014 — Add the dedicated recovery integration suite in tests/integration/test_codegraph_recovery.py:test_lock_and_query_failure_modes

## Working Memory

**File:Symbol**: `tests/integration/test_codegraph_recovery.py:test_lock_and_query_failure_modes`  
**Callers**: `Resolve via codegraph analyze_code_relationships during implementation`  
**Reuse path**: net-new bounded implementation in existing architecture

## Solution Sketch

Modify: `tests/integration/test_codegraph_recovery.py:test_lock_and_query_failure_modes`
Create: `tests/integration/test_codegraph_recovery.py`
Reuse: `tests/integration/test_codegraph_health.py` patterns, `scripts/cgc_doctor.sh`, and the shared graph-health classifier.
Composition: drive stale, locked, unreadable, and refreshed repo states through the doctor CLI and assert the recovery hint stays deterministic.
Test Specification: assert local edit invalidation, lock contention, and refresh/rebuild behavior emit the correct status and next-action guidance.
Domains touched: Domain 04, Domain 10, Domain 11, Domain 12, Domain 13, Domain 14, Domain 17

## Test Specification

Assert deterministic recovery behavior for stale symbol reads, lock contention, and refresh/rebuild cycles with no hidden side effects.

## Security Review

N/A — in-repo primitives only; enforce input validation and no secret leakage.

## Functional Goal

**Story Goal**: Prove the codegraph recovery path fails gracefully until the graph is refreshed or rebuilt, then recovers to a healthy state.  
**Acceptance Criteria**: Stale symbol queries, lock contention, unreadable state, and refresh/rebuild transitions all report the correct recovery hint and final healthy state.

## Quality Guards

Domain 04 (if timeout behavior is touched), Domain 10, Domain 11, Domain 12, Domain 13, Domain 14, Domain 17

## Process Checklist

- [X] discovery_completed
- [X] lld_recorded
- [X] quality_guards_passed
- [X] functional_goal_achieved
- [X] tests_passed
